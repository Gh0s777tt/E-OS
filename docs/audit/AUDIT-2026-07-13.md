# E-OS — Audyt repozytorium (ETAP 1)

**Data:** 2026-07-13  ·  **Zakres tej sekcji:** repozytorium główne `E-OS` (meta / fork cookbooka Redox), HEAD `5e7e44a7`.

> **Metoda:** workflow 6 równoległych finderów (4 wymiary × powierzchnia E-OS-owa) → **adwersaryjna weryfikacja każdego findingu** przeciw realnej treści pliku (44 agenty, 0 błędów). Audytowana wyłącznie powierzchnia **autorstwa E-OS** — 3347 plików upstream cookbooka Redox pominięto celowo. Poprawki **NIE** wprowadzone — czekają na akceptację.

Kolejne sekcje (warstwa krytyczna: kernel/relibc/redoxfs/base/bootloader, potem reszta forków, `eos-pkg-*`) zostaną dopisane do tego pliku.

## Podsumowanie — repo główne

Potwierdzone findingi: **33**  ·  odrzucone jako false-positive (adwersaryjnie): **5** z 38 surowych.

| Waga | Liczba |   | Wymiar | Liczba |
|------|:------:|---|--------|:------:|
| 🔴 KRYTYCZNE | 0 |   | A · Jakość kodu | 7 |
| 🟠 WYSOKIE | 2 |   | B · Wydajność | 0 |
| 🟡 ŚREDNIE | 16 |   | C · Bezpieczeństwo | 14 |
| ⚪ NISKIE | 15 |   | D · Uporządkowanie | 12 |

**0 findingów KRYTYCZNYCH.** Większość istotnych dotyczy **łańcucha zaufania pakietów/release** (podpisy istnieją, ale nie są wpięte w publikację — zgodne z własnym roadmapem R-503/R-703) oraz **spójności docs/CI** (twierdzenia o działającym CI mimo wyłączonych Actions). Reszta to hardening skryptów i drobne nity Rust.

## 🟠 WYSOKIE (2)

### M-01 · `.gitlab-ci.yml:14`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `secret-scan-coverage-gap`

**Problem.** The live GitLab secret-scan job runs `gitleaks detect --source .` on a GitLab CI checkout, which by default is a SHALLOW clone (no GIT_DEPTH:0 / variables block exists in the file), so gitleaks only scans the last handful of commits and misses secrets committed earlier in history.

**Dowód.** `L14: `- gitleaks detect --source . --no-banner --redact -v`  (no `variables:`/`GIT_DEPTH` anywhere in the 22-line file). Contrast: the sibling .github/workflows/gitleaks.yml deliberately sets `fetch-depth: 0   # full history so leaks in old commits are caught`.`

**Poprawka.** Add a full-clone directive so the only enforced secret gate scans all history, e.g. add at top level:
  variables:
    GIT_DEPTH: 0
(or `GIT_STRATEGY: clone` with depth 0) so `gitleaks detect` sees the complete commit history like the GitHub job intended.

### M-02 · `scripts/publish-repo-pages.sh:42`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `tofu-key-distribution`

**Problem.** The ed25519 package-signing public key is published to the same GitHub Pages host as the packages it authenticates (pkg/id_ed25519.pub.toml), a trust-on-first-use model where a host/MITM controlling the endpoint can substitute both key and packages.

**Dowód.** `lines 41-45 copy build/id_ed25519.pub.toml into $stage/pkg/; index.html at line 57 links `pkg/id_ed25519.pub.toml` for clients to fetch`

**Poprawka.** Pin the E-OS signing pubkey into the client image (roadmap R-702 'kill TOFU') and treat the copy served next to the repo only as a convenience mirror that is checked against the pinned key, never as the source of trust.

## 🟡 ŚREDNIE (16)

### M-03 · `.github/workflows/ci.yml`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `dead-ci-config`

**Problem.** All 7 .github/workflows/*.yml plus the dependabot 'github-actions' updater are dead weight — Actions is disabled account-wide (ROADMAP line 207), so none run — yet the config is retained, Dependabot still opens PRs to bump actions in the dead pipeline (tracked as R-007), and contributor docs assert checks that never execute (CONTRIBUTING.md:38 'keep CI green'; PULL_REQUEST_TEMPLATE.md 'gitleaks/push-protection will check'), misleading contributors into believing gating exists.

**Dowód.** `.github/workflows/ contains build.yml, cargo-audit.yml, ci.yml, codeql.yml, gitleaks.yml, pages.yml, release.yml; CONTRIBUTING.md:38 `Run `rustfmt` on Rust changes; keep CI green.`; dependabot.yml:4 `- package-ecosystem: "github-actions"``

**Poprawka.** Either add a top-of-file comment to each workflow marking it inert-until-R-004 (and drop the github-actions ecosystem from dependabot.yml so it stops churning PRs on a dead pipeline), or move the workflows to a .github/workflows.disabled/ dir; and reword CONTRIBUTING.md:38 / the PR template to reference the local gitleaks/cargo-audit hooks (R-005) instead of nonexistent CI gating.

### M-04 · `CHANGELOG.md:1`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `changelog-truncated`

**Problem.** CHANGELOG.md ships as a malformed fragment: it begins mid-list at line 1 (`- `[U-071]` **E-OS Settings...`) with no `# Changelog` title, no Keep-a-Changelog/SemVer preamble+legend, no `## [Unreleased]` or `## [0.1.0]` version headers, an orphan `### Fixed` at line 67, and it lost every entry U-001..U-070 plus the entire released-0.1.0 history — yet README.md line 305 advertises Keep-a-Changelog compliance and 'Every change is recorded, numbered (U-NNN)'.

**Dowód.** `line 1: `- `[U-071]` **E-OS Settings — native Crimson control panel...`  (only 101 lines, only U-071..U-080; grep '^#' returns only line 67 `### Fixed`)`

**Poprawka.** Restore the full changelog: commit f3ef3ca4 truncated it from 838→15 lines, so recover the version at commit 34731c52 (`# 📓 Changelog` title + Keep-a-Changelog/SemVer intro + legend + `## [Unreleased]` with `### Added/### Changed/### Fixed` + `## [0.1.0] — "Genesis" — 2026-06-06`) and re-append U-071..U-080 under `## [Unreleased]`.

### M-05 · `HARDWARE.md:3`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `upstream-copy-naming`

**Problem.** Root HARDWARE.md is a verbatim UPSTREAM Redox document ('This document tracks the current hardware compatibility of Redox OS'), placed at the E-OS repo root without the `REDOX-` prefix/upstream-marking convention used elsewhere (docs/REDOX-README.md, docs/REDOX-CONTRIBUTING.md), unbranded, not in SUMMARY.md, and overlapping E-OS's own docs/hardware-matrix.md — it reads as if it were E-OS's hardware status.

**Dowód.** `HARDWARE.md:3 `This document tracks the current hardware compatibility of Redox OS.` (only referenced from ROADMAP.md:316 `HARDWARE.md:40`, not from README/SUMMARY)`

**Poprawka.** Rename to docs/REDOX-HARDWARE.md to match the upstream-copy convention and list it in SUMMARY.md under 'Upstream (Redox OS)', or delete it as redundant with docs/hardware-matrix.md and repoint the ROADMAP R-916 citation accordingly.

### M-06 · `ROADMAP.md:89`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `roadmap-self-contradiction`

**Problem.** R-303 states in present/perfect tense that 'the release-artifact pipeline is live (release.yml...)' and 'The CI image build works — build.yml built a branded x86_64 image in GitHub Actions in ~12 min... artifact uploaded' (and R-301 line 78 marks signed release ✅ via release.yml), directly contradicting the same document's R-0xx section at line 207 which says 'GitHub Actions is disabled account-wide (HTTP 422, 0 runs complete), so every advertised pipeline — CI image build (R-303), release signing (R-301)... is inert.'

**Dowód.** `ROADMAP.md:89 `The **CI image build works** — `build.yml` built a branded x86_64 image in GitHub Actions in **~12 min**` vs :207 `GitHub Actions is disabled account-wide ... every advertised pipeline — CI image build (R-303), release signing (R-301) ... is inert.``

**Poprawka.** Rewrite R-303/R-301 to past tense with the disabled-Actions caveat (e.g. 'built successfully before Actions was disabled account-wide; currently inert, see R-004'), so the roadmap does not claim a live CI/release pipeline it also declares dead.

### M-07 · `SECURITY.md:69`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `security-doc-overstated`

**Problem.** The 'How we harden this repository' section presents CI-based protections as active — 'gitleaks in CI', 'CodeQL (code scanning)', checksums 'minisign-signed in CI' — with no caveat, even though GitHub Actions is disabled account-wide (ROADMAP line 207) so all three are inert; this contradicts README (which does carry the caveat, lines 172/279-280) and ROADMAP R-003 (line 211) which claims DONE: 'mark CodeQL/gitleaks/cargo-audit/release-signing as Actions-blocked in SECURITY.md/README'.

**Dowód.** `SECURITY.md:69 `🔑 **Secret scanning + push protection** and **gitleaks** in CI`; :71 `🤖 **Dependabot** (dependencies) and **CodeQL** (code scanning).``

**Poprawka.** Add the same 'GitHub Actions disabled account-wide — job inert (ROADMAP R-004)' caveat the README uses to the gitleaks/CodeQL/minisign-in-CI bullets, or state they run locally (launchd/git hooks, R-005) instead of 'in CI'.

### M-08 · `config/aarch64/eos.toml:140`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `duplication`

**Problem.** The entire ~500-line COSMIC dark-theme block (all com.system76.CosmicTheme.Dark/v2/* RON [[files]] entries plus the E-OS identity files) is copy-pasted byte-for-byte between config/x86_64/eos.toml and config/aarch64/eos.toml, and the two files have already structurally diverged (the e1000e NIC pcid overlay and the 30_serial-getty ordering exist only in x86_64), so any theme or branding edit must be made twice and will silently drift out of sync.

**Dowód.** `diff of the two files shows the CosmicTheme path set is identical ("THEME PATHS IDENTICAL") yet x86_64 alone carries the e1000e overlay (lines 137-146) and places 30_serial-getty differently; both files already `include = ["../desktop.toml"]`.`

**Poprawka.** Factor the shared identity + theme [[files]] into a new config/eos-common.toml and `include` it from both config/x86_64/eos.toml and config/aarch64/eos.toml (the installer already supports include, used for ../desktop.toml), leaving only the genuinely arch-specific entries (e1000e overlay, serial-getty) in the per-arch files.

### M-09 · `config/x86_64/eos.toml:102`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `default-credentials`

**Problem.** The shipped /etc/issue login banner hardcodes the literal default credentials ("Login:  user  (no password)   *   root / password"), which E-OS also repeats in /etc/motd-adjacent Welcome.txt (line 126), config/aarch64/eos.toml (lines 99, 123) and the eos-welcome script (recipes/other/eos/recipe.toml line 34); /etc/issue is redrawn at every serial/text login prompt, so on an installed system the R-602 OOBE changes the actual root password but never rewrites this banner, leaving it both factually wrong and broadcasting the exact default-credential scheme the product's headline hardening is supposed to remove.

**Dowód.** `config/x86_64/eos.toml:102  "  Login:  user  (no password)   *   root / password" (postinstall /etc/issue block, lines 88-104); identical aarch64:99`

**Poprawka.** Drop the literal "user (no password) * root / password" line from the persistent /etc/issue [[files]] block in both config/{x86_64,aarch64}/eos.toml (keep a neutral "log in as the account you created during first-boot setup"), or have the R-602 OOBE rewrite /etc/issue when it forces the password change so the banner cannot go stale or advertise the defaults post-install.

### M-10 · `config/x86_64/eos.toml:102`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `credential-disclosure`

**Problem.** The E-OS-authored /etc/issue login banner prints the default root password to anyone who can see the console login prompt, including the newly-added serial/debug getty.

**Dowód.** `Login:  user  (no password)   *   root / password`

**Poprawka.** Remove the literal credentials from /etc/issue; show only the E-OS version and a note to run eos-welcome. Keep the 'default accounts / change after install' guidance in Welcome.txt (post-login) rather than on the pre-auth banner. Same change in config/aarch64/eos.toml line 99.

### M-11 · `docs/SUMMARY.md`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `docs-summary-coverage`

**Problem.** 8 docs/*.md files are absent from SUMMARY.md, so mdBook (pages.yml runs `mdbook build`, which only includes SUMMARY-listed pages) will silently omit them from the docs site — including the user-facing docs/hardware-capabilities-roadmap.md which README (lines 222,249) and ROADMAP (line 150) link to as the storage/crypto track.

**Dowód.** `Orphans not in SUMMARY.md: acpi-off-removal-plan.md, driver-manager-design.md, feature-proposals.md, hardware-capabilities-roadmap.md, reality-ledger.md, roadmap-connectivity.md, update-system-design.md, upstream-reuse-analysis.md`

**Poprawka.** Add hardware-capabilities-roadmap.md (and any other user-facing ones) to SUMMARY.md under an appropriate heading; for the internal-planning docs (feature-proposals, reality-ledger, driver-manager-design, update-system-design, acpi-off-removal-plan, upstream-reuse-analysis, roadmap-connectivity) either list them under a 'Design / internal' section or move them out of docs/ so mdBook coverage is deliberate.

### M-12 · `release/SHA256SUMS:1`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `unsigned-release-artifact`

**Problem.** release/SHA256SUMS ships with no accompanying SHA256SUMS.minisig, yet keys/README.md documents `minisign -Vm SHA256SUMS -p eos-release.pub` as the verification step — so the checksum file is unauthenticated and the documented verify procedure fails out of the box, leaving the two committed hashes unverifiable against any published image.

**Dowód.** `find . -name '*.minisig' => none; keys/README.md:12 `minisign -Vm SHA256SUMS -p eos-release.pub`; release/SHA256SUMS contains two `*.img` hashes but no signature and no images`

**Poprawka.** Produce and commit SHA256SUMS.minisig via make-release.sh (MINISIGN_SECRET_KEY set) alongside SHA256SUMS, or clearly mark the committed SHA256SUMS as a build-time placeholder so a downstream user does not treat unsigned/phantom checksums as a trust anchor.

### M-13 · `sbom/eos-0.1.0-x86_64.cdx.json`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `sbom-accuracy`

**Problem.** The shipped SBOMs are 54 of 59 components version 'TODO'/'unknown' (identical in the aarch64 SBOM), so the artifact whose purpose is component/version provenance conveys almost no usable version data and misrepresents supply-chain coverage.

**Dowód.** `components include base@TODO, bash@TODO, ca-certificates@TODO, ...; 54/59 have version in {TODO,unknown}; gen-sbom.py:43 `ver = m.get("version", "unknown")` records whatever the cooked meta contains`

**Poprawka.** Populate real versions in the repo/<target>/*.toml cooked metadata (or derive from source_identifier / recipe pinned ref) before running gen-sbom.py, and have gen-sbom.py fail or warn loudly when >X% of components resolve to TODO/unknown so a placeholder SBOM is never shipped as final.

### M-14 · `scripts/eos-setup-mirrors.sh:44`
**Wymiar:** A · Jakość kodu  ·  **Kategoria:** `fragile-error-handling`

**Problem.** The mirror-existence check only catches JSON *decode* errors; if the GitLab API returns a JSON object instead of a list (e.g. a 404 `{"message":...}` or auth error), `any(... for m in d)` raises AttributeError, and under `set -euo pipefail` (plus glab's own non-zero exit through the pipe) the whole mirror-setup run aborts mid-loop.

**Dowód.** `L44-47: `has=$(glab api "projects/$enc/remote_mirrors" 2>/dev/null | python3 -c "import sys,json\ntry: d=json.load(sys.stdin)\nexcept: print('0'); sys.exit()\nprint('1' if any('github' in m.get('url','') for m in d) else '0')")` — the bare `except` wraps only `json.load`, not the `any(...)` iteration.`

**Poprawka.** Guard the type and add a fallback so one bad repo doesn't kill the batch: `d = json.load(sys.stdin); print('1' if isinstance(d, list) and any('github' in m.get('url','') for m in d) else '0')`, and append `|| echo 0` to the command substitution to absorb glab's non-zero exit under pipefail.

### M-15 · `scripts/local-scan.sh:14`
**Wymiar:** A · Jakość kodu  ·  **Kategoria:** `swallowed-audit-result`

**Problem.** cargo-audit's exit status is swallowed by `|| true`, so the local security scan reports success even when cargo audit finds vulnerable dependencies; a real CVE in Cargo.lock never fails the scan.

**Dowód.** `L14: `    [ -f "$lock" ] && ( cd "$(dirname "$lock")" && cargo audit ) || true` — the trailing `|| true` masks a non-zero `cargo audit` (which is how it signals advisories), and rc is never set from it.`

**Poprawka.** Separate the file-existence guard from the audit result and propagate failure, e.g.:
  if [ -f "$lock" ]; then ( cd "$(dirname "$lock")" && cargo audit ) || rc=1; fi
so advisories cause the scan (and the launchd timer) to report FAIL.

### M-16 · `scripts/publish-repo-pages.sh:40`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `trust-chain-gap`

**Problem.** The hybrid ed25519+ML-DSA-65 signer (tools/eos-repo-sign) is never invoked by any publish path — publish-repo-pages.sh copies repo.toml and the .pkgar files but emits no repo.toml.sig, and no client verifies one, so the entire PQ signing story authenticates nothing at runtime.

**Dowód.** `line 40: cp "$REPO"/repo.toml "$REPO"/*.pkgar "$stage/pkg/$TARGET/"  (no sign step); docs/update-system-design.md:149 calls this 'the R-503 "security theater as shipped" gap'`

**Poprawka.** Wire `tools/eos-repo-sign sign <secret> repo.toml` into publish-repo-pages.sh, copy the resulting repo.toml.sig into $stage/pkg/$TARGET/, and add fetch+verify of that signature in pkg-lib before any per-package toml is trusted (roadmap R-703). Until then the ML-DSA path provides zero protection.

### M-17 · `scripts/sync-forks.sh:33`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `token-in-process-args`

**Problem.** The GitHub token is embedded directly in the push URL passed as a `git push` argument, exposing it in the process list (`ps`, `/proc/<pid>/cmdline`) to other users on a shared build/dev host.

**Dowód.** `L33: `[ -n "$TOK" ] && ourspush="https://x-access-token:${TOK}@github.com/Gh0s777tt/eos-${name}.git" || ourspush="$ours"` and L44: `git -C m.git push -q "$ourspush" "$u:refs/heads/master" 2>/dev/null` — the token-bearing URL is on the command line.`

**Poprawka.** Keep the token out of argv: use a credential helper / GIT_ASKPASS, or pass it via header on stdin, e.g. `git -C m.git -c http.extraHeader="Authorization: Basic $(printf 'x-access-token:%s' "$TOK" | base64)" push -q "$ours" "$u:refs/heads/master"`, and push to the tokenless `$ours` URL.

### M-18 · `tools/eos-repo-sign/src/main.rs:104`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `data-loss-key-overwrite`

**Problem.** `keygen` writes the secret-key file with `std::fs::write`, which silently truncates/overwrites any existing file, so re-running keygen over an existing secret.toml destroys the only copy of the repo signing key and breaks the trust chain (would be WYSOKIE if used in production key management).

**Dowód.** `std::fs::write(secret_path, secret).unwrap_or_else(|e| die(format!("write {secret_path}: {e}"))); — comment even says keys must be backed up "OFF-REPO" yet the tool clobbers without asking.`

**Poprawka.** Before writing, check `std::path::Path::new(secret_path).exists()` and refuse (die with a clear message) unless a `--force` flag is passed, protecting the existing secret key from accidental overwrite.

## ⚪ NISKIE (15)

### M-19 · `.editorconfig`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `missing-editorconfig`

**Problem.** No .editorconfig at repo root, despite a multi-language tree (Rust, shell, Python, TOML, Markdown) and contributor guidance that relies only on rustfmt (CONTRIBUTING.md:38) — editors won't get consistent indentation/charset/EOL for the non-Rust files.

**Dowód.** ``ls .editorconfig` → NO .editorconfig (repo has .gitignore, .gitleaks.toml, rust-toolchain.toml but no .editorconfig)`

**Poprawka.** Add a minimal .editorconfig (root=true; utf-8; lf; final-newline; trim trailing whitespace; 4-space Rust/Python, 2-space YAML/TOML) so non-Rust files stay consistent.

### M-20 · `.gitlab-ci.yml:3`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `misleading-inconsistency`

**Problem.** The CI comment describes the mirror direction backwards relative to the rest of the tooling: it says CI runs on pushes 'mirrored from GitHub', but eos-setup-mirrors.sh and repos.toml define GitLab as the source of truth and GitHub as a read-only push-mirror (GitLab -> GitHub), which misrepresents which repo is authoritative.

**Dowód.** `.gitlab-ci.yml L2-4: `... run here instead — on\n# every push mirrored from GitHub.`  vs  scripts/eos-setup-mirrors.sh L1-3: `create GitLab -> GitHub push mirrors ... so GitHub stays a read-only mirror of the GitLab source of truth` and repos.toml: `gitlab = source of truth ... github = read-only mirror`.`

**Poprawka.** Correct the comment to reflect the actual model, e.g. 'run here on every push to the GitLab source of truth (GitHub is a downstream read-only mirror).'

### M-21 · `.gitleaks.toml:18`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `gitleaks-allowlist-too-broad`

**Problem.** The allowlist blanket-excludes all of Cargo.lock from secret scanning, so a credential accidentally committed inside a git source line (e.g. `source = "git+https://user:token@host/..."`) would be silently ignored; also `regexTarget="match"` is a no-op since no `regexes` are defined.

**Dowód.** `L12-18 paths include `'''Cargo\.lock$''',` and L21 `regexTarget = "match"` with no `[allowlist].regexes` present.`

**Poprawka.** Drop the blanket `Cargo\.lock$` path allowlist (Cargo.lock rarely needs it and can carry tokens in git URLs), or replace it with a narrow allowlist regex for the specific known-safe fixture; remove the dangling `regexTarget` line or pair it with an actual `regexes` list.

### M-22 · `README.md:10`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `external-asset-dependency`

**Problem.** The README renders its banner/badges/typing-tagline from four third-party image services (capsule-render.vercel.app, readme-typing-svg.demolab.com, img.shields.io, raw.githubusercontent.com/andreasbm); any outage or takedown leaves the landing page with broken images, and it sits at odds with the project's 'reproducible / self-hosted' ethos.

**Dowód.** `README.md:10 `<img src="https://capsule-render.vercel.app/api?..." alt="E-OS" />`; :14 readme-typing-svg.demolab.com; :46/:332 raw.githubusercontent.com/andreasbm/readme rainbow line`

**Poprawka.** Vendor the decorative banner/divider as static images under assets/ (already used for screenshots) and keep only the stable img.shields.io static badges, or accept the risk and note it — low priority, purely cosmetic robustness.

### M-23 · `rust-toolchain.toml:3`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `missing-shared-lint-config`

**Problem.** There is no shared rustfmt.toml, clippy.toml, or .editorconfig anywhere in the repo, and no CI job invokes fmt/clippy, even though rust-toolchain.toml line 3 installs both `rustfmt` and `clippy` — so formatting/lint standards are declared-but-unenforced and each ecosystem repo is free to drift.

**Dowód.** `rust-toolchain.toml: components = ["rust-src", "rustfmt", "clippy"]; find for rustfmt.toml/clippy.toml/.editorconfig returns nothing; grep of .github/ + .gitlab-ci.yml for clippy/rustfmt/`cargo fmt` returns nothing.`

**Poprawka.** Add ONE canonical rustfmt.toml, clippy.toml, and .editorconfig at the ecosystem root (or a shared config repo the 24 repos reference), and wire a `cargo fmt --check` + `cargo clippy -- -D warnings` gate into CI so the installed components are actually used.

### M-24 · `scripts/ci-integrity.sh:11`
**Wymiar:** A · Jakość kodu  ·  **Kategoria:** `overbroad-grep-scope`

**Problem.** The 'our own Rust sources' debug/password guard actually greps the entire tree (only excluding /target/), so vendored/built upstream *.rs under recipes/*/source/ during a populated checkout could trip the gate with false positives; harmless today only because few *.rs are tracked (currently matches nothing).

**Dowód.** `L11: `hits=$(grep -rInE 'println!\s*\(.*[Pp]assword|TODO:? *Remove this debug' --include='*.rs' . 2>/dev/null | grep -v '/target/' || true)` — comment on L10 says 'in our own Rust sources' but the path arg is `.` (whole repo).`

**Poprawka.** Scope the grep to E-OS-authored paths to match the intent, e.g. replace the trailing `.` with `src tools` (the E-OS Rust dirs), so upstream recipe sources can't produce false CI failures.

### M-25 · `scripts/eos-repos.sh:48`
**Wymiar:** A · Jakość kodu  ·  **Kategoria:** `arg-parsing-bug`

**Problem.** `clone` mis-parses `--from` when no directory is given: the flag is consumed as the target directory, so `eos-repos.sh clone --from github` clones into a dir literally named '--from' from the default gitlab remote instead of from github.

**Dowód.** `L48-50: `local dir="${1:-eos-ecosystem}" from="gitlab"` then `shift || true` then `[ "${1:-}" = "--from" ] && from="${2:-gitlab}"` — with args `--from github`, `dir` becomes `--from`.`

**Poprawka.** Parse the optional `--from VALUE` flag before binding the positional dir (e.g. a small getopts/case loop), or reject a DIR that starts with `--`.

### M-26 · `scripts/gen-sbom.py:9`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `unused-import`

**Problem.** `sys` is imported but never used anywhere in the script (grep for `sys.` returns nothing), which a linter would flag and clutters the import line.

**Dowód.** `import sys, os, json, glob, argparse   # `sys` never referenced in the file`

**Poprawka.** Drop `sys` from the import: `import os, json, glob, argparse`.

### M-27 · `scripts/gen-sbom.py:48`
**Wymiar:** A · Jakość kodu  ·  **Kategoria:** `sbom-robustness`

**Problem.** CycloneDX requires `bom-ref` values to be unique within a BOM, but component refs are built as `f"{name}@{ver}"`; two package .toml files with the same name+version would emit duplicate bom-refs and produce an invalid BOM, and `--timestamp` is likewise injected into metadata with no ISO-8601 validation.

**Dowód.** `"bom-ref": f"{name}@{ver}",  (line 48) and ap.add_argument("--timestamp", required=True ...) fed straight into metadata.timestamp (line 64) with no format check.`

**Poprawka.** Guarantee bom-ref uniqueness (e.g. de-duplicate or append the source path/blake3) and either validate `--timestamp` against ISO-8601 (datetime.fromisoformat) or document that the caller must pass a valid timestamp.

### M-28 · `tools/eos-repo-sign/Cargo.toml:14`
**Wymiar:** A · Jakość kodu  ·  **Kategoria:** `immature-crypto-dependency`

**Problem.** The post-quantum trust anchor depends on `ml-dsa = "0.1"` (locked 0.1.1), a pre-1.0, not-yet-audited RustCrypto implementation, which is a fragile basis for a security-critical FIPS-204 signature.

**Dowód.** `Cargo.toml:14 `ml-dsa = { version = "0.1", features = ["alloc"] }`; Cargo.lock ml-dsa 0.1.1`

**Poprawka.** Pin an exact version, track the crate toward a stabilized/audited release, and keep the ML-DSA path documented as advisory (not required) until the dependency matures; add `cargo audit` to CI for this tool.

### M-29 · `tools/eos-repo-sign/Cargo.toml:17`
**Wymiar:** D · Uporządkowanie  ·  **Kategoria:** `unused-dependency`

**Problem.** `signature = "2"` is declared as a direct dependency but main.rs never references `signature::` directly — the Signer/Verifier/Keypair traits are reached only via `ml_dsa::signature::` (signature 3.0.0) and `ed25519_dalek::` (signature 2.2.0 re-export), so the manifest line is dead and misleadingly implies the tool uses the signature-2 API.

**Dowód.** `Cargo.toml: `signature = "2"`; grep of main.rs shows only `ml_dsa::signature::{...}` (line 24) and no `use signature`; tool Cargo.lock lists both signature 2.2.0 (via ed25519-dalek) and 3.0.0 (via ml-dsa) independently of this line.`

**Poprawka.** Delete the `signature = "2"` line from tools/eos-repo-sign/Cargo.toml (the transitive versions ed25519-dalek/ml-dsa need are pulled in regardless).

### M-30 · `tools/eos-repo-sign/src/main.rs:177`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `signature-malleability`

**Problem.** ed25519 verification uses the non-strict `verify` instead of `verify_strict`, which accepts non-canonical S values and small-order/mixed-order public-key edge cases; low real risk for a keyed trust anchor but ed25519-dalek explicitly recommends strict verification for authentication contexts.

**Dowód.** `line 177: `let ed_ok = ed_vk.verify(&msg, &ed_sig).is_ok();``

**Poprawka.** Use `ed_vk.verify_strict(&msg, &ed_sig).is_ok()` so signatures must be canonical, matching the misuse-resistance goal stated in the module header.

### M-31 · `tools/eos-repo-sign/src/main.rs:164`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `missing-version-binding`

**Problem.** verify() never reads the version or algorithms fields it writes into the .sig, so verification enforces no version/algorithm binding; harmless today (both algorithms are hard-checked in full mode) but a v2 format change to the signed-message construction would be silently mis-verified by a v1 binary.

**Dowód.** `let sig = parse_kv(&sig_text); ... (no read of version / algorithms anywhere in verify)`

**Poprawka.** In verify(), read `version` from the sig map and die if it != SIG_VERSION, and assert `algorithms` matches the expected set, before running the crypto checks.

### M-32 · `tools/eos-repo-sign/src/main.rs:47`
**Wymiar:** A · Jakość kodu  ·  **Kategoria:** `panic-on-malformed-input`

**Problem.** hex_decode slices &s[i..i+2] on byte indices, so attacker-controlled key/sig text containing a multi-byte UTF-8 char at an odd boundary panics (slice not on a char boundary) instead of returning the intended graceful error.

**Dowód.** `u8::from_str_radix(&s[i..i + 2], 16).map_err(|e| e.to_string())`

**Poprawka.** Validate ASCII first (if !s.is_ascii() { return Err("non-ascii hex".into()); }) or iterate over s.as_bytes() chunks instead of string slicing.

### M-33 · `tools/eos-repo-sign/src/main.rs:177`
**Wymiar:** C · Bezpieczeństwo  ·  **Kategoria:** `non-strict-ed25519-verify`

**Problem.** Classical verification uses ed25519-dalek verify rather than verify_strict, so it accepts small-order/non-canonical edge cases; not exploitable to forge a different repo.toml here, but verify_strict is the recommended API for a security-critical verifier.

**Dowód.** `let ed_ok = ed_vk.verify(&msg, &ed_sig).is_ok();`

**Poprawka.** Use ed_vk.verify_strict(&msg, &ed_sig).is_ok() to reject non-canonical R / small-order A components.

## Odrzucone (false-positive po weryfikacji)

Transparentność: te findingi zgłoszono, ale adwersaryjna weryfikacja je obaliła (nie trafiają do zadań).

- **`scripts/make-release.sh`** — make-release.sh signs with `minisign -Sm ... -s KEY` but never verifies the produced signature, and if the key is passphrase-protected (which keys/README.md rec
  - *odrzucone:* Line 32 and the README quote both match the real files, but the finding's central impact is speculative and mistargeted. make-release.sh is explicitly documented (header lines 2-8, 10-12) as a LOCAL reproduction tool run by a human because 
- **`.github/workflows/codeql.yml`** — Seven fully-configured GitHub Actions workflows (build, cargo-audit, ci, codeql, gitleaks, pages, release) trigger on push/pull_request/schedule, yet the projec
  - *odrzucone:* Evidence materially mismatches the actual repo. (1) The claim "only gitleaks+integrity were ported, so ... cargo-audit ... enforced NOWHERE" is false: scripts/local-scan.sh (R-005, ROADMAP L213 marked DONE) runs `cargo audit` over Cargo.loc
- **`upstream/base/0003-pcid-gate-ACPI-_PRT-INTx-routing-to-non-x86-R-401f.patch`** — In the carried pcid ACPI-_PRT patch, resolve_link_gsi() locates the Extended Interrupt Descriptor by scanning the link device's _CRS buffer for the first byte e
  - *odrzucone:* The finding is filed against upstream/base/0003 line 187, but that patch is only 56 lines long (line 187 does not exist) and grep confirms it contains no 0x89 / resolve_link_gsi / position / pos+5 code. Patch 0003 exclusively adds cfg!(not(
- **`ROADMAP.md`** — The top-of-file `timeline` mermaid (line 15) and the section header both frame aarch64 as future work — 'v0.4.0 Reach : aarch64 bring-up : More hardware' and '#
  - *odrzucone:* All quoted lines match the real file (15, 116, 118, 120, 124) and it is E-OS's own ROADMAP.md, so the finding is neither hallucinated nor stale — but the alleged "problem" (⏳ header + timeline mislead readers into thinking aarch64 is unship
- **`tools/eos-repo-sign/src/main.rs`** — The hybrid-signature tool is a standalone build-host prototype nothing invokes: grep found no build/install/pkgar step that calls verify, so the docstring claim
  - *odrzucone:* The cited line 209 is `fn main() {`, not the quoted docstring text, which actually lives at lines 5-7. More importantly, the docstring does not overstate protection: line 5 explicitly reads "This tool prototypes the migration to a hybrid si

## Co jest dobre (notatki finderów)

- **sec-signing:** Verified positives (no findings, worth recording): (1) No private-key material anywhere in the repo — keys/ contains only the minisign public key (eos-release.pub) and README; grep for PEM/seed/secret_keys/id_ed25519 across toml/key/pub/md found nothing outside the source and docs. (2) tools/eos-repo-sign/src/main.rs crypto paths are fail-closed: every fallible step uses unwrap_or_else(die) (no ba
- **sec-ci-scripts:** Covered all E-OS-authored CI/shell tooling in the slice: .gitlab-ci.yml, .gitleaks.toml, scripts/{ci-integrity.sh, local-scan.sh, hooks/pre-push, gen-sbom.py, sync-forks.sh, eos-repos.sh, eos-setup-mirrors.sh}, plus the .github/workflows/ set (relevant to the dead-Actions focus).

SCOPE NOTE — 5 files listed in the slice are UPSTREAM Redox cookbook, NOT E-OS-authored (confirmed via git log): scrip
- **code-quality:** Covered all slice targets. tools/eos-repo-sign/src/main.rs is genuinely clean for a Rust CLI: NO raw unwrap()/expect()/unsafe/panic! in prod paths — every fallible call uses `.unwrap_or_else(|e| die(...))` with a `die() -> !` helper, and the hybrid ed25519+ML-DSA-65 verify logic is correct (pq_ok defaults true only under --classical-only, final gate is `ed_ok && pq_ok`). No TODO/FIXME in either sl
- **config-build:** Scope note: of the files listed for SLICE 4, only these are actually E-OS-authored — config/{x86_64,aarch64}/eos.toml, the 1-line greeter dependency edit in config/desktop-minimal.toml, recipes/other/eos/recipe.toml, patches/*.patch, and upstream/{base,kernel,relibc}/*.patch. Everything else (Makefile, build.sh, flake.nix, native_bootstrap.sh, podman_bootstrap.sh, Cargo.toml, mk/*, podman/*, and t
- **org-docs:** Covered the full organization/documentation slice: root standard files (LICENSE not read but referenced-checks pass; NOTICE, TRADEMARK.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md all read), README.md, CHANGELOG.md, ROADMAP.md, EOS_BUILD_STATE.md, HARDWARE.md, book.toml, docs/ (SUMMARY.md vs 27 loose .md, REDOX-* upstream copies), .gitignore, .github/ (CODEOWNERS, FUNDING.yml, dependabot.y
- **sec-deep:** Adversarial re-check of the two crown-jewel files. VERDICT: no break found on the high-risk axes. main.rs verify() is sound: full mode requires BOTH ed25519 && ML-DSA-65 to pass (line 202 `if ed_ok && pq_ok`), pq_ok cannot remain true while a real check runs, every hex/length/decode error routes to die() so there is no silent-OK-on-mismatch path, the file and .sig are read as identical raw bytes f

## Priorytetyzowana lista zadań naprawczych (repo główne)

Kolejność = waga → koszt → ryzyko. Nic nie ruszam bez Twojej akceptacji.

| # | Zadanie | Pliki | Waga |
|---|---------|-------|:----:|
| 1 | Add a full-clone directive so the only enforced secret gate scans all history, e.g | `.gitlab-ci.yml` | WYSOKIE |
| 2 | Pin the E-OS signing pubkey into the client image (roadmap R-702 'kill TOFU') and treat the copy served next t | `scripts/publish-repo-pages.sh` | WYSOKIE |
| 3 | Either add a top-of-file comment to each workflow marking it inert-until-R-004 (and drop the github-actions ec | `.github/workflows/ci.yml` | SREDNIE |
| 4 | Restore the full changelog: commit f3ef3ca4 truncated it from 838→15 lines, so recover the version at commit 3 | `CHANGELOG.md` | SREDNIE |
| 5 | Rename to docs/REDOX-HARDWARE.md to match the upstream-copy convention and list it in SUMMARY.md under 'Upstre | `HARDWARE.md` | SREDNIE |
| 6 | Rewrite R-303/R-301 to past tense with the disabled-Actions caveat (e.g | `ROADMAP.md` | SREDNIE |
| 7 | Add the same 'GitHub Actions disabled account-wide — job inert (ROADMAP R-004)' caveat the README uses to the  | `SECURITY.md` | SREDNIE |
| 8 | Factor the shared identity + theme [[files]] into a new config/eos-common.toml and `include` it from both conf | `config/aarch64/eos.toml` | SREDNIE |
| 9 | Drop the literal "user (no password) * root / password" line from the persistent /etc/issue [[files]] block in | `config/x86_64/eos.toml` | SREDNIE |
| 10 | Remove the literal credentials from /etc/issue; show only the E-OS version and a note to run eos-welcome | `config/x86_64/eos.toml` | SREDNIE |
| 11 | Add hardware-capabilities-roadmap.md (and any other user-facing ones) to SUMMARY.md under an appropriate headi | `docs/SUMMARY.md` | SREDNIE |
| 12 | Produce and commit SHA256SUMS.minisig via make-release.sh (MINISIGN_SECRET_KEY set) alongside SHA256SUMS, or c | `release/SHA256SUMS` | SREDNIE |
| 13 | Populate real versions in the repo/<target>/*.toml cooked metadata (or derive from source_identifier / recipe  | `sbom/eos-0.1.0-x86_64.cdx.json` | SREDNIE |
| 14 | Guard the type and add a fallback so one bad repo doesn't kill the batch: `d = json.load(sys.stdin); print('1' | `scripts/eos-setup-mirrors.sh` | SREDNIE |
| 15 | Separate the file-existence guard from the audit result and propagate failure, e.g.:
  if [ -f "$lock" ]; then | `scripts/local-scan.sh` | SREDNIE |
| 16 | Wire `tools/eos-repo-sign sign <secret> repo.toml` into publish-repo-pages.sh, copy the resulting repo.toml.si | `scripts/publish-repo-pages.sh` | SREDNIE |
| 17 | Keep the token out of argv: use a credential helper / GIT_ASKPASS, or pass it via header on stdin, e.g | `scripts/sync-forks.sh` | SREDNIE |
| 18 | Before writing, check `std::path::Path::new(secret_path).exists()` and refuse (die with a clear message) unles | `tools/eos-repo-sign/src/main.rs` | SREDNIE |
| 19 | Add a minimal .editorconfig (root=true; utf-8; lf; final-newline; trim trailing whitespace; 4-space Rust/Pytho | `.editorconfig` | NISKIE |
| 20 | Correct the comment to reflect the actual model, e.g | `.gitlab-ci.yml` | NISKIE |
| 21 | Drop the blanket `Cargo\.lock$` path allowlist (Cargo.lock rarely needs it and can carry tokens in git URLs),  | `.gitleaks.toml` | NISKIE |
| 22 | Vendor the decorative banner/divider as static images under assets/ (already used for screenshots) and keep on | `README.md` | NISKIE |
| 23 | Add ONE canonical rustfmt.toml, clippy.toml, and .editorconfig at the ecosystem root (or a shared config repo  | `rust-toolchain.toml` | NISKIE |
| 24 | Scope the grep to E-OS-authored paths to match the intent, e.g | `scripts/ci-integrity.sh` | NISKIE |
| 25 | Parse the optional `--from VALUE` flag before binding the positional dir (e.g | `scripts/eos-repos.sh` | NISKIE |
| 26 | Drop `sys` from the import: `import os, json, glob, argparse`. | `scripts/gen-sbom.py` | NISKIE |
| 27 | Guarantee bom-ref uniqueness (e.g | `scripts/gen-sbom.py` | NISKIE |
| 28 | Pin an exact version, track the crate toward a stabilized/audited release, and keep the ML-DSA path documented | `tools/eos-repo-sign/Cargo.toml` | NISKIE |
| 29 | Delete the `signature = "2"` line from tools/eos-repo-sign/Cargo.toml (the transitive versions ed25519-dalek/m | `tools/eos-repo-sign/Cargo.toml` | NISKIE |
| 30 | Use `ed_vk.verify_strict(&msg, &ed_sig).is_ok()` so signatures must be canonical, matching the misuse-resistan | `tools/eos-repo-sign/src/main.rs` | NISKIE |
| 31 | In verify(), read `version` from the sig map and die if it != SIG_VERSION, and assert `algorithms` matches the | `tools/eos-repo-sign/src/main.rs` | NISKIE |
| 32 | Validate ASCII first (if !s.is_ascii() { return Err("non-ascii hex".into()); }) or iterate over s.as_bytes() c | `tools/eos-repo-sign/src/main.rs` | NISKIE |
| 33 | Use ed_vk.verify_strict(&msg, &ed_sig).is_ok() to reject non-canonical R / small-order A components. | `tools/eos-repo-sign/src/main.rs` | NISKIE |
