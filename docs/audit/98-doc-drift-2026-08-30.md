# 98 — Dryf dokumentacji, 2026-08-30

Rejestr rozjazdów między tym, co dokumenty projektu **twierdzą**, a tym, co da się **zmierzyć
w drzewie**. Każdy wpis niesie ścieżkę i linię albo polecenie, które go odtwarza. Czego nie dało
się potwierdzić z tego drzewa — jest oznaczone `[NIEZWERYFIKOWANE]`, a nie zgadnięte.

Plik jest dopisywany, nie nadpisywany. Kolejne sekcje dokładają się na końcu.

---

## Sekcja 0 — kontrola adwersaryjna tego pliku, 2026-08-30 wieczorem

Ten raport został sprawdzony przeciwko drzewu **po** tym, jak go napisano: ponad czterdzieści
cytatów, każdy z osobna — ścieżka, linia, treść linii. Wynik stoi na górze, bo część znalezisk
dotyczy **tego pliku**, nie dokumentów, które opisuje.

**Nic nie jest scalone.** Dziewięć MR-ów pozostaje otwartych; `main` nie dostał ani jednej
z poprawek opisanych niżej. Wszędzie, gdzie ten dokument mówi „poprawione", chodzi o **gałąź**,
nie o produkt. Sprawdzone: `git branch --contains` dla `50d2c2c2e`, `57fe451ed` i `81cec7659`
zwraca wyłącznie `feat/m1-bootable-medium` i jej odpowiednik w `origin`.

**Dwie rzeczy zmieniły się między pomiarem a tą kontrolą — obie na skutek tego raportu:**

1. `57fe451ed` (2026-08-30 20:26, tylko `feat/m1-bootable-medium`) poprawił `README.md`
   w **dwóch z czterech** miejsc, które wskazała sekcja B. Skutek: `README.md` **nie jest już
   identyczny na obu gałęziach**, a numeracja linii w tabeli §2 obowiązuje odtąd dla `main`.
   `git rev-parse main:README.md` → `d89e0524e…`, `feat/m1-bootable-medium:README.md` →
   `8a51dae2f…`. Od linii 150 w dół gałąź robocza jest przesunięta o **+5**.
2. `81cec7659` (2026-08-30 20:33) cofnął trzy przedwczesne ✅ w `ROADMAP-v2.md` §12.4 — wprost
   na podstawie A.5. Skutek: A.5, A.6 i A.9 opisują stan **sprzed** tego commita.

Obie zmiany są naniesione niżej w miejscu, z zachowaniem stanu **przed** i **po** — bo
podmienienie zdania po cichu zatarłoby ślad, a to jest ta sama wada, którą ten raport tropi.

**Czternaście cytatów wskazywało złą linię.** Treść znaleziska najczęściej się broniła, adres
nie. Fałszywy cytat jest gorszy niż jego brak, więc lista jest tutaj, a nie tylko w poprawionych
zdaniach: `CLAUDE.md` `:400`→`:401`, `:494`→`:496` (3×), `:436`→`:437`, `:14-23`→`:11` i
`:13-23`, `:415`→`:414-415`, `:906`→`:906-908`; `ROADMAP-v2.md` `:838`→`:839`,
`:1006`/`:1007`→`:1020`/`:1021`, `:491`→`:493`; `ROADMAP.md` `:194`→`:193`, `:6`→`:6` i `:9`,
`:202`→`:202-203`; `docs/threat-model.md:79`→`:79-80`; `scripts/qemu-driver-check.sh:54`→`:53-54`;
`config/aarch64/eos.toml:108`→`:110`.

**Trzy liczby zestarzały się przez samą pracę audytową.** `git grep -il sbat` daje dziś **11**,
nie 10 — jedenastym trafieniem jest ten raport. `git ls-files '*.md'` daje **89**, nie 87, bo
doszły dwa pliki audytu. `git rev-list --count v0.2.0..feat/m1-bootable-medium` daje **86**,
nie 83: pomiar był na `94a2e57c7` (= 83), a od tego czasu doszły `57fe451ed`, `89d06dd93`
i `81cec7659`. Poprawione w miejscu. Dokument mierzący dryf sam jest źródłem dryfu — to nie jest
paradoks, tylko powód, żeby każdą liczbę zapisywać razem z commitem, na którym ją zmierzono.

**Co się obroniło bez zmian:** cała tabela `CHANGELOG.md` w §3 (wszystkie czternaście adresów,
łącznie z `### Fixed` w `:980` i `U-085` w `:981`), rozkład 17 zadań CI na 5 etapów w A.3,
wszystkie adresy w `.gitlab-ci.yml` z wiersza README `:197`, komplet pomiarów wersji z A.14,
liczby z tagów (`233`, `232` w treści tagu, `v0.2.0` = obiekt `tag`, `verify-tag` OK),
11 kontroli `ci-integrity.sh` i `pins ok=26 drift=0`.

---

## Sekcja A — ROADMAP i CLAUDE.md

**Gałąź:** `feat/m1-bootable-medium`, HEAD w chwili pomiaru `94a2e57c7`; przy kontroli
adwersaryjnej `81cec7659` (dwa commity dalej — `57fe451ed` i `81cec7659`, oba opisane w sekcji 0).
**Warunki pomiaru:** drzewo robocze czyste wobec `origin/feat/m1-bootable-medium`; wszystkie
polecenia uruchomione z korzenia repozytorium 2026-08-30. Poleceń `git` zmieniających stan nie
używano. Źródła forków **nie są rozwinięte** (`recipes/core/installer/` zawiera wyłącznie
`recipe.toml`), więc każde twierdzenie o kodzie instalatora jest oznaczone jako niezweryfikowane.

**Do której gałęzi odnoszą się adresy w tej sekcji — zmierzone, bo różnica jest duża:**

| plik | `main` vs `feat/m1-bootable-medium` | co z tego wynika |
|---|---|---|
| `CLAUDE.md` | identyczny | adresy A.1–A.4 obowiązują na obu |
| `ROADMAP.md` | 438 linii na obu, różnica to 3 poprawione odnośniki (`94a2e57c7`) | adresy A.10–A.13 obowiązują na obu |
| `config/`, `docs/install.md`, `repos.toml`, receptury | identyczne | adresy obowiązują na obu |
| `ROADMAP-v2.md` | **637 linii na `main`, 1276 na gałęzi** | **cały rozdział §12** (`:641-1276`) — epiki, kamienie milowe, tabela zadań M1 — **nie istnieje na `main`**. Adresy z A.5, A.6, A.9 powyżej `:637` są **wyłącznie** gałęziowe |
| `Makefile`, `mk/` | `50d2c2c2e` tylko na gałęzi | A.6 mierzy gałąź; na `main` stary stan (13 trafień `redox-live`) |

To jest sedno, nie przypis: **nic nie jest scalone**, więc dokument, który czyta ktoś klonujący
`main`, nie zawiera ani rozpiski M1, ani zmiany nazwy nośnika, ani dwóch poprawek README z sekcji B.

### A.0 Co przeszło — żeby czerwone niżej znaczyło co znaczy

`bash scripts/ci-integrity.sh` na tej gałęzi: **11 × `ok:`, `integrity: PASS`**, zero
`FAIL (instrument)`. `bash scripts/eos-repos.sh pins --strict` → `pins ok=26 drift=0
(non-allowlisted=0)`. Dokumenty nie kłamią o niczym, co te dwie bramki mierzą — poza samą **liczbą**
kontroli (A.1).

### A.1 `CLAUDE.md` mówi o **ośmiu** kontrolach; skrypt ma **jedenaście**

Policzone w skrypcie, nie z pamięci:

```
grep -nE '^# ?(──)? *[0-9]+[).]|^# ── [0-9]+\.' scripts/ci-integrity.sh
```

→ banery `0.`, `1)`…`6)`, `7.`, `8.`, `9.`, `10.`, `11.`. Kontrola `0` to sonda przyrządów
(`CLAUDE.md` §4.2), więc **niezmienników jest 11**. Uruchomienie potwierdza: jedenaście linii
`ok:`.

Dokument twierdzi inaczej w **dwóch** miejscach:

- `CLAUDE.md:39` — `bash scripts/ci-integrity.sh  # bramka niezmienników (8 kontroli)`
- `CLAUDE.md:497` — `` `integrity` (8 kontroli niezmienników) ``

Rozjazd o **trzy** pozycje. Znalezisko nie jest nowe: `ROADMAP-v2.md:1246` (§12.10 poz. 6) opisuje
je dokładnie i odsyła naprawę do `CLAUDE.md`. **Naprawa nie nastąpiła** — dziś, na tej gałęzi,
oba miejsca nadal mówią „8". To jest wada wobec `CLAUDE.md` §2 reguły 4, zdiagnozowana i
nienaprawiona.

Naprawiając, warto usunąć samą liczbę: skrypt jest jedynym źródłem prawdy o swojej długości, a
liczba wpisana ręcznie w dwóch miejscach rozjedzie się po raz drugi.

### A.2 `CLAUDE.md` §19 przeczy sam sobie o `R-601` — w tej samej tabeli

`CLAUDE.md:875` otwiera tabelę nagłówkiem **„Otwarte usterki (blokują `R-601`)"**. W tabeli
`R-601` występuje **dwa razy**, z przeciwnymi wynikami:

| linia | treść | stan |
|---|---|---|
| `CLAUDE.md:882` | partycja → instalacja → reboot → login | ✅ **UDOWODNIONE** (`U-176`), potwierdzone ponownie z poprawką `R-F18` (`U-181`) |
| `CLAUDE.md:886` | partycja → instalacja → reboot → login nadal **nieudowodnione**; blokuje `R-F19`. | 🚧 P0 |

Wiersz `:886` jest pozostałością i jest błędny w **dwóch** punktach naraz. Po pierwsze, przeczy
wierszowi `:882` oraz `ROADMAP.md:323` (`R-601` ✅, dowód `U-176`) i `ROADMAP-v2.md:839`
(wiersz **M1** w §12.3: *„`R-601` ✅ (dowód `U-176`)"*). Po drugie, odwraca zależność: to `R-F19` blokowało `R-601`, nie
odwrotnie — łańcuch `R-F19` → `R-F21` → `R-F22` → `R-F24` jest opisany w tej kolejności w
`ROADMAP.md:288`.

Ten sam wiersz psuje też nagłówek sekcji. Policzone w wierszach `CLAUDE.md:879-886`: **sześć**
pozycji ✅ — pięć „naprawione" (`R-F19`, `R-F21`, `R-F22`, `R-F24`, `R-F18`) i `R-601`
„UDOWODNIONE" — oraz **dwie** 🚧: `R-F23` (`:883`, P1, `hvf` / `synchronous_exception_at_el0`)
i pozostałościowy `R-601` (`:886`, P0). Dopiero po skreśleniu `:886` jedyną faktycznie otwartą
usterką zostaje `R-F23`. Tabela zatytułowana „otwarte usterki" niesie więc jedną otwartą pozycję
na osiem.

Powiązane, w tej samej tabeli: `CLAUDE.md:879` opisuje `R-F19` jako *„**nie** potwierdzone
end-to-end — ścieżka staje wcześniej na `R-F21`"*. `R-F21` jest w wierszu niżej oznaczony jako
naprawiony (`U-171`), a `R-601` w wierszu `:882` jako udowodniony end-to-end. Zastrzeżenie w
`:879` jest zdjęte przez `U-176` i nie zostało wykreślone.

### A.3 `CLAUDE.md` §0: trzy liczby w nagłówkowej tabeli nie zgadzają się z drzewem

Tabela `CLAUDE.md:13-23` stoi pod nagłówkiem `:11` **„Co to za repozytorium (przeskanowane, nie
założone)"**.
Przeskanowane ponownie dzisiaj:

| wiersz | dokument | pomiar |
|---|---|---|
| `:21` CI | *„`.gitlab-ci.yml`, **12 zadań** / 5 etapów"* | **17 zadań** / 5 etapów — tyle samo, ile podaje §13 (`CLAUDE.md:496`) w tym samym pliku |
| `:22` Dokumentacja | *„**38** plików `.md` w `docs/`"* | `find docs -name '*.md' -not -name '._*'` → **67** (samo `docs/` bez podkatalogów: 40) |
| `:23` Skrypty | *„**38** w `scripts/`"* | `find scripts -maxdepth 1 -type f -not -name '._*'` → **57** (w tym 50 × `.sh`, 6 × `.py`) |
| `:23` Skrypty | *„`sync-forks.sh` i `publish-repo-pages.sh` — **oba obecne lokalnie**"* | **prawda**: `scripts/sync-forks.sh` (2241 B), `scripts/publish-repo-pages.sh` (7667 B) |

Ostatni wiersz jest prawdziwy, ale wchodzi w **sprzeczność z §11**: `CLAUDE.md:414-415` (typ B) mówi
*„Synchronizacja wyłącznie przez `scripts/sync-forks.sh` (patrz §0 — **skryptu nie ma w tym repo**,
należy do repo orkiestrującego)"*. §11 powołuje się na §0 na poparcie tezy, którą §0 obala, a
drzewo przyznaje rację §0. Zdanie w §11 należy skreślić — to repozytorium **jest** repem
orkiestrującym (`repos.toml:15`, `type = "A"`), więc skrypt jest dokładnie tam, gdzie powinien.

Osobno, wykaz etapów CI: `CLAUDE.md:25` zapowiada **„Etapy CI i zadania (stan faktyczny)"**, po
czym blok `:27-33` podaje dwanaście zadań. Zmierzone w `.gitlab-ci.yml` (nagłówki zadań + ich
`stage:`) — jest ich **siedemnaście**:

| etap | zadania | w bloku §0 |
|---|---|---|
| `verify` | `secret-scan`, `integrity`, `pin-check`, `mirror-drift`, `rebase-check`, `docs-currency`, `renovate` | brak `mirror-drift`, `rebase-check` |
| `test` | `rust-checks`, `shell-lint`, `coverage`, `sbom` | brak `coverage`, `sbom` |
| `docs` | `rustdoc`, `pages`, `docs-pdf` | brak `rustdoc` |
| `release` | `semantic-release` | — |
| `build` | `build-image`, `build-image-x86_64` | — |

`CLAUDE.md:496` (§13) podaje ten sam stan **poprawnie**: „Stan faktyczny (17 zadań, 5 etapów)"
i wylicza wszystkie siedemnaście. Rozjazd jest więc wewnątrz jednego pliku — i to **dwukrotny**,
bo `:21` mówi „12", `:27-33` wylicza dwanaście, a `:496` mówi „17" i wylicza siedemnaście. Wersje opatrzone etykietami
„przeskanowane, nie założone" i „stan faktyczny" są tymi błędnymi.

`CLAUDE.md:40` („`pins --strict`, 26 repo") **jest poprawne** i zostało sprawdzone: `ok=26`.
Liczba 26 to 30 pozycji z `repos.toml` minus `E-OS`, minus dwa repozytoria pakietów (typ D) i
minus `eos-ui` (`repos.toml:281`, `pinned_in_recipe = false`).

### A.4 `CLAUDE.md` §11/§12 wobec `repos.toml` — **zgodne**, kontrola uruchomiona

Kontrola 7 z `ci-integrity.sh` porównuje typy z `CLAUDE.md` §11 z polem `type` w `repos.toml`.
Uruchomiona bezpośrednio:

```
$ python3 scripts/eos-check-repo-types.py
repo-types: OK — 30 repozytoriów, typy zgodne z CLAUDE.md §11
$ echo $?
0
```

W ramach bramki:

```
  ok: CLAUDE.md repo types match repos.toml
```

Przeliczone niezależnie z `repos.toml` (`grep -c '^\[\[repo\]\]'` → 30): typ A — 6, typ B — 10,
typ C — 12, typ D — 2. Listy w `CLAUDE.md:404-455` mają te same liczności i te same nazwy.
`CLAUDE.md:401` („**30 repozytoriów**") zgadza się z manifestem.

§12 (definicje ukończenia per typ, `CLAUDE.md:466-492`) nie zawiera list repozytoriów, więc nie ma
z czym się rozjechać. Jedyne, co §12 mówi sprawdzalnego o typie C, jest przedmiotem A.5.

Poza zakresem bramki, ale w tym samym miejscu: `recipes/core/installer/recipe.toml:2` opisuje
`eos-installer` jako *„E-OS vendored **mirror** of redox-os/installer (edit freely...)"*, podczas
gdy `repos.toml:109` deklaruje `type = "C"` (fork z łatkami), a `CLAUDE.md:424` wymienia go wśród
typu C. Komentarz w recepturze niesie nazewnictwo typu B dla repozytorium typu C — dokładnie ten
rodzaj pomyłki, przed którym ostrzega `CLAUDE.md:437` („pięć repozytoriów opisanych tu jako lustra
niosło realny kod"; sam wynik „żaden z 22 forków nie jest pusty" stoi wiersz wyżej, w `:436`). Bramka tego nie łapie, bo czyta `CLAUDE.md` i `repos.toml`, nie komentarze
w recepturach.

### A.5 `ROADMAP-v2.md` §12.4: `R-607a` i `R-612a` **były** oznaczone ✅, a pin instalatora nie drgnął — znalezisko przyjęte, znacznik cofnięty, pin dalej stoi

To jest najcięższe znalezisko tej sekcji, bo dotyczy pozycji oznaczonych jako zrobione.

**Stan w chwili pomiaru.** `ROADMAP-v2.md:882` (zadanie 7, `R-607a`) i `:883` (zadanie 8,
`R-612a`) nosiły ✅. Obie zmiany żyją w forku `eos-installer`, w MR-ach !2 i !3 —
**niescalonych**. Zmierzone w tym drzewie:

- `recipes/core/installer/recipe.toml:5` → `rev = "c8d32ad39e5c778fb8aec51c953f2898d9e55495"`
- `repos.toml:116` → `pinned_rev = "c8d32ad39e5c778fb8aec51c953f2898d9e55495"`
- `git log -1 --date=short -- recipes/core/installer/recipe.toml` →
  `8bd2c79d6 2026-08-27 feat(R-601): partition -> install -> reboot -> login is PROVEN (U-176)`
- `bash scripts/eos-repos.sh pins --strict` → `eos-installer | master | c8d32ad39e | c8d32ad39e | OK(tip)`

Czyli: pin wskazuje rewizję z `U-176` (2026-08-27), ostatnia zmiana pina jest z 27 sierpnia, a tip
gałęzi `master` forka **jest** tą samą rewizją. Praca z MR-ów !2 i !3 nie jest w tym, co E-OS
buduje. Potwierdza to niezależnie treść commita `984b614cc` z dzisiaj:
*„Blokowało to bezpośrednio `R-607a` i `R-612a` z M1: oba zmieniają
`recipes/core/installer/source`"*.

Wobec `CLAUDE.md` §12, typ C, punkt 4 — *„Push na **oba** hosty **przed podbiciem pina**"* — oraz
§1.6, zadania 7 i 8 nie spełniają definicji ukończenia dla repozytorium typu C: pin nie został
podbity, więc żaden obraz E-OS tej zmiany nie niesie. `[NIEZWERYFIKOWANE]`, czy kod w MR-ach jest
poprawny — źródła forka nie są rozwinięte w tym drzewie, a `disk_wrapper.rs` nie istnieje pod
żadną ścieżką (`find . -name disk_wrapper.rs` → pusto). Weryfikowalne jest wyłącznie to, że tego
kodu **nie ma w produkcie**.

Wewnętrznie tabela §12.4 sama to sygnalizowała, i to był drugi rozjazd: kolumna **zdolność** dla
zadania 8 mówiła **ZROBIONE** (z odsyłaczem do MR), a dla zadań 1 i 7 **DO ZBUDOWANIA** — mimo ✅
przy identyfikatorze. Trzy pozycje oznaczone tym samym znacznikiem niosły trzy różne opisy stanu.

**Stan po ponownym pomiarze — tego samego dnia, później wieczorem.** Znalezisko zostało przyjęte
i naprawione commitem `81cec7659` *„docs: cofnij trzy przedwczesne ✅ w roadmapie — kod jest
w niescalonych MR-ach"*, którego komunikat powołuje się wprost na tę podsekcję. Zmierzone dzisiaj
w drzewie, na `feat/m1-bootable-medium`:

- `ROADMAP-v2.md:876` (zadanie 1, `R-611a`) → 🚧
- `ROADMAP-v2.md:882` (zadanie 7, `R-607a`) → 🚧
- `ROADMAP-v2.md:883` (zadanie 8, `R-612a`) → 🚧
- `ROADMAP-v2.md:888-900` — nowy blok *„Dlaczego zadania 1, 7 i 8 mają 🚧, a nie ✅"*, który
  wypisuje warunki dojścia do ✅: scalenie MR-ów w forku, podbicie `rev` w recepturze
  i `repos.toml`, scalenie MR-a w repozytorium głównym.

**Pin nadal nie drgnął.** `recipes/core/installer/recipe.toml:5` i `repos.toml:116` wciąż niosą
`c8d32ad39e5c778fb8aec51c953f2898d9e55495`, a `pins --strict` wciąż melduje
`eos-installer | master | c8d32ad39e | c8d32ad39e | OK(tip)`. Naprawiony jest **znacznik
w dokumencie**, nie stan produktu — i sam commit naprawiający leży na gałęzi niescalonej, więc
na `main` §12.4 nadal niesie trzy ✅. Zostaje też niespójność mniejszego kalibru: kolumna
**zdolność** zadania 8 mówi **ZROBIONE** przy 🚧 przy identyfikatorze.

### A.6 `ROADMAP-v2.md` §12.4 zadanie 1 (`R-611a`) — kod istnieje na niescalonej gałęzi, a kolumna „dowód" opisuje stan sprzed poprawki

Kod istnieje i jest zmierzalny — na `feat/m1-bootable-medium`, gałęzi **niescalonej**; na `main`
tej zmiany nie ma. Znacznik w tabeli to po `81cec7659` 🚧, nie ✅ (patrz A.5). Zmierzone na
gałęzi roboczej:

- `mk/config.mk:194` → `INSTALLER_MEDIUM_NAME=eos-$(EOS_VERSION)-$(ARCH)-installer.img`,
  `:195` → `INSTALLER_MEDIUM=$(BUILD)/$(INSTALLER_MEDIUM_NAME)`
- `mk/disk.mk:20` → reguła na `$(INSTALLER_MEDIUM)`
- `Makefile:14-16` → cel `print-installer-medium`; `:18-25` → `live`, `popsicle` przez zmienną
- `grep -c redox-live Makefile mk/*.mk` → **1**, i to komentarz `mk/config.mk:187`
  („The installation medium. Named, not `redox-live.iso`.")

Dryf jest w kolumnie „dowód" tego samego wiersza (`ROADMAP-v2.md:876`), która nadal mówi w czasie
teraźniejszym: *„**Dziś** `mk/disk.mk:20` i `Makefile:13-17` (cele `live`, `popsicle`) znają
wyłącznie `redox-live.iso`; łącznie `grep -c redox-live Makefile mk/*.mk` → **13 trafień
w 4 plikach**"*. Ten pomiar był prawdziwy przed `50d2c2c2e` — i **nadal jest prawdziwy na `main`**: zmierzone
z `git archive main Makefile mk`, `grep -c redox-live` → `Makefile` 6, `mk/ci.mk` 3,
`mk/disk.mk` 3, `mk/qemu.mk` 1, razem **13 w 4 plikach**, dokładnie jak w wierszu. Na
`feat/m1-bootable-medium` to samo polecenie zwraca **1**. Wiersz niesie więc pomiar aktualny
dla jednej gałęzi i nieaktualny dla drugiej — i nie mówi, o którą chodzi. To jest ta wada, nie
„liczba jest zła".

Dwie pozostałości po tej zmianie nazwy, obie poza `Makefile`/`mk/`:

- `scripts/qemu-driver-check.sh:53-54` — ścieżka **funkcjonalna** (`:53`) pyta
  `make -s print-installer-medium ARCH=aarch64 CONFIG_NAME=eos PODMAN_BUILD=0`, czyli jest
  poprawna; nieaktualny jest tekst błędu w `:54`, który zobaczy człowiek:
  `run: make ARCH=aarch64 CONFIG_NAME=eos live  # build/aarch64/eos/redox-live.iso`.
  Odsyła do pliku, którego build na tej gałęzi już nie produkuje.
- `mk/ci.mk:25` — nośnik zbudowany pod nową nazwą jest eksportowany jako
  `$(IMG_DIR)/redox_<config>_<tag>_livedisk.iso`. Cele to `server desktop demo`, nie `eos`, więc
  ścieżka E-OS jest nietknięta; odnotowane, bo commit `50d2c2c2e` deklaruje *„Wszystkie
  przestawione"*.

### A.7 `ROADMAP-v2.md` §2.2 wymienia jako braki **cztery rzeczy zrobione**, które §2.6 zamyka ✅

Sekcja 2.2 („Czego brakuje technicznie 🔴") nie została zaktualizowana po wykonaniu zadań `V2-MS`.
Cztery wiersze przeczą tabeli §2.6 w tym samym pliku:

| §2.2 twierdzi | §2.6 | zmierzone dzisiaj |
|---|---|---|
| `.sbat` — *„w całym drzewie **zero** wystąpień SBAT"* | `V2-MS01` ✅ (`U-218`) | `git grep -il sbat` → **11 śledzonych plików** (10 w chwili pomiaru; jedenastym trafieniem jest ten raport), w tym `recipes/core/bootloader/sbat.csv` i `scripts/eos-add-sbat.py`; `recipes/core/bootloader/recipe.toml:63` stempluje SBAT do obu bootloaderów UEFI **przed** podpisem |
| *„Bootloader nie weryfikuje tego, co ładuje — `load_to_memory()` sprawdza tylko bajty magiczne"* | `V2-MS02` ✅ (`U-212`) | `recipes/core/bootloader/recipe.toml:14,28,32` wstrzykuje klucz weryfikacji rozruchu i wypisuje *„bootloader WILL verify kernel and initfs"*; `scripts/eos-boot-verify-proof.sh` istnieje |
| *„Trzy dokumenty kłamią: `docs/threat-model.md:79`, `docs/hardening.md:168`, `docs/plan-do-sprzetu.md:39-41` wciąż twierdzą, że nic nie podpisuje bootloadera"* | `V2-MS03` ✅ (`U-211`) | wszystkie trzy poprawione: `threat-model.md:79-80` mówi *„Secure Boot signing of the bootloader exists (ADR-0005)"*; `hardening.md:168` *„UEFI Secure Boot: **signed**"*; `plan-do-sprzetu.md:40` wprost *„**Nieaktualne jest twierdzenie, że nikt nie podpisuje bootloadera**"* |
| *„`recipe.toml:54` robi **niehermetyczny** `apt-get install sbsigntool`"* | `V2-MS05` ✅ (`U-218`) | jedyne wystąpienie `apt-get` w tej recepturze to `:94`, w komentarzu wyjaśniającym, że narzędzie **nie** pochodzi z `apt-get`; `:98` twardo wymaga `command -v sbsign` z obrazu bazowego |

Dwa wiersze §2.2 **nadal obowiązują** i zostały sprawdzone: `grep -c 'eos-secureboot-proof'
.gitlab-ci.yml` → **0** (brak bramki SB w CI, `V2-MS04` 🔴), a reprodukowalność bajtowa pozostaje
nieudowodniona (`CLAUDE.md:906-908`: „Odtwarzalność builda […] samego porównania **nikt jeszcze
nie wykonał**"; sam cytat stoi w `:908`).

Konsekwencja jest poważniejsza niż estetyka: §2.2 jest tabelą, którą projekt zamierza pokazać
recenzentowi `shim-review`. W obecnej formie zaniża własny stan o cztery pozycje.

### A.8 `ROADMAP-v2.md` §2.1: dwa dowody nie trafiają w to, na co wskazują

Wiersz „Źródło bootloadera publiczne, przypięte" (`ROADMAP-v2.md:90`) cytuje
`recipes/core/bootloader/recipe.toml:4-6` → *rev `b249982f`*. Odczytane pod tym adresem:
`git = ".../eos-bootloader.git"`, `branch = "eos-rebased"`, `rev = "87b214b5b481be7f7049fbb07cf927961b00da5b"`.
`pins --strict` potwierdza `eos-bootloader | eos-rebased | 87b214b5b4 | OK(tip)`. Cytowany rev
`b249982f` nie występuje ani w recepturze, ani w `repos.toml`.

Wiersz „Cały łańcuch rozruchu open source" (`ROADMAP-v2.md:92`) mówi *„wszystkie **28** forków
publiczne"*. Z `repos.toml`: 30 pozycji, z czego forków (typ B + typ C) jest **22** — i tę samą
liczbę podaje `CLAUDE.md:436` („żaden z **22** forków nie jest pusty"). Liczba 28 nie odpowiada
też żadnemu innemu przekrojowi: pozycji `eos-*` jest 29, pozycji poza `E-OS` — 27.

Pozostałe dowody z §2.1, sprawdzone i **trafne**: `cookbook.lock:7-8` to faktycznie
`[recipes.bootloader]` / `fsrule = "source"`; `keys/eos-repo-sign.pub.toml` istnieje;
`scripts/eos-secureboot-proof.sh` istnieje.

### A.9 Rozjazd §12.4 ↔ §12.6/§9.4 — **zamknięty przez cofnięcie ✅**; dwa z trzech adresów były błędne

Pierwotna teza tej podsekcji: commit `f8a5f7e16` oznaczył zadania 1, 7 i 8 w §12.4 jako ✅, ale
zmienił **tylko** tę tabelę (`git show --stat f8a5f7e16` → `ROADMAP-v2.md | 22 +++---`, jeden
plik, 19 wstawień / 3 usunięcia), więc trzy inne miejsca tego samego dokumentu miały mówić co
innego. Ponowny pomiar — po `81cec7659`, patrz A.5 — obala dwie trzecie tej tezy, a przy okazji
prostuje adresy. Obie wersje są tu wypisane, bo podmiana zdania po cichu zatarłaby ślad.

**Co przestało obowiązywać, i miało zły adres.** Wiersze `R-611` i `R-612` w §12.6 stoją pod
`ROADMAP-v2.md:1020` i `:1021`, **nie** pod `:1006`/`:1007` — tam jest akapit o `R-701`, `R-303`
i `V2-MS12`, bez żadnego z tych identyfikatorów. Oba wiersze niosą **DO ZBUDOWANIA** / **🔴**.
Po cofnięciu ✅ na 🚧 w §12.4 to jest stan **zgodny** z resztą dokumentu, nie sprzeczny —
sprzeczność zniknęła razem z przyczyną.

**Co zostaje, też pod innym adresem.** Wiersz `R-607` w §9.4 stoi w `ROADMAP-v2.md:493`, nie
`:491` (tam jest `R-605`). Uzasadnia 🔴 zdaniem *„realny rozmiar bloku (4Kn) + macierz instalacji
na prawdziwym firmware — `DiskWrapper::open` **zawsze** raportuje 512"*. Zdanie opisuje kod forka,
a pin wskazuje rewizję sprzed poprawki, więc **wobec produktu jest prawdziwe**. Nietrafna była
pierwotna teza, że §12.4 uznaje je za obalone: kolumna „dowód" zadania 7 (`:882`) sama pisze
*„`disk_wrapper.rs:28` ma **dziś** `let block_size = 512;` na sztywno"*. Oba miejsca mówią to samo.

Otwarta zostaje jedna rzecz i nie ma właściciela: kto przepisze `:493` **po** scaleniu MR !2
i podbiciu pina. Żadne zadanie M1 tego nie obejmuje. `[NIEZWERYFIKOWANE]`, czy MR !2 faktycznie
usuwa ten literał — źródeł forka w tym drzewie nie ma (A.16 poz. 1).

### A.10 Pozycje ✅ z `ROADMAP.md` — wyrywkowa weryfikacja w drzewie

Sprawdzone czternaście pozycji oznaczonych ✅. Liczby nagłówkowe zgadzają się z zawartością —
stoją w dwóch liniach, nie w jednej: `ROADMAP.md:6` („**75 nieukończonych pozycji** (16 🚧 ·
43 ⏳ · 16 💡)") i `:9` („**67 pozycji zrobionych** zostaje tutaj jako zapis historyczny").
Przeliczone przez `grep -c` po znaczniku na początku pozycji: 67 ✅ · 16 🚧 · 43 ⏳ · 16 💡,
razem 142; 16+43+16 = 75.

| poz. | twierdzenie | wynik pomiaru |
|---|---|---|
| `R-202` | `config/x86_64/eos.toml` | ✅ istnieje, wraz z `config/aarch64/eos.toml` |
| `R-203` | `patches/bootloader-eos-red-black.patch`, baner `"E-OS Bootloader"` | ✅ plik istnieje, baner obecny |
| `R-204` | nadpisanie `/etc/os-release`, `/etc/issue` | ✅ `config/aarch64/eos.toml:91` (`path = "/usr/lib/os-release"`), `:110` (`path = "/etc/issue"`; `:108` to komentarz nad blokiem) |
| `R-206` | `recipes/other/eos` → `/usr/bin/eos-welcome`, `/usr/share/eos/eos-release` | ✅ `recipe.toml:13,41,112` |
| `R-209` | `recipes/other/eos` → `/usr/bin/eos` | ✅ `recipe.toml:44,109` |
| `R-301` | `keys/eos-release.pub`, podpis minisign z `make-release.sh` | ✅ klucz obecny, skrypt obecny |
| `R-302` | SBOM CycloneDX 1.5, 59 komponentów | ✅ format i liczba zgadzają się (`specVersion 1.5`, `components 59`, oba pliki) — **ale patrz A.11** |
| `R-304` | `docs/threat-model.md` + `docs/hardening.md` | ✅ oba istnieją |
| `R-305` | `docs/encryption.md` | ✅ istnieje |
| `R-F02` | brak źródeł forka wendorowanych do tego repo | ✅ kontrola 11 `ci-integrity.sh` → `ok` |
| `R-F14` | brak składni bash 4 w `scripts/` | ✅ kontrola 5 → `ok` |
| `R-F16` | `scripts/repro-intx-lines.sh` | ✅ istnieje |
| `R-F20` | przepisy z forkiem budowane ze źródeł | ✅ kontrola 6 → `ok`, `cookbook.lock` śledzony |
| `R-701a` | oba `config/*/eos.toml` nadpisują `/etc/pkg.d/50_redox`, adres zakomentowany | ✅ kontrola 9 → `ok`; `config/aarch64/eos.toml:744`, `config/x86_64/eos.toml:775` |
| `R-904a` | brak `ip` w namespace użytkownika, `icmp` zostaje | ✅ `grep -c '"ip",'` → 0 w obu; `grep -c '"icmp",'` → 1 w obu; `login_schemes.toml` nadpisany (`aarch64:770`, `x86_64:801`) |

Dwie pozycje ✅ **nie przechodzą** i są opisane osobno: `R-1001` (A.11) i `R-1004` (A.12).
Dwie pozycje z `ROADMAP-v2.md` §12.4, które w chwili pomiaru nosiły ✅, nie przechodziły:
`R-607a` i `R-612a`. Po `81cec7659` noszą 🚧 — znacznik zgadza się dziś ze stanem, pin nadal nie
(A.5). Sekcja §12.4 istnieje wyłącznie na `feat/m1-bootable-medium`.

### A.11 `ROADMAP.md` `R-1001` ✅ podaje polecenie, które od dzisiaj **pada**

`ROADMAP.md:182-188` opisuje nośnik jako `redox-live.iso` i podaje wprost:

> `make CONFIG_NAME=eos build/<arch>/eos/redox-live.iso`

Po `50d2c2c2e` w drzewie nie ma reguły produkującej ten plik — jedyne trafienie `redox-live`
w `Makefile` i `mk/` to komentarz `mk/config.mk:187`. Pozycja oznaczona ✅ instruuje więc
poleceniem, które kończy się błędem *„No rule to make target"*. `ROADMAP.md` jest wprawdzie
archiwum (`:1-10`), ale archiwum zachowane jako **zapis historyczny** wciąż jest czytane jako
instrukcja, a to jest jedyne miejsce w obu roadmapach, gdzie polecenie budowy nośnika stoi obok ✅.

Ta sama zmiana nazwy nie dotarła do dokumentacji użytkowej i to jest wada wobec `CLAUDE.md` §2:

- `docs/install.md:38` — `make CONFIG_NAME=eos ARCH=x86_64 build/x86_64/eos/redox-live.iso`
- `docs/install.md:100-101` — `make ... live  # -> build/x86_64/eos/redox-live.iso` oraz
  `dd if=build/x86_64/eos/redox-live.iso`
- `docs/plan-do-sprzetu.md:37` — *„Do czasu poprawki: zwykłe `dd` obrazu `redox-live.iso`"*
- `docs/ci.md` — zawiera odwołania do `redox-live`

`ROADMAP-v2.md:885` (zadanie 10) obejmuje **wyłącznie** `docs/install.md`
(`grep -c 'redox-live.iso' docs/install.md` → dziś **3**, cel 0). `docs/plan-do-sprzetu.md`
i `docs/ci.md` nie mają właściciela w żadnym zadaniu M1 — to luka w rozpisce, nie tylko
w dokumentach.

Przy okazji `R-302`: pozycja mówi *„SBOM generated **per build**"*, a `sbom/` zawiera wyłącznie
`eos-0.1.0-aarch64.cdx.json` i `eos-0.1.0-x86_64.cdx.json` — dwie wersje produktu wstecz wobec tagu
`v0.2.0`. `ROADMAP-v2.md:185` (`V2-MS08` 🔴) mierzy to samo i mówi prawdę: *„dziś statyczny dla
0.1.0 i cicho się starzeje"*. `CLAUDE.md:610` i `:909` potwierdzają („SBOM-y obrazów w `sbom/` nadal powstają
ręcznie"). Twierdzenie „per build" w `ROADMAP.md:94` jest jedynym z trzech, które się nie zgadza.

### A.12 `ROADMAP.md` `R-1004` ✅ opisuje mechanizm, który nie działa

`ROADMAP.md:202-203` — *„mdBook built + deployed by the GitLab CI `pages` job, **live at**
<https://e-os.gitlab.io/e-os/>"* (cytat łamie się przez dwie linie; `:202` niesie pierwszą połowę).

Zadanie `pages` istnieje (`.gitlab-ci.yml:252`, etap `docs`), ale GitLab CI na tym projekcie
zwraca `ci_quota_exceeded` na wszystkich zadaniach (ostatni pipeline `2803590199`, czas 0 s,
sprawdzone dzisiaj — za briefem, nie przeze mnie). Zdanie „deployed by the GitLab CI `pages` job"
opisuje więc mechanizm, który dziś nie może się wykonać. Czy sama strona nadal odpowiada z
poprzedniego wdrożenia — `[NIEZWERYFIKOWANE]`, sieci nie sprawdzałem.

To jest ta sama klasa wady, którą `ROADMAP.md:244` (`R-003`) uznaje za zamkniętą: dokument
obiecujący artefakt, którego martwe CI nie dowozi. Wtedy chodziło o GitHub Actions, teraz o
GitLaba.

### A.13 Pozycje 🚧 / ⏳ — które opisy przestały obowiązywać

Sprawdzone wyrywkowo; poniżej wyłącznie te, gdzie **treść** się rozjechała, niezależnie od tego,
czy sam znacznik jest jeszcze zasadny.

**`R-1003` 🚧 (`ROADMAP.md:193`, lista „Remaining" w `:199-201`)** — wymienia trzy rzeczy, z czego dwie są
zrobione:

- *„wire `/etc/pkg.d/50_eos` into the images"* — zrobione na aarch64: `config/aarch64/eos.toml:737`
  z **aktywnym** adresem `https://gh0s777tt.github.io/eos-pkg-aarch64/pkg`. Na x86_64 wpis istnieje
  (`config/x86_64/eos.toml:767`), ale adres jest zakomentowany — i to zgadza się z
  `ROADMAP-v2.md:947` i `:993` (`C-4`, „kanał na x86_64 — dziś brak aktywnego").
- *„an E-OS-owned signing key"* — klucz przypięty w obu obrazach:
  `config/aarch64/eos.toml:818` i `config/x86_64/eos.toml:849` →
  `/etc/pkg/eos-repo-sign.pub.toml`, plik `keys/eos-repo-sign.pub.toml` obecny.

Aktualne jest wyłącznie *„first publish from a build rig"* i *„the app ecosystem"*.

**`R-608` ⏳ (`ROADMAP.md:330`)** — całe uzasadnienie jest nieaktualne. Pozycja mówi, że
*„`docs/install.md` §2 claims the Installer walks through **creating users/passwords** and
**choosing the package set**"* i że *„§3 repeats it in the TUI comment
(`# prompts for disk, users, encryption, packages`)"*. Zmierzone:

- `docs/install.md:58-64` niesie jawne ostrzeżenie: *„**It does not create accounts, and it does
  not let you pick packages.**"* z odesłaniem do `R-603`.
- `docs/install.md:71` → `redox_installer_tui  # prompts for disk + RedoxFS password`, a `:74-75`
  powtarza ograniczenie.

Pozycja `R-608` **pozostaje otwarta**, ale z zupełnie innego powodu, i ten powód jest już zapisany
w `ROADMAP-v2.md:885` (nazwa artefaktu po `R-611a`). Uzasadnienie w `ROADMAP.md` opisuje dokument
sprzed poprawki.

**`R-303` 🚧 (`ROADMAP.md:97`)** — opis jest optymistyczniejszy niż stan. Mówi *„now on **GitLab
CI**"* i wylicza jako pozostałe: token wydania, odtworzenie kontenera `eosbuild`, powtarzalność
bajtowa. Nie wymienia tego, że **cały** GitLab CI jest dziś martwy z powodu wyczerpanego limitu,
a więc `semantic-release` i `build-image` nie mogą się uruchomić niezależnie od tamtych trzech
rzeczy. Znacznik 🚧 zostaje; lista blokad jest niepełna.

**`R-201` 🚧, `R-207` 🚧, `R-602` 🚧, `R-701` 🚧, `R-F23` 🚧** — sprawdzone, opisy nadal
obowiązują. W szczególności `R-F23` jest jedyną faktycznie otwartą usterką w tabeli
`CLAUDE.md` §19 (patrz A.2).

### A.14 `G-17` (niespójne wersje) nie ma pozycji w żadnym z trzech dokumentów

`grep -n 'G-17' ROADMAP.md ROADMAP-v2.md CLAUDE.md` → **zero trafień**. Zmierzony stan:

| miejsce | wersja |
|---|---|
| `config/base.toml:104-105` | `0.9.0` (dziedziczone z upstreamu Redox) |
| `config/aarch64/eos.toml:96-97` | `0.1.0 (Genesis)` |
| `config/x86_64/eos.toml:98-99` | `0.1.0 (Genesis)` |
| `recipes/other/eos/recipe.toml:113` | `E-OS 0.1.0 (Genesis)` |
| `mk/config.mk:185` | `EOS_VERSION?=0.2.0` |
| ostatni tag | `v0.2.0` |

Commit `50d2c2c2e` sam to odnotowuje w treści (*„to jest G-17 i NIE jest tu naprawiane; ta zmienna
rządzi wyłącznie NAZWĄ PLIKU"*), ale zapis żyje wyłącznie w komunikacie commita — nie ma go
w rejestrze zadań, więc nikt go nie znajdzie, czytając roadmapę.

`R-611a` podniósł stawkę tego znaleziska, zamiast ją zostawić bez zmian: nośnik nazywa się teraz
`eos-0.2.0-<arch>-installer.img`, a system, który z niego wystartuje, przedstawia się jako
`E-OS 0.1.0 (Genesis)` — w `/etc/os-release`, w `/etc/issue` i w `eos info`. Nazwa pliku i treść
pliku podają dwie różne wersje tego samego produktu. To jest coś, co pierwszy przebieg na metalu
(zadanie 11 M1) zobaczy na ekranie.

### A.15 `README.md`: znacznik `SYNC` przechodzi bramkę, ale jest o ~70 wpisów wstecz

Kontrola 3 z `ci-integrity.sh` sprawdza wyłącznie **obecność** ciągu `SYNC:`. `README.md:5`:

```
<!-- SYNC: v0.2.0 · Unreleased U-152 · 2026-08-22 — keep this file in step with CHANGELOG.md/ROADMAP.md on every feature. -->
```

Najwyższy identyfikator w `CHANGELOG.md` to `U-224`; `ROADMAP-v2.md` cytuje `U-222`…`U-224`.
Znacznik, którego cała funkcja polega na sygnalizowaniu aktualności, sam jest nieaktualny o 72
wpisy — i bramka tego nie widzi, bo mierzy obecność, nie zgodność. To jest `CLAUDE.md` §13 zasada
*„bramka, która nie może zapalić się na czerwono, nie istnieje"* zastosowana do kontroli 3:
w obecnej postaci może paść wyłącznie wtedy, gdy ktoś skasuje linię.

### A.16 Czego w tej sekcji nie zweryfikowałem

Zgodnie z `CLAUDE.md` §2 regułą 3.

1. **Nie czytałem kodu żadnego forka.** `recipes/core/installer/` i `recipes/core/bootloader/`
   zawierają w tym drzewie odpowiednio `recipe.toml` oraz `recipe.toml` + `sbat.csv`.
   Twierdzenia o `disk_wrapper.rs`, `installer.rs`, `with_whole_disk` i `load_to_memory()` są
   cytowane za dokumentami. Weryfikowalne jest wyłącznie to, na jaką rewizję wskazuje pin.
2. **Nie sprawdzałem stanu MR-ów ani pipeline'ów.** Stan GitLaba i GitHuba przyjmuję za briefem.
   Wniosek z A.5 nie stoi na tym: stoi na tym, że pin w `recipe.toml` i `repos.toml` jest
   z 2026-08-27 i nie zmienił się dzisiaj.
3. **Nie uruchomiłem żadnej kontroli negatywnej dla bramek.** `ci-integrity.sh` przeszedł, ale nie
   widziałem, jak pada — więc wobec `CLAUDE.md` §4.1 mój przebieg jest pomiarem, nie dowodem, że
   bramka działa.
4. **Nie budowałem obrazu ani nośnika.** `make ... live` nie było uruchamiane; twierdzenie
   z A.6, że kod `R-611a` istnieje i działa, opiera się na odczycie reguł `make`, nie na
   artefakcie. Nośnika o nowej nazwie **nikt nie widział** — `build/` w tym drzewie zawiera
   `container.tag`, `hostbuild-eos-control` i `id_ed25519.pub.toml`, nie zawiera `build/x86_64/`
   ani `build/fstools/`. `[NIEZWERYFIKOWANE]`, czy `make live` faktycznie produkuje plik o nazwie
   z `INSTALLER_MEDIUM_NAME`.
5. **Nie sprawdzałem sieci.** Liveness `https://e-os.gitlab.io/e-os/` (A.12) i dostępność
   `https://gh0s777tt.github.io/eos-pkg-aarch64/pkg` (A.13) są `[NIEZWERYFIKOWANE]`.
6. **Nie przeczytałem raportów audytu z `fix/p0-audit-findings`.** Odsyłacze do `C-4` i `C-19`
   przepisuję za briefem i za `ROADMAP-v2.md`.
7. **Nie sprawdziłem wersji specyfikacji, którą wypuszcza zadanie `sbom`.** `CLAUDE.md:607-610`
   (§17) mówi „CycloneDX **1.3**", a `.gitlab-ci.yml:226-227` woła `cargo cyclonedx --format json`
   bez flagi wersji specyfikacji. Pliki w `sbom/` (1.5) pochodzą z innego generatora
   (`scripts/gen-sbom.py`), więc nie rozstrzygają. `[NIEZWERYFIKOWANE]` — wymaga uruchomienia
   `cargo-cyclonedx`.


---

## Sekcja B — README i CHANGELOG

**Data pomiaru:** 2026-08-30
**Zakres:** `README.md` i `CHANGELOG.md` na gałęziach `main` oraz `feat/m1-bootable-medium`
**Metoda:** `git show <gałąź>:<plik>`, `git ls-tree`, `git cat-file`, `git rev-list`, `git verify-tag`
na repozytorium `/Volumes/Project itp/Projekty/E-OS Project/E-OS`. Żadnego polecenia
zmieniającego stan nie uruchomiono.
**Zastrzeżenie:** ten dokument **raportuje**. W chwili pomiaru nie poprawiono ani jednej linii
`README.md` ani `CHANGELOG.md`.
**Dopisane przy kontroli adwersaryjnej (sekcja 0):** poprawki poszły później tego samego wieczora
commitem `57fe451ed`, **tylko na `feat/m1-bootable-medium`** i **tylko w dwóch z czterech**
wskazanych miejsc. `CHANGELOG.md` nietknięty. Poniżej każdy wiersz, którego to dotyczy, niesie
stan przed i po; reszta obowiązuje bez zmian.

---

## 0. Ustalenie, które rządziło całą resztą — i które przestało obowiązywać dla `README.md`

**Stan w chwili pomiaru: oba pliki bajt w bajt identyczne na obu gałęziach.**

```
git rev-parse main:README.md      feat/m1-bootable-medium:README.md
  → d89e0524e91ca26df59b544b6d82bfdfb71366d3   (obie)
git rev-parse main:CHANGELOG.md   feat/m1-bootable-medium:CHANGELOG.md
  → 9224d9a428e9b899f1ab45eb4a4b242b9e2ff7a0   (obie)
git diff main feat/m1-bootable-medium -- README.md CHANGELOG.md
  → pusty
```

**Stan po ponownym pomiarze (kontrola adwersaryjna, ten sam dzień):**

```
git rev-parse main:README.md                    → d89e0524e91ca26df59b544b6d82bfdfb71366d3
git rev-parse feat/m1-bootable-medium:README.md → 8a51dae2faafb36baf615488ba327a26bcff9418
git diff --stat main feat/m1-bootable-medium -- README.md
  → README.md | 15 ++++++++++-----   (10 wstawień, 5 usunięć)
git rev-parse main:CHANGELOG.md  feat/m1-bootable-medium:CHANGELOG.md
  → 9224d9a428e9b899f1ab45eb4a4b242b9e2ff7a0   (obie — bez zmian)
```

Zmiana pochodzi z `57fe451ed` (2026-08-30 20:26, wyłącznie `feat/m1-bootable-medium`), którego
komunikat powołuje się na ten raport. Dotknięte są dwa bloki: `:129-137` i `:143-150`.

**Co z tego wynika dla reszty sekcji B, i trzeba to czytać przed tabelami:**

1. `CHANGELOG.md` **nadal jest identyczny** na obu gałęziach — tabela §3 obowiązuje bez zastrzeżeń.
2. `README.md` **nie jest**. Numeracja w tabeli §2 to numeracja **`main`**. Na
   `feat/m1-bootable-medium` wszystko od linii 150 w dół jest przesunięte o **+5**
   (`:294`→`:299`, `:310-315`→`:315-320`, `:328-329`→`:333-334`, `:368-369`→`:373-374`),
   a `main` ma 407 linii wobec 412 na gałęzi roboczej.
3. **Nic nie jest scalone**, więc `main` — repozytorium, które czyta przychodzący człowiek —
   niesie **wszystkie** wady z tabeli §2, łącznie z dwiema poprawionymi na gałęzi.

### 0.1 Daty, które tłumaczą skalę dryfu

| Plik / obiekt | Ostatni commit | Data |
|---|---|---|
| `README.md` | `1662771aa` *fix(gic): root-cause R-F16 … (U-153)* | **2026-08-22 23:35** |
| `CHANGELOG.md` | `51cac0382` *…correct the R-702 record* | **2026-08-30 10:56** |
| zmiana nazwy nośnika (`50d2c2c2e`, R-611a) | tylko `feat/m1-bootable-medium` | **2026-08-30 19:22** |

`README.md` stoi **8 dni** i **72 pozycje CHANGELOG-a** za drzewem: znacznik `SYNC` w linii 5
podaje `U-152`, a najnowsza pozycja CHANGELOG-a to `U-224` (224 − 152 = 72). To pokrywa się
z ustaleniem audytu 02 §8.

`CHANGELOG.md` jest świeższy, ale **nie obejmuje commitu zmiany nazwy nośnika** — ten powstał
osiem godzin po ostatnim dotknięciu CHANGELOG-a i nie doczekał się wpisu.

---

## 1. Pytania zadane wprost — odpowiedzi

### 1.1 Czy README nadal mówi o `redox-live.iso`?

**NIE — i nigdy nie mówiło.** `git show <gałąź>:README.md | grep -n "redox-live\|\.iso"` →
**zero trafień** na obu gałęziach. Jedyne nazwy artefaktów w README to `harddrive.img`
(linie 241–242), a te są **poprawne**.

Dryf jest **odwrotny do spodziewanego**: po `50d2c2c2e` cel `make live` produkuje
`eos-$(EOS_VERSION)-$(ARCH)-installer.img` (`mk/config.mk:194-195`), doszedł cel
`make print-installer-medium` (`Makefile:14-15`) i istnieje `docs/install.md` — a **README
nie wspomina o nośniku instalacyjnym ani razu**. Czytelnik README nie dowie się, że E-OS ma
nośnik instalacyjny, jak się nazywa ani jak go zapisać. Szczegóły: wiersz `R-611a` w §2.

### 1.2 Czy README mówi prawdę o `keys/eos-repo-sign.pub.toml`?

**NIE. Twierdzenie jest nieprawdziwe — audyt 02 §8 ma rację.**

README `:134` — *„but `keys/eos-repo-sign.pub.toml` does not exist yet (`R-702`)"*
README `:312` — *„no public key is pinned in any image (`R-702`), so it warns and proceeds"*

Pomiar:

```
git ls-tree -r --name-only main -- keys/
git ls-tree -r --name-only feat/m1-bootable-medium -- keys/
  keys/README.md
  keys/eos-pkg-signing.pub.toml
  keys/eos-release.pub
  keys/eos-repo-sign.pub.toml      ← ISTNIEJE, na OBU gałęziach
  keys/wycofane/eos-release-DCEC85BA6057ED4A.pub
```

CHANGELOG `:16` (`U-224`) prostuje to wprost: *„Klucz `R-702` istnieje — moja poprzednia
diagnoza była błędna"*, klucz publiczny **wpięty w oba obrazy** jako
`/etc/pkg/eos-repo-sign.pub.toml`, klient **zawodzi bezpiecznie** zamiast ostrzegać i jechać dalej.

**CHANGELOG został poprawiony 2026-08-30; README nie.** To ten sam fakt opisany w dwóch
plikach w dwóch przeciwnych wersjach, w jednym drzewie.

**Po ponownym pomiarze — poprawione w połowie, i tylko na jednej gałęzi.** `57fe451ed` przepisał
`README.md:134` na `feat/m1-bootable-medium`; nowa treść (`:132-137` po zmianie) brzmi
*„`keys/eos-repo-sign.pub.toml` is tracked (4075 B, added 2026-08-28) and is pinned into the image
at `/etc/pkg/eos-repo-sign.pub.toml`, so a bad index is fatal"*. Ale **drugie** miejsce z tym samym
fałszem zostało nietknięte: `feat/m1-bootable-medium:README.md:317` nadal mówi *„no public key is
pinned in any image (`R-702`), so it warns and proceeds"* — dosłownie to samo zdanie, które
`:134` właśnie odwołało. Na `main` (`:312`) stoi oczywiście też. §4 poz. 1 nazywała to „jedną
poprawką w dwóch miejscach"; wykonano jedno z dwóch, więc README **sam sobie przeczy** na obu
gałęziach, zamiast mówić nieprawdę spójnie.

### 1.3 Czy README mówi prawdę o `eos-guard` i `eos-sysmon`?

**NIE. Audyt 02 §8 ma rację — nie ma ich w obrazie.**

README `:144-145` — *„🛡️ **eos-guard** … i 📈 **eos-sysmon** …, **both native Crimson apps**"*
README `:294` — wiersz tabeli komponentów `eos-notes / eos-guard / eos-sysmon`

Pomiar w drzewie (obie gałęzie identycznie):

- Receptury **istnieją**: `recipes/gui/eos-guard/recipe.toml`, `recipes/gui/eos-sysmon/recipe.toml`
- W konfiguracjach obrazu **nie ma ich jako pakietów**. `config/x86_64/eos.toml:21-25`:

```
[packages.eos-notes]
# U-095: E-OS Control consolidates the system monitor (eos-sysmon)
# and the integrity monitor (eos-guard) into one tabbed control center.
# The standalone repos remain (archived); only eos-control ships.
[packages.eos-control]
```

To samo w `config/aarch64/eos.toml:19-23`. **Konfiguracja sama dokumentuje, że tylko
`eos-control` trafia do obrazu** — README wymienia dwie aplikacje, które nie istnieją
jako osobne. Funkcja jest, opis jest nieaktualny.

**Po ponownym pomiarze — również poprawione w połowie, i tylko na jednej gałęzi.** `57fe451ed`
przepisał `README.md:144-145` na `feat/m1-bootable-medium`; nowa treść mówi wprost *„shipped as
tabs inside **eos-control**, not as separate apps"* z odsyłaczem do `config/x86_64/eos.toml:22-23`.
Drugie miejsce zostało nietknięte: wiersz tabeli komponentów
`feat/m1-bootable-medium:README.md:299` (na `main` `:294`) nadal brzmi
`| eos-notes / eos-guard / eos-sysmon | Notes · FS-integrity monitor · system monitor | **E-OS** |`
i nadal przedstawia obie aplikacje jako dostarczane składniki.

---

## 2. `README.md` — tabela twierdzeń

Numeracja linii wg `git show main:README.md`. **Uwaga po kontroli adwersaryjnej:** numeracja
**nie jest już wspólna dla obu gałęzi** — na `feat/m1-bootable-medium` wszystko od linii 150 w dół
jest przesunięte o **+5** (patrz §0). Wiersze poniżej opisują stan `main`, czyli stan, który
zobaczy każdy, kto sklonuje projekt; tam, gdzie gałąź robocza już się różni, jest to powiedziane
w kolumnie „Stan faktyczny".

| Plik | Linia | Twierdzenie | Stan faktyczny (zmierzony) | Werdykt | Co zrobić |
|---|---|---|---|---|---|
| README.md | 5 | `SYNC: v0.2.0 · Unreleased U-152 · 2026-08-22` | Najnowsza pozycja CHANGELOG-a to `U-224` (`CHANGELOG.md:16`). README jest **72 pozycje** w tyle; ostatni commit README `1662771aa` z 2026-08-22 | **DRYF** | Przestawić znacznik na `U-224` **dopiero po** naprawie pozycji z tej tabeli — inaczej znacznik zalegalizuje nieprawdziwe zdania |
| README.md | 19 | Odznaka `Rust nightly-2026-05-24` | `rust-toolchain.toml`: `channel = "nightly-2026-05-24"` | **ZGODNE** | — |
| README.md | 21 | Odznaka `arch x86_64 · aarch64` | `config/x86_64/eos.toml`, `config/aarch64/eos.toml` obecne; `mk/config.mk:8` wymienia `aarch64, i586, x86_64` | **ZGODNE** | — |
| README.md | 27 | Odznaka `version 0.2.0 dev` | Zgodne z tagiem `v0.2.0` i `mk/config.mk:185 EOS_VERSION?=0.2.0`. **Sprzeczne** z `config/x86_64/eos.toml:99` i `config/aarch64/eos.toml:97` → `VERSION_ID="0.1.0"` oraz `config/base.toml:104` → `VERSION_ID="0.9.0"` | **DRYF** (znalezisko **G-17**, nienaprawione) | Nie ruszać odznaki osobno. G-17 to jedna decyzja: która wersja trafia do `/etc/os-release`. `mk/config.mk:180-184` już wylicza wszystkie cztery miejsca i wskazuje tag jako rozstrzygający |
| README.md | 28 | Odznaka `desktop boot verified ✓` | Wymaga uruchomienia QEMU; z drzewa niesprawdzalne | **[NIEZWERYFIKOWANE]** | Albo zmierzyć `scripts/ci-boot-smoke.sh`, albo dopisać przy odznace datę i gałąź ostatniego pomiaru |
| README.md | 29–30 | Odznaki `FDE hardware AES ✓`, `RAID-1 mirror ✓` | Audyt 02 §8 potwierdza AES-XTS w binarkach obrazu i `raid1d` w obrazie — pomiar na obrazie, nie na tym drzewie | **[NIEZWERYFIKOWANE]** w tym drzewie | Jak wyżej — odznaka bez daty pomiaru starzeje się po cichu |
| README.md | 38 | Odnośnik do dokumentacji `https://e-os.gitlab.io/e-os/` | Zasób zewnętrzny; nie mierzony w tym przebiegu. Zadanie `pages` istnieje (`.gitlab-ci.yml:252`), ale **ostatni pipeline `2803590199` w całości `ci_quota_exceeded`, 0 s** | **[NIEZWERYFIKOWANE]** | Sprawdzić, czy strona nie serwuje treści sprzed wyczerpania limitu CI |
| README.md | 47 | „GitHub Actions is off account-wide" | Zgodne z pomiarem: 276 historycznych uruchomień, ostatnie prawdziwe workflow'y 2026-06-16, minimalny workflow `on: push` wypchnięty wprost na github.com **nie utworzył uruchomienia** | **ZGODNE** | — |
| README.md | 47 vs 218 | L47: GitHub to „read-only mirror", zgłoszenia i MR-y na GitLabie. L218: `git clone https://github.com/Gh0s777tt/E-OS.git` | Sprzeczność wewnętrzna. `docs/adr/0001-gitlab-jako-zrodlo-prawdy.md` czyni GitLaba źródłem prawdy | **DRYF** | Zmienić polecenie w Quick Start na `https://gitlab.com/e-os/e-os`, GitHuba zostawić jako wzmiankę o lustrze |
| README.md | 57–71 | Trzy zrzuty ekranu (`assets/screenshots/eos-cosmic-desktop.png`, `-login-greeter.png`, `-bootloader.png`) | Wszystkie trzy pliki obecne w drzewie | **ZGODNE** | — |
| README.md | 59 | Podpis „E-OS 0.1.0 »Genesis«" | Zgodne z `config/*/eos.toml` (`VERSION_ID="0.1.0"`), niezgodne z odznaką 0.2.0 w linii 27 | **DRYF** (część **G-17**) | Rozstrzygnąć razem z G-17 |
| README.md | 113 | `cosmic-comp` nie działa na żadnej architekturze | Odnośnik do `docs/known-issues.md` — plik istnieje; samego twierdzenia z drzewa nie sprawdzę | **[NIEZWERYFIKOWANE]** | — |
| README.md | **134** | „but `keys/eos-repo-sign.pub.toml` **does not exist yet** (`R-702`)" | **Plik istnieje na obu gałęziach** (`keys/eos-repo-sign.pub.toml`). `CHANGELOG.md:16` (`U-224`) prostuje: klucz jest, wpięty w oba obrazy jako `/etc/pkg/eos-repo-sign.pub.toml`, klient zawodzi bezpiecznie | **DRYF — twierdzenie nieprawdziwe**; poprawione na `feat/m1-bootable-medium` (`57fe451ed`), **na `main` stoi nadal** | Zrobione na gałęzi roboczej — nowa treść w `feat…:README.md:132-137`. **Nie zamknięte:** bliźniacze zdanie w `:310-315` (`main`) / `:315-320` (gałąź) nie zostało tknięte, więc README przeczy dziś sam sobie. Domknąć razem, potem scalić do `main` |
| README.md | 136 | „Since then — the `U-081`–`U-114` wave" | Obie pozycje obecne w CHANGELOG-u | **ZGODNE** | — |
| README.md | **144–145** | „**eos-guard** … i **eos-sysmon** …, **both native Crimson apps**" | Receptury istnieją (`recipes/gui/eos-guard`, `recipes/gui/eos-sysmon`), ale **żadna z nich nie jest pakietem** w `config/x86_64/eos.toml` ani `config/aarch64/eos.toml`. Komentarz `config/x86_64/eos.toml:22-24`: *„only eos-control ships"* | **DRYF**; poprawione na `feat/m1-bootable-medium` (`57fe451ed`), **na `main` stoi nadal** | Zrobione na gałęzi roboczej — nowa treść mówi *„shipped as tabs inside eos-control, not as separate apps"* z odsyłaczem do `config/x86_64/eos.toml:22-23`. **Nie zamknięte:** wiersz tabeli komponentów `:294` (`main`) / `:299` (gałąź) nadal wymienia obie aplikacje jako składniki |
| README.md | 194 | Lista sterowników `nvmed`, `virtio-blkd`, `e1000d`, `xhcid`, `usbnetd`, `ihdad`, `ahcid` | Sterowniki mieszkają w pakiecie `base` (fork `eos-base`), którego nie ma w tym drzewie. `git grep` po `config/` trafia tylko `e1000d` i `raid1d`, i to w komentarzach | **[NIEZWERYFIKOWANE]** w tym drzewie | Audyt 02 §6 liczy 16 sterowników w obrazie — zacytować tamten pomiar zamiast utrzymywać listę bez źródła |
| README.md | 197 | Lista zadań CI: `secret-scan · integrity · pin-check · docs-currency · rust-checks`/cargo-deny `· pages` + ciężki build/boot-smoke na `eos-heavy` | Wymienione nazwy **są poprawne** (`.gitlab-ci.yml:22, 30, 39, 80, 128, 252`; `cargo-deny` :149-160; `eos-heavy` :293, 353; `boot-smoke` :384). Lista jest jednak **niepełna** — pomija `mirror-drift` (:52), `rebase-check` (:66), `shell-lint` (:177), `coverage` (:200), `sbom` (:218), `rustdoc` (:237), `docs-pdf` (:291), `semantic-release` (:331), `renovate` (:439) | **DRYF (niepełne)** | Dopisać brakujące zadania albo napisać „m.in."; nie zostawiać listy wyglądającej na zamkniętą |
| README.md | 197 | Cały akapit przedstawia CI jako **działające** | **Ostatni pipeline `2803590199`: wszystkie zadania `ci_quota_exceeded`, czas 0 s.** GitHub Actions nie wykonują się w ogóle. **Żadna z opisanych bramek dziś nie chodzi** | **DRYF — najpoważniejszy w tym pliku** | Dopisać jedno zdanie o stanie: bramki są **skonfigurowane**, nie **wykonywane**. Czytelnik ma dziś prawo sądzić, że każdy commit przechodzi cargo-deny |
| README.md | 218 | `git clone https://github.com/Gh0s777tt/E-OS.git` | Patrz wiersz „47 vs 218” | **DRYF** | — |
| README.md | 225–226 | `make CI=1 all` buduje „the full **x86_64** desktop image” | `mk/config.mk:9` → `ARCH?=$(HOST_ARCH)`, `:5` → `HOST_ARCH?=$(shell uname -m)`, **bez normalizacji**; `:20` → `CONFIG_NAME?=desktop`. Architektura zależy od hosta, nie jest ustalona na x86_64. README `:212` wymienia macOS/Apple Silicon jako host wspierany. Czy `make` liczy `uname -m` na hoście czy w kontenerze (`PODMAN_BUILD?=1`) — **nie zmierzone** | **[NIEZWERYFIKOWANE]**, podejrzenie dryfu | Zmierzyć `make -n print-installer-medium` na Apple Silicon. Jeśli `ARCH` wychodzi `arm64`, README powinno podawać `ARCH=x86_64` wprost |
| README.md | 241–242 | Obrazy wyjściowe `build/x86_64/desktop/harddrive.img` · `build/aarch64/eos/harddrive.img` | `Makefile:8` → `all: $(BUILD)/harddrive.img`; `mk/config.mk:169` → `BUILD=build/$(ARCH)/$(CONFIG_NAME)`. Ścieżki zgadzają się dla podanych `ARCH`/`CONFIG_NAME` | **ZGODNE** | — |
| README.md | — (brak) | **Nośnik instalacyjny nie występuje w README ani razu** | Po `50d2c2c2e` (**tylko `feat/m1-bootable-medium`**, `main: NIE`): `mk/config.mk:194-195` → `INSTALLER_MEDIUM_NAME=eos-$(EOS_VERSION)-$(ARCH)-installer.img`; `Makefile:14-15` → cel `print-installer-medium`; `Makefile:18` → cel `live`; `docs/install.md` istnieje i **nie jest z README linkowany** | **DRYF (pominięcie), `R-611a`** | Dopisać do Quick Start cel `make live`, nazwę artefaktu i odnośnik do `docs/install.md`. **Uwaga: dotyczy wyłącznie `feat/m1-bootable-medium`** — na `main` zmiany nazwy jeszcze nie ma, więc poprawka README musi jechać w tej samej gałęzi co `50d2c2c2e`, nie w oderwaniu |
| README.md | 244–245 | OOBE wymusza zmianę hasła na każdej ścieżce (`R-602`) | Audyt 02 §8 potwierdza ciągi znaków w binarkach obrazu (`orblogin`, `login`) — pomiar na obrazie | **[NIEZWERYFIKOWANE]** w tym drzewie | — |
| README.md | 247–249, 275–276, 315, 325, 336, 339, 345–348, 372, 381, 389, 395–396 | Wszystkie odnośniki do plików repozytorium | Sprawdzono 22 ścieżki (`ROADMAP.md`, `SECURITY.md`, `CLAUDE.md`, `EOS_BUILD_STATE.md`, `docs/ci.md`, `docs/hardening.md`, `docs/security.md`, `licenses/Redox-OS-MIT.txt`, zrzuty ekranu …) — **wszystkie istnieją** | **ZGODNE** | — spójne z pomiarem „410 OK, 0 błędów” na tej gałęzi |
| README.md | 202–205 | Wykres składu: Rust 88 %, Assembly 4 %, Shell/build 5 %, Config/docs 3 % (opisany „approx.”) | W **tym** repozytorium (`git ls-files`, przeliczone przy kontroli): 22 pliki `.rs`, 59 `.sh`, 3211 `.toml`, **89** `.md` (87 w chwili pomiaru — dwa nowe pliki to raporty audytu), **0** `.s`/`.S`. Kod Rusta mieszka w forkach (`eos-kernel`, `eos-relibc`, `eos-redoxfs`, `eos-base` …), których w tym drzewie nie ma. Zakres wykresu (cała dystrybucja czy repozytorium) **nie jest w README określony** | **[NIEZWERYFIKOWANE]** | Dopisać przy wykresie, co jest liczone i czym zmierzone, albo usunąć. Dziś nie da się go ani potwierdzić, ani obalić |
| README.md | 296 | `tools/eos-repo-sign` — prototyp hybrydowego podpisu | `tools/eos-repo-sign/{Cargo.toml,Cargo.lock,README.md,src/main.rs}` obecne | **ZGODNE** | — |
| README.md | **310–315** | „no public key is pinned in any image (`R-702`), so it **warns and proceeds**” | Zaprzeczone przez `CHANGELOG.md:16` (`U-224`): klucz publiczny **wpięty w oba obrazy**, wartości w `config/x86_64` i `config/aarch64` zgadzają się z `keys/eos-repo-sign.pub.toml`, klient **zawodzi bezpiecznie** | **DRYF — twierdzenie nieprawdziwe**, **niepoprawione na żadnej gałęzi** | Priorytet 1, i to jest teraz **jedyna** połowa, która została. `57fe451ed` poprawił linię 134 i tę **pominął**: `feat/m1-bootable-medium:README.md:317` nadal niesie *„no public key is pinned in any image"*, czyli dokładnie to, co `:134` na tej samej gałęzi już odwołało. Jedna poprawka, dwa miejsca — wykonano jedno |
| README.md | 316–319 | Hook `pre-commit` z gitleaks „fails closed” (`U-140`), dawniej kończył się `\|\| true` | `lefthook.yml:4-29`: sekcja `pre-commit`, komenda `gitleaks`, brak narzędzia = `exit 1`, świadome obejście przez `EOS_SKIP_SECRET_SCAN=1`. Komentarz :11-13 potwierdza historię z `\|\| true` | **ZGODNE** | — |
| README.md | 316–318 | `secret-scan` w CI nad pełną historią (`GIT_DEPTH: 0`) | `.gitlab-ci.yml:22` zadanie istnieje. **Nie wykonuje się** — `ci_quota_exceeded`. Osobno: `gitleaks` **FAIL** na `main`, `docs/installer-design`, `feat/m1-bootable-medium`, `chore/docs-rebuild`, `chore/repo-restructure` — fałszywy alarm C-19 na dwóch plikach kluczy **publicznych**; allowlista (`.gitleaks.toml:41,50`) istnieje **tylko** na `fix/p0-audit-findings` i `chore/security-hardening` | **DRYF** | README opisuje bramkę, która na tej gałęzi jest czerwona i nie chodzi. Wpierw doprowadzić allowlistę na `main`, potem opisywać |
| README.md | 320–325 | `cargo-deny` w `rust-checks`, `pin-check` (`eos-repos.sh pins --strict`), każda binarka SHA256-pinned (`U-118`) | `.gitlab-ci.yml:128` `rust-checks`, `:149-152` pobranie + `sha256sum -c -` + `cargo-deny check`, `:39` `pin-check`, `scripts/eos-repos.sh` obecny | **ZGODNE** jako konfiguracja | Dopisać, że bramka nie jest dziś wykonywana (patrz wiersz o CI) |
| README.md | 327 | „`CODEOWNERS` exists but is advisory” | `.github/CODEOWNERS` istnieje. **Leży wyłącznie pod `.github/`**, czyli po stronie lustra. GitLab — deklarowane źródło prawdy (`ADR-0001`) — czyta `CODEOWNERS` z katalogu głównego lub `.gitlab/`, więc tam **nie jest w ogóle wczytywany** | **DRYF (niedopowiedzenie)** | „advisory” jest łagodniejsze niż stan faktyczny. Albo przenieść/zduplikować plik do `.gitlab/CODEOWNERS`, albo napisać, że na GitLabie go nie ma |
| README.md | **328–329** | „the project has had **0 merge requests**” | **Dziewięć otwartych MR-ów**, zmierzone dzisiaj: `e-os/e-os` !1–!4, `eos-installer` !1–!3, `eos-pkgutils` !1–!3 | **DRYF** | Zaktualizować liczbę. Wniosek zdania (CI raportuje po pushu, nie blokuje) **pozostaje prawdziwy** — bo `only_allow_merge_if_pipeline_succeeds` jest wyłączone, a pipeline i tak nie startuje. Poprawić przesłankę, nie wniosek |
| README.md | 331–333 | Commity SSH-podpisane; `v0.2.0` pierwszym **anotowanym, podpisanym** tagiem; dwa starsze tagi *lightweight* | `git cat-file -t`: `v0.2.0` → **tag**, `v0.1.0` → **commit**, `eos-base-2026-06-06` → **commit**. `git verify-tag v0.2.0` → *Good "git" signature … ED25519 SHA256:xdbjGJ4nQ865Ek53oR0ow2eT+DrLGdgovsA2TuQyMz4* | **ZGODNE** | — jedno z niewielu twierdzeń README podpartych weryfikowalnym dowodem kryptograficznym |
| README.md | 356 | „Current: **v0.2.0** — a development milestone, not a published release” | Najwyższy tag produktu to `v0.2.0`; treść tagu potwierdza: *„This tag marks a TREE, not a published release"* | **ZGODNE** | — |
| README.md | 358 | `v0.1.0` „stays as a historical marker **232 commits** back” | `git rev-list --count v0.1.0..v0.2.0` = **233**. (Dla kontekstu, przeliczone przy kontroli adwersaryjnej: `v0.1.0..main` = 309, `v0.2.0..main` = 76, `v0.2.0..feat/m1-bootable-medium` = **86** — w chwili pomiaru 83, gałąź dostała od tego czasu trzy commity.) Ta sama liczba 232 stoi też w treści tagu `v0.2.0` | **DRYF (drobny)** | Poprawić na 233 w README. **Treści tagu nie da się poprawić bez przetagowania** — zostawić, odnotować rozbieżność |
| README.md | 360 | „plus … `U-137`…`U-152`" | Obie pozycje obecne w CHANGELOG-u | **ZGODNE** | — |
| README.md | 362–366 | Tag `v0.2.0` niesie `R-F16`; **naprawione na `main` po tagu** przez `U-153` (read-modify-write na `GICD_ICENABLER`) | `CHANGELOG.md:112` — pozycja `U-153` istnieje i opisuje dokładnie tę przyczynę (`eos-kernel` `9687852732` → `18dce5577d`). `v0.2.0..main` = 76 commitów, więc „po tagu” się zgadza | **ZGODNE** | — |
| README.md | **368–369** | „⚠️ **Still open (`R-F18`)**: … boots in ~110–124 s instead of ~16 s” | `CHANGELOG.md:58` — `U-180`: **„`R-F18` naprawione"**. Zmierzone: `virq 37` 16 829 830 → **8**; najgorszy wiersz **160 s → 16 s**; cała matryca 10/10 w **16–33 s**; `boot-smoke` PASS. Poprawka w `eos-base` `816546df` | **DRYF** | Priorytet 1. README trzyma jako otwartą usterkę, którą CHANGELOG zamyka pomiarem. Twierdzenie było prawdziwe 2026-08-22 (ostatnie dotknięcie README) i unieważnione przez `U-180`. Przenieść do sekcji rozwiązanych z odsyłaczem do `U-180`, uwzględniając sprostowanie `U-181` |

---

## 3. `CHANGELOG.md` — tabela twierdzeń

| Plik | Linia | Twierdzenie | Stan faktyczny (zmierzony) | Werdykt | Co zrobić |
|---|---|---|---|---|---|
| CHANGELOG.md | 3–4 | „following [Keep a Changelog] and [Semantic Versioning]" | Plik ma **jedną jedyną** sekcję wersji: `## [Unreleased]` (`:13`). Nagłówki w całym pliku: `# Changelog` (:1), `## [Unreleased]` (:13), `### Added & Changed` (:15), `### Fixed` (:980). **Nie ma `## [0.2.0]`, nie ma `## [0.1.0]`, nie ma ani jednej daty wydania** | **DRYF** | Wydzielić `## [0.2.0] — 2026-08-22` z pozycji do `U-152` włącznie; resztę (`U-153`…`U-224`) zostawić w `[Unreleased]`. Bez tego deklaracja z linii 3 jest pusta |
| CHANGELOG.md | 13 + całość | Wszystkie **151 pozycji** (`U-071` … `U-224`) leżą pod `[Unreleased]` | Tag **`v0.2.0` istnieje**, jest anotowany i podpisany, a mimo to **żadna pozycja nie jest przypisana do wydania**. Pozycje sprzed tagu i po tagu są w pliku nieodróżnialne | **DRYF — niespójność z tagiem `v0.2.0`** | Jak wyżej. Granica przebiega po `U-152` — potwierdza to zarówno znacznik `SYNC` w README `:5`, jak i treść tagu |
| CHANGELOG.md | 16 (`U-224`) | „Klucz `R-702` istnieje — moja poprzednia diagnoza była błędna”, klucz wpięty w oba obrazy | `keys/eos-repo-sign.pub.toml` **obecny w drzewie** na obu gałęziach. Wpis jest **zgodny z drzewem i prostuje README** | **ZGODNE** | Nic w CHANGELOG-u. To README ma się do tego dostosować (§1.2) |
| CHANGELOG.md | 16 (`U-224`) | Wynik pełnego builda: `serial = 10175`, **85 pakietów**, oba obrazy po 1400 MiB, binarka `pkg` z czterema nowymi wariantami błędów | Liczby pochodzą z artefaktów spoza tego drzewa (`~/eos-artifacts/`); nie odtwarzalne z `git` | **[NIEZWERYFIKOWANE]** | Zostawić — wpis podaje metodę pomiaru, co jest właściwym poziomem staranności |
| CHANGELOG.md | 58 (`U-180`) | `R-F18` naprawione, `virq 37` 16 829 830 → 8, 160 s → 16 s, `boot-smoke` PASS | Wpis wewnętrznie spójny i wskazuje rewizję forka (`eos-base` `816546df`). Sprzeczny z README `:368-369` | **ZGODNE** (dryf jest po stronie README) | Patrz README `:368-369` |
| CHANGELOG.md | 57 (`U-181`) | Sprostowanie do `U-180`: `R-F25` nie było usterką, tylko zepsutym przyrządem (`pgrep` bez `-f` na macOS obcina nazwę do 15 znaków) | Wpis prostuje poprzedni **osobną pozycją**, zamiast po cichu edytować tamten | **ZGODNE** | — wzorzec do utrzymania |
| CHANGELOG.md | 112 (`U-153`) | `R-F16` rozpoznane i naprawione (`eos-kernel` `9687852732` → `18dce5577d`) | Pozycja obecna, zgodna z README `:362-366` | **ZGODNE** | — |
| CHANGELOG.md | 5–11 | Historia sprzed `U-071` jest pod kodami `R-NNN`; dziesięć numerów (`U-001`–`U-006`, `U-008`, `U-012`–`U-014`) **nie występuje w historii `main`**; przetrwały na `origin/archive/pre-migration-de-phase1` | Sprawdzenie próbki: `git log main --grep="U-012"` zwraca **wyłącznie** commit `bb4855006` *„pre-U-071 history lives on an archived branch (U-131)"*, czyli sam ten meta-wpis. Twierdzenie się broni | **ZGODNE** | — |
| CHANGELOG.md | — (brak) | **Brak wpisu o zmianie nazwy nośnika (`R-611a`)** | `git show feat/m1-bootable-medium:CHANGELOG.md \| grep -c "R-611a"` → **0**. Commit `50d2c2c2e` (2026-08-30 **19:22**) jest **osiem godzin po** ostatnim commicie CHANGELOG-a (`51cac0382`, 2026-08-30 **10:56**) i **nie dotknął ani `README.md`, ani `CHANGELOG.md`** (14 zmienionych plików, same `Makefile`/`mk/`/`scripts/`/`redox.ipxe`) | **DRYF (brak wpisu)** | Dopisać pozycję `U-225` o `R-611a` na `feat/m1-bootable-medium`. Uzasadnienie i pomiary są już gotowe w komunikacie commitu i w `mk/config.mk:187-193` |
| CHANGELOG.md | 16, 23 | `U-224` wymienia artefakt `eos-x86_64-live.iso` | Nazwa **nieaktualna na `feat/m1-bootable-medium`** po `50d2c2c2e`, aktualna na `main` (gdzie zmiany nie ma). Wpis powstał **przed** commitem zmiany nazwy | **ZGODNE jako zapis historyczny** | **Nie poprawiać.** Wpis opisuje, co się wtedy zdarzyło. Poprawa historii dla nowej nazwy zatarłaby ślad — nową nazwę wprowadza nowa pozycja |
| CHANGELOG.md | 31, 61, 65, 140 | Cztery odwołania do `redox-live.iso` w starszych pozycjach | Zapisy historyczne (`U-…` opisujące pracę sprzed zmiany nazwy) | **ZGODNE jako zapis historyczny** | Jak wyżej — nie ruszać |
| CHANGELOG.md | 980 | Sekcja `### Fixed` zaczyna się od `U-085` | Sekcja istnieje; pozycje w niej są **starsze** niż w `### Added & Changed` powyżej, czyli plik ma dwie niezależne, malejące osie czasu pod jednym `[Unreleased]` | **DRYF (strukturalny)** | Przy wydzielaniu sekcji `[0.2.0]` uporządkować przy okazji — dziś czytelnik nie wie, że pod `Added & Changed` kończy się na `U-085`, a `Fixed` startuje od nowa |

---

## 4. Skrót: co naprawić najpierw

Kolejność wg tego, ile szkody robi nieprawda, nie wg trudności.

> **Stan wykonania po kontroli adwersaryjnej.** Z ośmiu punktów niżej ruszone są **dwa, każdy
> w połowie**, i wyłącznie na `feat/m1-bootable-medium` (`57fe451ed`). Na `main` nie ruszono nic.
> Nic nie jest scalone, więc żaden z tych punktów nie jest zamknięty wobec produktu.

1. **README `:134` i `:310-315` — klucz `eos-repo-sign`.** README twierdzi, że kluczowego
   elementu bezpieczeństwa nie ma, a on jest i działa. To jedyne miejsce, w którym dokument
   **zaniża** produkt. Źródło poprawki gotowe: `CHANGELOG.md:16` (`U-224`).
   **Stan: `:134` poprawione na gałęzi roboczej, `:310-315` NIE — na żadnej gałęzi.** Zdania
   przeczą sobie w jednym pliku; to jest gorzej niż przed poprawką, dopóki druga połowa nie wejdzie.
2. **README `:368-369` — `R-F18`.** Utrzymuje jako otwartą usterkę zamkniętą pomiarem
   w `U-180` (`CHANGELOG.md:58`). **Stan: nietknięte** (`main:README.md:368-369`,
   `feat…:README.md:373-374`).
3. **README `:144-145` i `:294` — `eos-guard` / `eos-sysmon`.** Opisane jako osobne aplikacje;
   do obrazu trafia wyłącznie `eos-control` (`config/x86_64/eos.toml:22-24`).
   **Stan: `:144-145` poprawione na gałęzi roboczej, `:294` NIE** — wiersz tabeli komponentów
   (`main:294` / `feat…:299`) nadal wymienia obie aplikacje.
4. **README `:197` — stan CI.** Dokument przedstawia bramki jako działające; ostatni pipeline
   to `ci_quota_exceeded` w każdym zadaniu, 0 s, a GitHub Actions nie wykonują się wcale.
   **Stan: nietknięte.**
5. **README `:328-329` — „0 merge requests”.** Otwartych MR-ów jest dziewięć — i to jest wciąż
   aktualne, bo **żaden nie został scalony**. **Stan: nietknięte.**
6. **CHANGELOG — wydzielić `## [0.2.0]`.** 151 pozycji pod `[Unreleased]` przy istniejącym
   podpisanym tagu przeczy deklaracji Keep a Changelog z linii 3. **Stan: nietknięte** —
   `CHANGELOG.md` jest bajt w bajt ten sam na obu gałęziach (`9224d9a42…`).
7. **CHANGELOG — dopisać pozycję o `R-611a`** (tylko `feat/m1-bootable-medium`) i **dopisać
   nośnik instalacyjny do README** (`make live`, `eos-<wer>-<arch>-installer.img`,
   `docs/install.md`). **Stan: nietknięte** — `grep -c 'R-611a'` w `CHANGELOG.md` nadal 0.
8. **README `:5` — znacznik `SYNC`.** Przestawić na `U-224` **na końcu**, po punktach 1–7.
   Przestawiony wcześniej poświadczy nieprawdę jako aktualną. **Stan: nietknięte, i słusznie** —
   punkty 1–7 nie są domknięte, a punkty 1 i 3 są domknięte w połowie.

### Poza zakresem tej naprawy

- **G-17** (wersja w czterech miejscach: `config/base.toml:104` → 0.9.0,
  `config/x86_64/eos.toml:99` i `config/aarch64/eos.toml:97` → 0.1.0,
  `mk/config.mk:185` → 0.2.0, tag → v0.2.0). Dotyka README `:27` i `:59`, ale sam
  w sobie **nie jest usterką dokumentacji** — to decyzja o `/etc/os-release`.
  `mk/config.mk:180-184` już ją opisuje i świadomie odkłada.
- **Tag `v0.2.0` niesie w treści liczbę 232** przy zmierzonych 233 commitach.
  Nie do poprawienia bez przetagowania; odnotowane.
- **`gitleaks` FAIL** na pięciu gałęziach (fałszywy alarm C-19). To usterka konfiguracji
  (`.gitleaks.toml` bez allowlisty poza `fix/p0-audit-findings` i `chore/security-hardening`),
  nie dokumentacji — ale unieważnia twierdzenie README `:316-318` o działającym skanowaniu.

---

## 5. Ograniczenia tego pomiaru

- Sprawdzono **wyłącznie** `README.md` i `CHANGELOG.md`. `ROADMAP.md`, `ROADMAP-v2.md`,
  `SECURITY.md`, `CLAUDE.md`, `EOS_BUILD_STATE.md` i `docs/` **nie były w zakresie** —
  a `README.md` odsyła do nich 22 razy.
- Twierdzenia o zawartości **zbudowanego obrazu** (sterowniki, OOBE, `netsurf`, odznaki
  „verified”) są z tego drzewa niesprawdzalne. Tam, gdzie audyt 02 ma pomiar na obrazie,
  zacytowano audyt jako źródło — nie potwierdzono go niezależnie.
- Stan GitLaba/GitHuba (liczba MR-ów, pipeline `2803590199`, 276 uruchomień Actions) pochodzi
  z pomiaru przekazanego w zadaniu, nie z tego repozytorium. Zgadza się z tym, co widać
  w drzewie: `.gitlab-ci.yml` bez śladu wykonania, a `git ls-tree -r` pokazuje `.github/workflows/`
  i `verify.sh` **wyłącznie** na `chore/security-hardening` (8 workflowów + `verify.sh`; na `main`
  i `feat/m1-bootable-medium` zero).
- Nie wykonano żadnego builda, żadnego uruchomienia QEMU i żadnej bramki poza
  `ci-integrity.sh` i `eos-repos.sh pins --strict`.

### 5.1 Co dołożyła kontrola adwersaryjna (sekcja 0)

- **Sprawdzono ponad czterdzieści cytatów** z obu sekcji — ścieżkę, linię i treść linii.
  Czternaście adresów było błędnych i jest poprawionych; ich lista stoi w sekcji 0.
- **Nie sprawdzono ponownie** tego, co i tak było oznaczone `[NIEZWERYFIKOWANE]`: sieci,
  zawartości zbudowanego obrazu, źródeł forków, przebiegu QEMU. Te pozycje zostają jak były.
- **Nie uruchomiono `gitleaks`.** Twierdzenie o FAIL-u na pięciu gałęziach i o allowliście
  wyłącznie na `fix/p0-audit-findings` i `chore/security-hardening` pochodzi z briefu; z drzewa
  potwierdzono **tylko** drugą połowę — `git show <gałąź>:.gitleaks.toml` znajduje wpisy
  `keys/eos-pkg-signing.pub.toml` (`:41`) i `keys/upstream-redox-pkg.pub.toml` (`:50`) na tych
  dwóch gałęziach, a na `main` i `feat/m1-bootable-medium` pliku z tymi wpisami nie ma.
  Sam wynik skanu jest `[NIEZWERYFIKOWANE]`.
- **Nie użyto żadnego polecenia `git` zmieniającego stan** — ani przy pomiarze, ani przy kontroli.
