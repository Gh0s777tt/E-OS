# ROADMAP-v2.md — superseded

**Status:** merged into [`ROADMAP.md`](ROADMAP.md) on 2026-08-31 · **Owner:** Gh0s777tt

**Do not plan from this file and do not cite it.** Its content now lives in `ROADMAP.md`, which is
the single live roadmap and the single place where the status of an `R-*` identifier is maintained.

**Why it was merged:** two roadmaps meant two sources of truth. Each file declared the other
retired, so any identifier could be read two ways and neither reading could be falsified.

**What moved.** The Polish substance of this document was **translated** into English in
`ROADMAP.md`, not summarised: the `rhboot/shim-review` Secure Boot audit, the measured driver
inventory, the Mac / Linux / hardware split, the `eos-guard` and `eos-notes` plans, the standards
analysis, the anti-promises, and the whole §12 installer programme with milestones M1–M8. Every
`R-*` identifier that lived here is either in the merged register or in its Annex C list of
retired identifiers, each with a stated reason.

**Old section numbers, for anyone following a stale citation:** §1 → §1–2 · §2 → §5.1–5.2 ·
§3 → §8 · §4 → §4 · §5 → §7.3 · §6 → §7.4 · §7 → §12 · §8 → §12.5 · §9 → §5–§11 (the subject
register) · §10 → §6.5 · §11 → §14 · §12 → §3.4 and §6, with its identifier-collision decisions
in Annex B. Line-number citations of the form `ROADMAP-v2.md:NNN` no longer resolve and were
already stale before this merge; cite the `R-*` identifier instead.

**Nothing was deleted.** Removing a document is the owner's decision, so the file stays in the
tree. The full 122,262-byte text and every revision remain in git:
`git show 87e8194b1:ROADMAP-v2.md` is the last version before this replacement.
