# Deliberate pin drift allowlist for `eos-repos.sh pins --strict` (CI pin-check).
#
# One repo name per line (first token); everything after is a free-text reason.
# A repo listed here is allowed to have its recipe pin behind the fork tip without
# failing CI — use ONLY for intentional holds (e.g. a fork tip that is unverified
# or deliberately not yet shipped). Accidental staleness must be fixed by bumping
# the recipe rev, NOT by adding it here.
#
# Format:  <repo-name>   <reason>
#
# Both holds below are the R-902 network work (DHCP/static) pushed to the forks
# on 2026-07-24/25 but never bumped in the recipes. They are FEATURE commits, so
# a bump needs the full gate run (image build + boot-smoke) — impossible while
# the `eosbuild` container on the eos-heavy runner is gone (see U-114). Bump both
# pins and delete these lines as soon as the build container is restored.
eos-control    R-902 DHCP<->static toggle (40dc67f) unverified — build container down, bump deferred (U-114)
eos-installer  R-902 installer network pane (ed6eb7c) unverified — build container down, bump deferred (U-114)
