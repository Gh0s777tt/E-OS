# Deliberate pin drift allowlist for `eos-repos.sh pins --strict` (CI pin-check).
#
# One repo name per line (first token); everything after is a free-text reason.
# A repo listed here is allowed to have its recipe pin behind the fork tip without
# failing CI — use ONLY for intentional holds (e.g. a fork tip that is unverified
# or deliberately not yet shipped). Accidental staleness must be fixed by bumping
# the recipe rev, NOT by adding it here.
#
# Format:  <repo-name>   <reason>
#
# Currently EMPTY, and that is the desired steady state.
#
# History: the two R-902 holds (eos-control, eos-installer) placed here in U-114
# were cleared in U-132 — the gate run they were waiting for was executed and
# passed (cargo check both arches, `make CI=1 ARCH=aarch64 CONFIG_NAME=eos all`
# EXIT=0, boot-smoke PASS, EOS-CONTROL-SELFTEST-OK), and both recipe revs were
# bumped to their fork tips. If you add an entry here, state the removal
# condition, not just the reason.

eos-installer   R-604a (#9) jest scalone w forku, ale pin CELOWO nie jest podbity: ekran wyboru
#                dysku nie zostal ani razu uruchomiony na Redoksie (CLAUDE.md §5.3), bo harness nie
#                dociera do instalatora (#16). Tryb awarii jest bezpieczny — instalacja się nie
#                zaczyna zamiast iść na zły dysk — ale „nigdy nieuruchomione" nie jest stanem,
#                w którym wolno przypiąć rewizję wchodzącą do obrazu.
#                WARUNEK USUNIĘCIA: ci-install-smoke.sh dochodzi do promptu `Disk to erase:` i
#                pokazuje `refused:` na błędnej nazwie — wtedy podbić pin do tipu forka i USUNĄĆ
#                ten wpis. Praca czeka na gałęzi feat/r-604a-disk-identity.
