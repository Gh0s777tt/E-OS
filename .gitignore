/build/
/prefix/
.config
**/my_*
# Local settings folder for Jetbrains products (RustRover, IntelliJ, CLion)
.idea/
# Local settings folder for Visual Studio Professional
.vs/
# Local settings folder for vscode
.vscode/
# Local settings folder for the devcontainer extension that most IDEs support.
.devcontainer/
# Cookbook
/repo
/web
/cookbook.toml
/cookbook.lock
source
source.tmp
source-new
source-old
source.tar
source.tar.tmp
target
wget-log
.eos-bl-src/
.eos-uu-src/
.eos-orbdata-src/
*.key
/book
/release/*.img
/release/*.tar.gz
# Release checksums + signature are build outputs regenerated per-release by
# scripts/make-release.sh and shipped as release ASSETS — never a committed
# trust anchor (a committed copy goes stale and references non-shipped images).
/release/SHA256SUMS
/release/*.minisig
.eos-kernel-src
.eos-drivers-src
.eos-base-src
.DS_Store
