[source]
# E-OS vendored mirror of redox-os/extrautils (edit freely; sync via scripts/sync-forks.sh).
git = "https://github.com/Gh0s777tt/eos-extrautils.git"
branch = "master"
rev = "afbac08e38e87102e06e0f0b90668e29cf72bf6f"

[build]
template = "custom"
dependencies = [
    "xz"
]
script = """
# TODO: Can't be linked correctly yet
# DYNAMIC_INIT

if [ "${COOKBOOK_DYNAMIC}" != "1" ]; then
    install_flags+=" --features=static"
fi

cookbook_cargo
"""
