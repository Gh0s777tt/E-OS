[source]
# E-OS vendored mirror of redox-os/netdb (edit freely; sync via scripts/sync-forks.sh).
git = "https://gitlab.com/e-os/eos-netdb.git"
branch = "master"
rev = "8977054ec602f5dfc366272d82f81f21fcbe0ee9"

[build]
template = "custom"
script = """
cp -rv "${COOKBOOK_SOURCE}/"* "${COOKBOOK_STAGE}"
"""
