[source]
# Tracks upstream redox-os/redoxfs. (The U-030 SBOM pin was only needed against the
# June forks; the July fork rebase resolved the relibc-ABI drift, so redoxfs is
# unpinned again — verified booting to the greeter on both arches.)
git = "https://gitlab.redox-os.org/redox-os/redoxfs.git"

[build]
template = "custom"
script = """
# Must be statically linked
cookbook_cargo
"""
