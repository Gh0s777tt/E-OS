# Changelog

All notable changes to E-OS. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)
and the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

**How this file was rebuilt.** Reconstructed on 2026-08-30 from `git log`, the three annotated
tags, and the one published release. Each entry names the commit that introduced it, so any line
can be checked. Entries were assigned to a release by finding the commit that first added them and
testing which tag range it falls in — not by guessing from dates.

**Releases.** There is exactly one published release object, [`v0.1.0`](https://github.com/Gh0s777tt/E-OS/releases/tag/v0.1.0).
`v0.2.0` is an annotated, signed tag with **no** release object; `eos-base-2026-06-06` is an
unsigned pre-history marker. Tag list: <https://github.com/Gh0s777tt/E-OS/tags>.

---

## [Unreleased]

Work since `v0.2.0` (2026-08-22): 288 commits, 114 of them on 2026-09-01–02.

**Przegląd bramek, 2026-09-02.** Dwie tury audytu wieloagentowego objęły wszystkie 65 skryptów
(7539 wierszy — tyle liczyły tamtego ranka; poprawki poniżej je wydłużyły), `.gitlab-ci.yml`, osiem plików GitHub Actions, hooki `pre-commit`/`lefthook`/`pre-push`,
`Makefile` z `mk/*.mk` oraz narzędzie `eos-repo-sign`. Szukanym wzorcem było jedno pytanie: **czy to
sprawdzenie potrafi zawieść?** 328 agentów, 101 surowych znalezisk, **34 potwierdzone** po potrójnej
weryfikacji adwersaryjnej (67 obalonych). Każda poprawka poniżej ma pomiar w obie strony: stan przed,
stan po, i kontrolę negatywną pokazującą, że bramka teraz czerwieni się z właściwego powodu.

### Added
- **Pytanie „czy produkty da się zbudować na Windows i Linuksa" ma odpowiedź: tak, obie (`PR-008`).** Roadmapa trzymała tu `[UNVERIFIED]`; teraz stoją tam pomiary.
  - **Windows.** `cargo zigbuild --target x86_64-pc-windows-gnu` na macOS daje `PE32+ executable (console) x86-64, for MS Windows`, **22 818 304 B**, w **1 m 23 s** — ale dopiero po obejściu **wady upstreamu**: `i-slint-backend-winit` 1.17.1 deklaruje pole `muda_adapter` pod `#[cfg(muda)]`, a dopasowuje je w gałęzi zabezpieczonej wyłącznie `#[cfg(target_os = "windows")]`. Ponieważ `muda` jest **niejawną** cechą (opcjonalna zależność, nie ma jej w `[features]`), przy `default-features = false` — czyli tak, jak buduje **każdy** produkt E-OS, żeby cel Redox nie wciągnął backendu okienkowego — ten crate **w ogóle nie kompiluje się na Windows**. Zapisane w `known-issues.md` razem z jednoliniową poprawką; zgłoszenie na cudzy tracker to działanie właściciela, nie narzędzia.
  - **Linux.** Ten sam tool daje `ELF 64-bit LSB pie executable, x86-64 … stripped`, **17 001 800 B**, w **3 m 08 s** — po przeniesieniu fontconfiga z czasu linkowania do czasu wykonania cechą `i-slint-common/fontconfig-dlopen`. Koszt wypowiedziany, nie ukryty: przypina **wewnętrzny** crate Slinta dokładną wersją, więc podbicie Slinta musi ruszyć obie linie naraz.
  - **Oczywista alternatywa zmierzona i odrzucona.** Budowanie Linuksa x86_64 w **emulowanym** kontenerze amd64 na tym hoście nie dochodzi nawet do naszego kodu: `rustc` sam się wywala — `qemu: uncaught target signal 11 (Segmentation fault)`. Kontener arm64 chodzi natywnie, ale daje ELF **aarch64**, czyli inny produkt, nie zamiennik.
  - **`packaging/release.sh` sprawdza plik, nie kod wyjścia** — format przez `file`, podłoga 1 MiB, archiwum i suma SHA256. **Jego pierwszy test negatywny był fałszywą zielenią:** uszkodziłem zbudowaną binarkę i uruchomiłem pakowanie ponownie — cargo **przelinkowało** plik, więc asercja dwa razy oglądała naprawiony artefakt i dwa razy przeszła. Mutacja, którą build potrafi naprawić, nie jest mutacją. Kontrole żyją teraz w funkcjach, a `--selftest` karmi je spreparowanymi plikami, których nic nie naprawia: **7 przypadków, każdy odrzucony zgodnie z zamiarem**.
- **Haki lokalne wreszcie istnieją na tej maszynie — i udowodniono, że potrafią odmówić (`RH-006`).** Zmierzone rano: `.git/hooks` zawierał wyłącznie pliki `*.sample`, `core.hooksPath` pusty. README i `docs/security/index.md` opisywały „zamknięty na czerwono" skan sekretów przed commitem jako to, co stoi między sekretem a lustrem — a on **nie chodził na żadnym commicie w tym repozytorium**.
  - **`lefthook install` + linia, której żądał `.pre-commit-config.yaml`.** Jego własny nagłówek mówi wprost: dwaj menedżerowie haków piszą do tego samego pliku i wygrywa ten, który uruchomił `install` jako ostatni, więc lefthook ma zachować własność `.git/hooks`, a `pre-commit` ma być **wywoływany przez niego**. Dopóki tej linii nie było, dziesięć hooków z tamtego pliku nie chodziło **nigdzie**.
  - **Dowód, że hak odmawia, a nie tylko istnieje:** podstawiony token kształtu `glpat-<20 znaków>` → `leaks found: 1`, `git commit` kończy się **kodem 1**, czubek gałęzi się nie zmienia.
  - **Pierwsza próba tego dowodu była pułapką i dlatego jest tu zapisana.** Użyłem **przykładowego klucza AWS z dokumentacji** — reguły `gitleaks` mają go na liście dozwolonych. Wynik: `no leaks found`, commit **przeszedł**, i wyglądało to dokładnie jak zepsuty hak. Mutacja, która chybia, jest nie do odróżnienia od bramki, która nie działa (§5.9 poziom 2).
  - **Nowy etap `hooks` w `verify.sh` (19 etapów).** Pada, gdy **ta kopia robocza** nie ma haka lefthooka albo ma cudzego; na CI melduje niezmiennik jako **nieadekwatny** zamiast udawać, że go zmierzył — efemeryczny checkout CI nie ma dewelopera do chronienia. Cztery kontrole zmierzone: hak schowany → FAIL z nazwą pliku; hak obcy → FAIL z nazwą wyścigu; `CI=true` → PASS jako nieadekwatny; przywrócony → PASS.
- **Sześć wpisów wróciło do sekcji, do której należą — i dostały bramkę, która to egzekwuje (kontrola 20).** Cała dzisiejsza praca (polityka poświadczeń, siedem repozytoriów, pokrycie testami i bezpieczeństwem, E-OS Guard, piętnaście decyzji, jedna roadmapa) stała pod `## [0.2.0] - 2026-08-22` — wydaniem otagowanym **dwanaście dni wcześniej**.
  - **Wszystkie inne bramki świeciły na zielono, bo żadna nie czytała nagłówków sekcji.** Markdown poprawny, CRLF nienaruszone, `docs-currency` porównuje różnicę, a same wpisy były **prawdziwe** — kłamała wyłącznie ich **pozycja**. Czytelnik pytający „co weszło w 0.2.0?" dostałby opis pracy, której wtedy jeszcze nie było.
  - **Reguła w jednym zdaniu:** pod `## [x.y.z] - RRRR-MM-DD` żaden wiersz nie może cytować daty **późniejszej** niż to wydanie. `## [Unreleased]` nie ma daty i nigdy nie jest ograniczone — i o to chodzi, bo to tam należy praca w toku.
  - **Kontrola przeciwna wykonana przed napisaniem reguły** (§5.9 poziom 4): na drzewie **sprzed** poprawki reguła daje **6 trafień z numerami wierszy**, na drzewie **po** — 0. Reguła, która czyta tak samo po obu stronach poprawki, niczego nie zmierzyła.
  - **Przeniesienie sprawdzone jako przeniesienie, nie jako przepisanie:** wielozbiór wierszy pliku przed i po jest **identyczny** (`lines only in old: 0`, `lines only in new: 0`), więc żaden znak nie zginął ani się nie zmienił — zmieniła się wyłącznie kolejność.
- **Jedna polityka poświadczeń dla pięciu ścieżek uwierzytelniania — biblioteka w forku `eos-userutils` (decyzje Q1, Q3, Q4, Q5).** Cel `lib` o nazwie `eos_credpolicy`, wyłącznie `std`, **bez zależności** — bo ten sam kod musi kompilować się dla hosta (instalator jest narzędziem hosta) i dla `*-unknown-redox` (greeter nie jest). Reguły: próg hasła **12 znaków**, PIN **6 cyfr**, blocklista wysyłana z biblioteką, PIN **wyłącznie jako odblokowanie ekranu** — nigdy `sudo`, `passwd` ani szyfrowanie dysku — a licznik prób to plik per konto, który root może skasować.
  - **Ocena siły liczona z prawdziwego kosztu.** `assess_password` zwraca wynik, entropię i czas łamania przy szybkości zgadywania **podanej przez wywołującego** — a wywołujący podaje **zmierzony** koszt haszowania: 14–15 ms na hasz argon2id na hoście referencyjnym, czyli około 70 prób na sekundę na rdzeń. Zwraca **klucz i18n**, nigdy gotowego zdania.
  - **Celowo niewpięta w żadne wywołanie.** Wpięcie zmienia to, co akceptuje `passwd`, więc jest osobną zmianą z własnym boot-smoke — a jedna z jej konsekwencji jest już znana: harness instalacyjny ustawia hasło `eos`, trzy znaki, więc próg i harness muszą ruszyć w **tym samym** MR, inaczej `R-601c` świeci na czerwono.
  - **Testy musiały mieć gdzie chodzić, i to wymagało pomiaru.** `cargo test --lib` w tym pakiecie pada na `libredox`, który nie buduje się na macOS — bez związku z tym kodem. Pierwsze podejście, włączenie modułu przez `#[path]` z pliku opakowującego, **nie działa**: moduły używają ścieżek `crate::`, a pod opakowaniem `crate::` wskazuje korzeń opakowania. Dlatego `credpolicy-hostcheck` kieruje swój cel `lib` **wprost** na `../src/credpolicy/lib.rs` — wtedy każde `crate::` znaczy to, co mówi. Ten sam wzorzec i ten sam powód co `hostcheck/` w forku `eos-users`. Wynik: **119 testów jednostkowych + 6 doc-testów**, wszystkie zielone.
  - **Dwie rzeczy poszły źle i obie są tu zapisane, nie wygładzone.** Pierwsza: pierwotny commit dodawał `[lib] name = "eos_credpolicy"` — a pakiet ma **dokładnie jedną** bibliotekę, więc moja **wyparła** istniejącą `userutils` i pięć binarek przestało się kompilować z `unresolved import userutils`. Złapała to budowa, nie przegląd kodu; dlatego credpolicy jest teraz `pub mod credpolicy` w `src/lib.rs`, czyli ta sama decyzja wyrażona tak, jak pozwala cargo. Druga, poważniejsza: poprawkę wprowadzono **przez `--force-push`** na opublikowaną gałąź `eos-july`, co `CLAUDE.md` §5.7 zakazuje **bez wyjątku**. Właściwym ruchem był kolejny commit. Nadpisany `cf7fc00` nie zginął (jest w klonie i w reflogu), ale był zepsuty; czubek to `28c6923`. Zapisane, bo reguła istnieje po to, żeby historia opublikowana była niezmienna, a nie po to, żeby łamać ją, gdy wygodnie.
  - **Analiza ryzyka i plan wycofania** (§5.6 — to dotyka granicy uprawnień): ryzyko dla zachowania **zerowe**, bo nic tego nie woła; obraz zyskuje bibliotekę, w którą nikt nie wchodzi. Ryzyko, które to **usuwa**, to stan dzisiejszy — pięć ścieżek uwierzytelniania, z których każda sama decyduje, co jest dobrym hasłem. Wycofanie: cofnij commit, przywróć poprzedni pin w `repos.toml` i `recipes/core/userutils`, przebuduj. Żaden stan na dysku się nie zmienia, więc nie ma czego migrować z powrotem.
- **Siedem repozytoriów utworzonych, zapełnionych i zarejestrowanych (decyzja Q13) — plus RFC hipernadzorcy do upstreamu (Q11).** Manifest ma **37** pozycji zamiast 30; `CLAUDE.md` §11 i `repos.toml` zmieniają się w tym samym commicie, bo kontrola 7 porównuje jedno z drugim i inaczej świeci na czerwono.
  - **`eos-users`** — fork `redox_users` z domyślnym argon2id, gałąź `eos`, **podpisany tag `v0.4.6-eos.1`** na obu hostach. To jest wydanie z decyzji Q2: nie czekamy na upstream, robimy własne. Tag wskazuje `da42b4b`, czyli stan **po** poprawce — nie pierwszą wersję, której zestaw testów pozostawał zielony z cofniętą zmianą. Zmierzone po naprawie: przywrócenie `Argon2i` **zabija 2 z 13** testów uruchamialnych na hoście, przywrócenie `Argon2id` — wszystkie przechodzą.
  - **`eos-sheets`, `eos-slides`, `eos-drive`, `eos-store`** — z jednego generatora, żeby nie rozjechały się w pierwszym tygodniu. Każdy: 19 plików, 6 testów, `fmt`/`clippy -D warnings`/`build`/`test` czysto, `--selftest` drukujący swój znacznik, backend okienkowy hosta za cechą `host-backend` (bez niej `--check-backend` kończy się **kodem 2** i mówi dlaczego, z nią — zerem). Generator ma własny selftest: **38 przypadków**, w tym 18 negatywnych. **Nie są jeszcze w żadnym obrazie** — to szkielety, a wysłanie okna ze statusem byłoby wysłaniem obietnicy.
  - **`eos-website`** — 12 stron w `pl` i `en`, i18n od pierwszego commita, strona changelogu **generowana** z `CHANGELOG.md` tego repozytorium, więc nie może się rozjechać. Pięć bramek plus `negative-tests.sh`, który psuje każdą z nich celowo w kopii drzewa i sprawdza **kod wyjścia oraz komunikat**.
  - **`eos-support`** — tickety jako issues z szablonami i zestawem etykiet; strona wsparcia buduje link do nowego zgłoszenia po stronie klienta. Statyczny host nie ma backendu i strona **mówi to wprost**, zamiast sugerować, że ma.
  - **`docs/rfc/0001-hypervisor-in-redox.md`** — 1489 linii, zmierzone wobec `redox-os/kernel` `f4d59db5`. **Szkic, niewysłany**: wysłanie go do upstreamu jest działaniem człowieka, nie narzędzia.
  - **Bramka złapała realną wadę przy rejestracji:** cztery nowe receptury nie miały reguły `fsrule = "source"` w `cookbook.lock`, więc cookbook **pobierałby je jako binarki upstreamu** — których nie ma, bo to nasze produkty. Naprawione narzędziem (`eos-source-rules.sh --apply`), nie ręczną edycją pliku generowanego (§5.7). `pins --strict`: **ok=30, drift=0**.
- **Pokrycie testami i pokrycie bezpieczeństwa sprawdzane automatycznie, z podłogami, które potrafią paść (`TQ-001`, `TQ-002`).** Dwa nowe etapy `verify.sh` — **18 zamiast 16** — i dwie generowane strony, `docs/reference/coverage.md` oraz `docs/reference/security-coverage.md`, bo liczba, której nikt nie umie odszukać, to liczba, która się rozjeżdża: README i `CLAUDE.md` cytowały pokrycie wpisane ręcznie miesiące wcześniej. Podłogi mieszkają w `coverage-floors.toml` i `security-coverage.toml`, **nie w skryptach** (§5.10 reguła 1): podniesienie progu jest zwykłym commitem, obniżenie widać w diffie.
  - **Pokrycie bezpieczeństwa jako pięć mierzalnych wielkości**, bo procent linii mówi, które linie się **wykonały**, a nie czy sprawdzono te **groźne**: `SC-1` bloki `unsafe` z komentarzem `SAFETY:`, `SC-2` parsery z celem fuzzingu, `SC-3` własne crate'y objęte `cargo-deny` i `osv-scanner`, `SC-4` wejścia z testem wrogiej wartości, `SC-5` wynik mutacji w kodzie zaufania. Zmierzone dziś: SC-1 100% (0 z 0 — E-OS-owy Rust nie ma `unsafe`), SC-3 100% (1 z 1), SC-2 i SC-5 **SKIPPED**, bo `cargo-fuzz` i `cargo-mutants` nie są zainstalowane — nigdy ciche 0%, nigdy ciche 100%.
  - **Dwie wady znalezione we własnych skryptach, zanim trafiły do repozytorium.** Pierwsza: raport pokrycia czytał z tabeli tekstowej „pierwsze pole kończące się procentem" — a to **regiony** (38,12%), nie **linie** (41,06%), podczas gdy podłoga 38 jest podłogą linii. Bramka przechodziła **o trzy setne punktu**, porównując nie tę liczbę. Teraz czyta z JSON-a, gdzie każda liczba ma nazwę. Druga: `SC-4` przy **zerze zadeklarowanych wejść** raportował 100% i przechodził podłogę 50 — prawda pusta spełnia każdy próg. To jest dokładnie ta „bramka, która potrafi tylko przejść", przed którą ostrzega §5.4; teraz brak manifestu to **SKIPPED** i kod 2, nie zaliczenie.
  - **Testy negatywne i mutacyjne, zmierzone:** `--selftest` obu skryptów; podniesienie podłogi pokrycia do 60 → **kod 1** z „BELOW THE FLOOR", po przywróceniu → 0; podłoga `SC-4` = 50 przy braku manifestu → **SKIPPED**, a bez `--allow-missing` **kod 2**; podłożony blok `unsafe` bez `SAFETY:` → `SC-1` na czerwono. Kod 2 ≠ kod 1: bramka, która nie mogła się wykonać, wskazuje przybornik, nie drzewo (`U-177`).
  - **Trzecia klasa P-16, tym razem w moim własnym skrypcie:** cel pokrycia lądował na dysku wewnętrznym i przy 126 MiB wolnego przebieg padał na `ENOSPC`, co wygląda identycznie jak przebieg, który niczego nie znalazł. Skrypt honoruje teraz `EOS_COV_TARGET` i **odmawia wcześnie**, nazywając system plików: „need 2048 MiB, have 19 MiB" (sprawdzone na celowo małym wolumenie).
- **E-OS Guard wraca do obrazu jako aplikacja (`PR-002`, decyzja właściciela Q12) — i po drodze `libonig` odblokował każdą budowę obrazu.** `[packages.eos-guard]` w obu konfiguracjach; `eos-sysmon` zostaje w archiwum, bo cała jego powierzchnia to zakładka Overview w `eos-control`, a dwie listy procesów to dwie odpowiedzi na jedno pytanie.
  - **Dowód w artefakcie, nie w kodzie wyjścia** (§5.3): zamontowana **kopia** obrazu aarch64 (P-6 — nigdy oryginał) pokazuje `/usr/bin/eos-guard` (aarch64 **14 635 056 B**, x86_64 **15 670 816 B**) i wpis pulpitu `/usr/share/ui/apps/40_eos-guard` obok `30_eos-notes` i `45_eos-control`; `boot-smoke` **PASS na obu architekturach**. Przyrząd sprawdzony przed odczytem: pierwsza próba montowania dała **puste** wyniki we wszystkich sondach, bo binarki `redoxfs` nie było pod zgadywaną ścieżką — wynik negatywny z niesprawnego narzędzia to nie wynik (§4.2). Poprawna ścieżka to `build/fstools/bin/redoxfs`.
  - **`strings` na obrazie znalazł „eos-guard" 12 razy i to nie był dowód** — dokładnie pułapka z §5.9 („`grep BOOTX64` trafiał w ciąg wewnątrz roota, nie w ESP"). Dopiero listing katalogu binarek rozstrzygnął.
  - **#26 rozszerzone i częściowo rozwiązane.** Zgłoszenie mówiło, że obraz **x86_64** nie składa się po przegotowaniu `cosmic-edit`, i że nie dowiedziono tego z dziewiczego drzewa. Zmierzone dziś: po usunięciu `repo.tag` (wymuszenie przegotowania, P-2) **aarch64 pada tak samo** — `Package PackageName("libonig") not found`. Przyczyna: `cosmic-edit/target/…/auto_deps.toml` (wygenerowany **2026-09-03 01:09**, czyli przed tą zmianą — kontrola przeciwna, §5.9 poziom 4) wymaga `libonig` i `libxkbcommon` jako zależności **uruchomieniowych**. `libxkbcommon` gotował się, bo jest zależnością **budowania**; `libonig` nie gotował się nigdy. Obie receptury **istnieją** w `recipes/wip/libs/other/` z komentarzem `#TODO: promote`. Po `make r.libonig` (pakiet pobrany jako gotowa binarka, bo `.config` ustawia `REPO_BINARY=1`) obraz złożył się na obu wyjściach: `harddrive.img` i `eos-0.2.0-aarch64-installer.img`, po 1400 MiB.
  - **Nowa pułapka hosta P-16: `mktemp -d` na macOS ignoruje `TMPDIR`.** Zmierzone: `TMPDIR=/Volumes/EOS-Podman/tmp bash -c 'mktemp -d'` → `/var/folders/…`. Skutek był kosztowny: `eos-esp-add-cert.sh` kopiuje obraz 1400 MiB do katalogu tymczasowego, trafił na **dysk wewnętrzny** z 578 MiB wolnego i 40-minutowa budowa padła na **ostatnim** kroku, po poprawnym złożeniu obu obrazów. §21.1 mówi „zmierz, który dysk boli" — ten krok nie mierzył żadnego.
  - **Sprzątanie po pomiarze, nie z przeczucia** (§21.1): dysk wewnętrzny miał **513 MiB** wolnego przy 96 % zajętości, zewnętrzny 1,2 TiB. Zajmowały go **moje własne** katalogi robocze: stare klony i katalogi `target` w scratchpadzie sesji. Usunięte po sprawdzeniu, że nie ma w nich pracy nigdzie niewypchniętej — trzy klony miały niezacommitowane zmiany albo lokalne scalenia, więc ich różnice zapisano jako łatki do archiwum właściciela **przed** usunięciem (§21.5). Odzyskane **11,5 GiB**; wszystkie usunięte katalogi odtwarza `git clone` albo `cargo build`.
- **Strona roadmapy — widok jednego planu, a nie druga roadmapa (`WS-003`, prośba właściciela „pamiętaj również o zaktualizowaniu strony roadmapy").** `docs/roadmap/index.html` leży **w drzewie mdBooka**, więc istniejące zadanie `pages` publikuje ją pod `/roadmap/` bez jednej linii zmiany w CI.
  - **Dlaczego to było ryzykowne i co je zdejmuje.** Właściciel poprosił o **jeden** plik planu i dostał go — osiem dokumentów scalono w `ROADMAP.md`. Wyrenderowana strona odtwarza dokładnie tę usterkę, którą scalenie usunęło: **drugie miejsce, w którym zapisany jest stan projektu**. Dlatego powstała kontrola 19 (`scripts/eos-check-roadmap-page.py`): osiem kafelków kamieni milowych **pokazuje** znaczniki z `ROADMAP.md` §3.4, a bramka porównuje je przy każdym `ci-integrity.sh`.
  - **Znacznik czytany z widocznego tekstu, nie z atrybutu** — §5.4 mówi, że bramka sprawdzająca *obecność* nie jest bramką. Ukryty atrybut mógłby zostać poprawny, gdy kafelek czytany przez człowieka się rozjedzie; tutaj sprawdzana wartość i czytana wartość to **te same znaki**.
  - **Cztery kontrole, wszystkie zmierzone, żadna założona.** Przestawienie znacznika M1 w planie → `rule 1: the M1 tile shows 🟡, ROADMAP.md section 3.4 says ✅`, exit 1. Przestawienie znacznika M2 na stronie → czerwono z nazwą M2. Zacytowanie `R-607z` → `rule 2`, z nazwą identyfikatora. Schowanie `ROADMAP.md` → **exit 2**, kod „przyrząd", nie „drzewo" (podział z `U-177`). `--selftest` domyka to sześcioma mutacjami, każda odrzucana przez tę regułę, którą łamie.
  - **Artefakt sprawdzony, nie kod wyjścia** (§5.3): `mdbook build` do katalogu poza repozytorium daje `book/roadmap/index.html` **bajt w bajt identyczny** ze źródłem (`cmp -s` → identical), z ośmioma kafelkami `data-roadmap`. Twierdzenie „strona się opublikuje" jest więc pomiarem, a nie założeniem o zachowaniu mdBooka.
  - **Przy okazji, jeden generowany plik przestał udawać źródło.** `mdbook-mermaid install .` zrzuca `mermaid.min.js` i `mermaid-init.js` do **korzenia** repozytorium — po lokalnej budowie książki wisiały nieśledzone w `git status`, czyli dokładnie w stanie, w którym ktoś porządkujący drzewo je commituje. Dopisane do `.gitignore`; sprawdzone przez ponowne wygenerowanie ich i pusty `git status`.
- **Harness testuje instalację szyfrowaną** (`R-601e`) — `EOS_SMOKE_FDE=1` instaluje na zaszyfrowany RedoxFS, a etap 2 dowodzi tego w trzech krokach, z których każdy może zawieść osobno: bootloader musi **zapytać** o hasło, **celowo błędne** hasło nie ma prawa odblokować dysku, i dopiero wtedy poprawne ma prawo dać `login:`. `EOS_SMOKE_FDE_NEGATIVE=1` zostaje jako powtarzalna kontrola negatywna — instaluje bez szyfrowania, przez co krok 1 musi paść komunikatem *„never asked for one (the boot-mode menu with no password asked for). It is not encrypted."*

- **Nośnik instalacyjny jest publikowany jako artefakt** (`R-601a`, #4) — oba obrazy kompresowane `zstd -3 -T0` i wystawiane obok sum kontrolnych i SBOM-u. Zmierzone przed wyborem poziomu: `-19` daje 130 MB w 98 s, `-3` daje 173 MB w **1,5 s**, a rozpakowanie jest bajt w bajt identyczne. W przebiegu nocnym artefakt waży **345 MB** przy limicie 1 GB. Sumy kontrolne zostają nad obrazami **surowymi** — weryfikuje się plik, który się zapisuje na dysk, nie opakowanie transportowe.
- **`ci-integrity` check 15: żaden artefakt budowania ani cache nie może być śledzony** — dodane po tym, jak sam wcommitowałem dwa pliki `.pyc` do `main` w jednym dniu, przez `git add -A`, i **żadna bramka tego nie zauważyła**: cache nie jest sekretem, więc gitleaks go ignoruje, i nie jest usterką, więc reszta też. `CLAUDE.md` zabraniał tego od zawsze — zakaz był całą egzekucją.
- **`ci-integrity` check 14: żadne odwołanie `docs/*.md` nie może wskazywać na nieistniejący plik** (#17) — `lychee --offline` tego nie widzi, bo to wzmianki w backtickach, nie odsyłacze.
- **U-161** — R-601` drives the installer end to end for the first time ([`d022574c6`](https://gitlab.com/e-os/e-os/-/commit/d022574c6), `test(install): drive the installer end to end; find R-F19 (U-161)`)
- **U-169** — x86_64 zbudowany i zbootowany po raz pierwszy; typ repozytorium przestał być zdaniem w dokumencie, a stał się polem, które CI egzekwuje ([`dbee16183`](https://gitlab.com/e-os/e-os/-/commit/dbee16183), `feat(gates): enforce repo type from the manifest; first x86_64 build + boot (U-1`)
- **U-176** — R-601` UDOWODNIONE ([`8bd2c79d6`](https://gitlab.com/e-os/e-os/-/commit/8bd2c79d6), `feat(R-601): partition -> install -> reboot -> login is PROVEN (U-176)`)
- **U-198** — Pierwsze podpisane repozytorium istnieje i weryfikuje się ([`a4669ff7c`](https://gitlab.com/e-os/e-os/-/commit/a4669ff7c), `feat(R-008): first signed repository, verified both ways -- and a name collision`)
- **U-199** — R-701` przygotowane, ale świadomie NIEAKTYWNE ([`3b4c2e5a4`](https://gitlab.com/e-os/e-os/-/commit/3b4c2e5a4), `feat(R-701): the E-OS package source, shipped inert because it would point at a `)
- **U-208** — Cały łańcuch pod Secure Boot ([`50bd29522`](https://gitlab.com/e-os/e-os/-/commit/50bd29522), `feat(secureboot): the installed system boots under Secure Boot too -- full chain`)
- **U-209** — R-008` zrobione (pierwsza podpisana publikacja żyje), i naprawa dwóch rzeczy z sesji operatora ([`92f09fdc9`](https://gitlab.com/e-os/e-os/-/commit/92f09fdc9), `feat(build): R-008 published + a build path that works on this exFAT host (U-209`)
- **U-210** — R-701` wpięte ([`cf2e63e89`](https://gitlab.com/e-os/e-os/-/commit/cf2e63e89), `feat(R-701): wire the aarch64 image to the published signed repo; teach the gate`)
- **U-211** — Audyt wobec `rhboot/shim-review`, triage norm i SCALENIE `ROADMAP.md` do `ROADMAP-v2.md ([`a38061843`](https://gitlab.com/e-os/e-os/-/commit/a38061843), `feat(boot): verify kernel and initfs by signature; shim-review audit; merge road`)
- **U-212** — V2-MS02 ([`a38061843`](https://gitlab.com/e-os/e-os/-/commit/a38061843), `feat(boot): verify kernel and initfs by signature; shim-review audit; merge road`)
- **U-220** — Pomiar POSIX-a (`V2-STD01`) ([`f3eb2dfd9`](https://gitlab.com/e-os/e-os/-/commit/f3eb2dfd9), `test(posix): unblock the POSIX suite and record a tenth hang upstream does not l`)
- **U-221** — V2-STD01 ([`ca18ca4bd`](https://gitlab.com/e-os/e-os/-/commit/ca18ca4bd), `test(posix): the first POSIX numbers this project has ever had, and three hangs `)
- **U-222** — V2-STD01` DOMKNIĘTE ([`308cd2402`](https://gitlab.com/e-os/e-os/-/commit/308cd2402), `test(posix): full POSIX.1-2024 result, 4267/5650, and a correction to the partia`)
- **U-228** — `R-611b`: `scripts/make-release.sh` pakuje nośnik instalacyjny obok `harddrive.img`, jego sha256 idzie do tego samego `SHA256SUMS`, więc obejmuje go podpis minisign. Do dziś pętla brała wyłącznie `harddrive.img`, a brak nośnika był **ciszą**, nie błędem. Nazwa pochodzi z `mk/config.mk` przez `make print-installer-medium`, żeby wzorzec miał jeden dom. Po drodze zmierzone i zamknięte dwie dalsze usterki: ścieżka `build/$arch/eos/` była zaszyta na sztywno, a `.config` ustawia `CONFIG_NAME` przez `?=`, więc `CONFIG_NAME=desktop` zmieszałby dwie konfiguracje w jednym `SHA256SUMS`; oraz `VERSION` domyślnie `0.1.0`, gdy build stempluje `EOS_VERSION=0.2.0` w nazwę nośnika — teraz domyślnie `make print-eos-version`, a niezgodność jest odmową, nie przemianowaniem. Testy: `scripts/eos-test-make-release.sh`, 7 przypadków (4 odmowy), wpięte do `verify.sh` jako etap `release-pack` (15 → **16 etapów**). Kontrola przeciwna na kodzie sprzed zmiany: **5 padło, 2 przeszły**. Mutacja obnażyła wadę w samym teście — po usunięciu kontroli istnienia nośnika 7/7 nadal przechodziło, bo test mierzył awarię `cp`, nie moją bramkę; po zaostrzeniu asercji ta sama mutacja zabija dokładnie jeden, właściwy test ([`97982bdef`](https://gitlab.com/e-os/e-os/-/commit/97982bdef), `feat(R-611b): make-release.sh pakuje nośnik instalacyjny obok obrazu dysku`)
- **U-230** — `R-611c`: nośnik instalacyjny wiezie `EFI/EOS/eos-secureboot.der` i `EFI/EOS/README.txt`. E-OS podpisuje własny bootloader **własnym** kluczem, więc na obcym pececie z Secure Bootem potrzebny jest jeden krok właściciela — wgranie certyfikatu do firmware. Trzymanie go gdzie indziej zawodzi dokładnie wtedy, gdy jest potrzebny: człowiek stoi przed menu firmware z pendrivem i maszyną, która nie startuje. Format **DER**, bo taki przyjmują menu UEFI. Offset ESP **odczytywany z GPT**, nie zakładany na 1 MiB (`R-609` planuje przepartycjonowanie). Jeden skrypt `scripts/eos-esp-add-cert.sh`, wołany przez `eos-build.sh` **i** oba zadania `build-image` — nie dwie kopie. Kontrola wymagana przez zadanie działa **wewnątrz** skryptu, na plikach wyjętych z ESP: certyfikat weryfikuje `BOOTX64.EFI` (x86_64) i `BOOTAA64.EFI` (aarch64), a świeżo wygenerowany obcy certyfikat jest **odmawiany**. Mutacja: podanie obcego certyfikatu jako wysyłanego → `FAIL -- the shipped certificate does NOT verify the shipped bootloader`, kod 1. Zmierzone: 813 B + 1535 B, 788 480 B wolne na 1 MiB ESP ([`148474bed`](https://gitlab.com/e-os/e-os/-/commit/148474bed), `feat(R-611c): nośnik wiezie certyfikat Secure Boot i instrukcję jego wgrania`)

### Changed
- **Polityka haseł wpięta w `passwd` — i udowodniona w obie strony (`R-602a`, `R-602b`).** Biblioteka istniała od rana i nie była wpięta w nic. Pin forka `e839cfa`.
  - **Dowód pozytywny:** `install-smoke: PASS — installed to a second disk and booted it to a login prompt` na aarch64, z 17-znakowym hasłem, przez pełny przebieg OOBE → instalacja (`fast install`, 1661 s pod TCG) → restart → `eos login:`.
  - **Dowód negatywny — ten, który czyni z tego bramkę** (§5.4): `EOS_SMOKE_PASSWORD=eos` (trzy znaki) → `install-smoke: FAIL — passwd refused this password (credential policy)`, **w 90 sekund**, z nazwaną przyczyną i wskazówką, jak wybrać hasło.
  - **Podłoga stoi tam, gdzie sekret jest WYBIERANY, nie okazywany.** `login` świadomie nietknięty: podłoga długości zastosowana przy weryfikacji **zablokowałaby każde konto założone wcześniej**. To rozróżnienie jest powodem, dla którego to jeden commit, a nie pięć.
  - **Decyzja i jej treść mieszkają w bibliotece, i to nie jest sprzątanie:** binarek tego crate'a **nie da się zbudować na hoście** (`libredox` nie ma celu macOS — `cargo check` pada na `libc::O_PATH` i czterdziestu stałych errno), więc kod w `src/bin/` jest dowodliwy wyłącznie przez budowę obrazu. `render_verdict` kładzie część wartą testowania tam, gdzie `credpolicy-hostcheck` ją uruchamia (**123** testy jednostkowe, było 119).
  - **Hasło harnessu wybrała polityka, nie ja.** Dodany przykład `judge` odpowiedział: `eos` → REJECT (za krótkie i na liście), `eos-smoke-harness` → ACCEPT, a `correct horse battery staple` → **REJECT**: dwadzieścia osiem znaków, odrzucone, bo lista blokad zna najsłynniejsze hasło świata. Sama podłoga długości by go przepuściła.
  - **Negatywny przebieg znalazł usterkę w moim własnym oprzyrządowaniu.** Pierwsze ramię harnessu wypatrywało odmowy **przy potwierdzeniu hasła** — a `passwd` pyta dwa razy, porównuje i **dopiero potem** ocenia. Odrzucone hasło pokazuje więc potwierdzenie jak każde inne i milknie po nim. Efekt: 30-minutowy timeout zamiast zdania. Ramię przeniesione tam, gdzie odmowa naprawdę się pojawia; nowy przebieg pada w 90 s.
  - **Artefakt przeczytany** (§5.3): binarka `passwd` dla `aarch64-unknown-redox` (1 443 920 B) niesie `co najmniej` ×3, `najczesciej uzywanych` ×1, `EOS_CREDPOLICY_ALLOW_WEAK` ×2. **Kontrola przeciwna** (§5.9 poziom 4): commit rodzic `28c6923` nie ma w źródle ani `enforce_policy`, ani `render_verdict`.
- **Piny siedmiu repozytoriów produktowych podbite do dzisiejszych czubków** — `pins --strict` pokazywał siedem wierszy DRIFT po scaleniach `PR-008` i przemiatki `RH-017`/`RH-018`. To nie jest sama księgowość: `eos-guard` i `eos-control` niosły **prawdziwe zmiany kodu**, nie tylko CI, a do obrazu wchodzą właśnie przez te piny.
  - **Zbudowane i zbootowane:** `eos-build.sh aarch64` → `Done.`, `esp-cert: PASS` wraz z kontrolą negatywną, `boot-smoke: PASS — reached userspace login`.
  - **Ale tylko TRZY z siedmiu produktów faktycznie się przebudowały** (`PR-016`). Po wyczyszczeniu wszystkich siedmiu drzew receptur i przebudowie `source/` mają wyłącznie `eos-guard` (5e1d574), `eos-notes` (53a31ba) i `eos-control` (5857f2e). Pozostałe cztery — `eos-sheets`, `eos-slides`, `eos-drive`, `eos-store` — **nie zostały nawet pobrane**.
  - **Powód, zmierzony a nie zgadnięty:** nie ma ich w **żadnej** konfiguracji obrazu (`grep packages.eos-sheets config/*/eos.toml` → 0 trafień w obu). Mają repozytoria, receptury, pakowanie per-system i działające CI — i nie trafiają do obrazu. Ich piny są więc **podbite, ale nieprzećwiczone**: żadna budowa nigdy nie skompilowała ich pod Redoksa.
  - **To jest luka wobec prośby właściciela** („programy te będą wbudowane w system z możliwością włączenia lub wyłączenia przy instalacji”), zapisana jako `PR-016`. Dopisanie linii do konfiguracji jest łatwe; **prawdziwą pracą będzie sprawdzenie, czy te aplikacje Slinta w ogóle kompilują się na `*-unknown-redox`** — dotąd budowano je wyłącznie na cele hostowe.
- **Trzy listy „definicji ukończenia”, które się nie zgadzały — i szablon MR-a, który kłamał, że jest jedną z nich (`RH-011`, kontrola 23).** Zmierzone: `CLAUDE.md` §6 — **18** pozycji, `CONTRIBUTING.md` — inne **14**, szablon merge requesta — **8**, a jego nagłówek twierdził, że **jest** „definicją ukończenia z CLAUDE.md”.
  - **Drugie fałszywe twierdzenie w tym samym pliku:** nagłówek weryfikacji cytował „trzy bramki (patrz CLAUDE.md)”, a **tego pojęcia kontrakt nigdy nie zawierał**. Zastąpione tym, co §5.2 faktycznie mówi.
  - **§6 jest teraz jedyną pełną listą.** Pozostałe dwa dokumenty mówią, czym naprawdę są — `CONTRIBUTING.md` podaje **polecenia**, szablon **kształt opisu** — i oba na §6 wskazują. Czytelnik, który odhacza listę ośmiu pozycji, wierzy, że skończył; to jest ten koszt.
  - **Bramka świadomie NIE porównuje treści pozycji:** kontrakt jest po polsku, `CONTRIBUTING.md` po angielsku, więc diff tekstowy byłby szumem — a bramka, która krzyczy bez powodu, zostaje obchodzona. Wymusza to, żeby każda kopia mówiła, **gdzie mieszka cała lista**.
  - **Bramkę trzeba było naprawić dwa razy i druga wersja jest lekcją.** Pierwsza reguła odpalała tylko na dokumentach z dosłownym nagłówkiem *Definition of Done* — a `CONTRIBUTING.md` trzyma listę pod *Pull / merge request checklist*, więc **usunięcie jego kotwicy przeszło na zielono**. Reguła oparta na nagłówku sprawdza nagłówek, nie rzecz (§5.4). Wymóg kotwicy jest teraz bezwarunkowy, a ten przypadek jest przypadkiem `--selftest`.
  - **Przy okazji złapana świeża nieaktualność, którą sam wprowadziłem godzinę wcześniej:** `CLAUDE.md` §3 i `CONTRIBUTING.md` kazały uruchamiać `shellcheck -f gcc $(git ls-files 'scripts/*.sh')` — wąskie polecenie, które zastąpiła zmiana `RH-014`.
- **`RH-016` zamknięte — a jego pierwotna liczba była błędna i to zostaje widoczne.** Wpis mówił „16 z 60 plików powłoki nie ustawia nic, 36 nie ma `-e`”.
  - **Przyrząd sprawdzony przed uwierzeniem w wynik** (§20.6): pierwsze podejście skanowało **okno 25 linii** i zgłosiło, że `verify.sh` **sam** nie ustawia nic — co jest nieprawdą, bo `set -` stoi tam za długim nagłówkiem. Po przeskanowaniu **całego pliku** liczby są inne.
  - **Podział według autorstwa, i to on rozstrzyga:** kod **własny E-OS** to **15 plików z `set -euo pipefail`, 15 z `set -uo pipefail`, ZERO bez niczego**. Każdy plik z alarmującej liczby był **vendorowanym pomocnikiem cookbooka**.
  - **Działanie na pierwotnej liczbie byłoby gorsze niż usterka:** oznaczałoby edycję plików upstreamu i niesienie tej dywergencji przy **każdej** synchronizacji (`ADR-0003`).
  - **Bramka wymaga `-u` i `pipefail`, a `-e` świadomie NIE.** Piętnaście własnych skryptów pomija `-e`, bo są bramkami i harnessami, które **badają kody wyjścia** — `ci-integrity.sh` zbiera porażki zamiast umrzeć na pierwszej, `ci-boot-smoke.sh` czyta status `timeout`. Wymaganie `-e` zepsułoby dokładnie te skrypty, których zadaniem jest **iść dalej i zameldować**.
  - **Kontrole:** `--selftest` 11 przypadków (sześć na ustawieniach, pięć na regule własności — bo połowa tej bramki to rozstrzygnięcie, czyj jest skrypt); mutacja na prawdziwym drzewie (usunięcie `pipefail` z `eos-check-assets.sh`) → **exit 1** z nazwą pliku i powodem; uruchomienie poza drzewem gita → **exit 2**, nie 1.
- **14 plików powłoki nie było objętych żadnym linterem — i to nie było widać, bo zakres był literalnym globem (`RH-014`).** `shellcheck` chodził po `scripts/*.sh`, więc poza nim zostały m.in. `scripts/hooks/pre-push` (hak skanu sekretów), trzy `examples/*.sh`, które czytelnik jest zapraszany uruchomić, i `upstream/prepare-mrs.sh`, który **kasuje katalogi**.
  - **Zmierzone na pięciu plikach własnych E-OS: 0 błędów, 1 ostrzeżenie** — i tym ostrzeżeniem było `SC2115` na `rm -rf "$WORKDIR/$repo"`. Puste `$repo` zamienia to w `rm -rf "$WORKDIR/"`, czyli kasuje **cały** katalog roboczy. Poprawione na `"${WORKDIR:?}/${repo:?}"`. Poszerzenie bramki nic nie kosztowało i coś znalazło — to zwykle ten kształt.
  - **Zakres jest teraz liczony, nie spisany:** wywodzi się z `git ls-files`, więc nowy skrypt jest lintowany **w dniu, w którym trafia do repo** (dziś 60 plików). Spisana lista starzeje się w ciszy; policzona nie.
  - **Dziewięć plików vendorowanych jest wyłączonych z nazwy i z uzasadnieniem** (`build.sh`, `native_bootstrap.sh`, `podman_bootstrap.sh`, `podman/rustinstall.sh`, pięć `bin/*-pkg-config`). Niosą razem **15 błędów `SC2068`**, ale `ADR-0003` mówi, że kod vendorowany zachowuje formę upstreamu: „naprawa" kupiłaby styl za cenę dywergencji niesionej przy **każdej** przyszłej synchronizacji.
  - **`recipes/**` i `src/**` też wyłączone:** fragmenty receptur cookbooka są **sourcowane**, nie wykonywane, więc z definicji nie mają shebanga — pierwsze poszerzenie zakresu dało z nich 4 błędy `SC2148`, które nie mówią nic o drzewie. Wyłączenie z pomiarem zapisanym w kodzie, nie z przeczucia.
- **Kontrakt cytował 14 własnych sekcji, których nie ma — i sam nie miał bramki, która by to widziała (`RH-010`, kontrola 21).** `CLAUDE.md` jest dokumentem, który cytuje każdy inny dokument i każdy MR; jego własne odsyłacze nigdy nie były sprawdzane.
  - **Zmierzone przyrządem, nie okiem:** `§4.1`, `§4.2` ×3, `§4.3` ×2, `§4.5`, `§10.1`, `§10.2`, `§10.3` ×2, `§1.6` ×2, `§11.3` — same pozostałości po starszej numeracji. Dodatkowo `## 19. TODO` stało **fizycznie po** `## 21`.
  - **Cztery odsyłacze tylko wyglądały na zepsute:** to odwołania **międzydokumentowe** do `ROADMAP`. Bramka uznaje je za poprawne, gdy są **zakwalifikowane** nazwą dokumentu — bo problemem nie jest odwołanie do innego pliku, tylko zostawienie czytelnikowi zgadywania, o który plik chodzi.
  - **Trzy `§2 reguła 4` rozwiązywały się, a wskazywały sekcję bez żadnych numerowanych reguł.** Przepięte na `§18 pkt 6` — regułę, którą naprawdę miały na myśli. Tej klasy bramka **nie widzi**; znalazło ją czytanie.
  - **Dlaczego to nie jest kosmetyka:** kontrakt, który cytuje sam siebie błędnie, uczy czytelnika przestać chodzić za cytatami. To gorsze niż brak cytatów.
  - **Bramka ma trzy reguły i pięcioprzypadkowy `--selftest`**, a na prawdziwym drzewie trzy mutacje (zły `§`, zły `ROADMAP §`, sekcja poza kolejnością) dają **exit 1**, ukrycie `ROADMAP.md` — **exit 2**, nie 1.
- **Poprawka do poprawki: strażnik `workflow:` nie działał **wcale**, a moje **dwa** twierdzenia o nim były błędne.** Pierwsza reguła — własny szablon GitLaba, `$CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUEST_IID` → `when: never` — **nie zdeduplikowała niczego**.
  - **Zmierzone dwa razy, z regułą już scaloną, na gałęzi, która już miała otwarty merge request:** powstały oba potoki — `2817226692` (`push`, **3 zadania**, więc strażnik go przepuścił) i `2817226740` (`merge_request_event`). Zmienna `$CI_OPEN_MERGE_REQUEST_IID` po prostu nie jest tu wypełniana w potokach gałęziowych, więc ramię `never` nigdy nie pasowało.
  - **Moje drugie twierdzenie — „działa po utworzeniu merge requesta" — obaliło samo siebie:** commit, który je niósł, poszedł na gałąź z otwartym MR-em i **też** utworzył dwa potoki. Napisałem wniosek przed pomiarem i pomiar go przewrócił.
  - **Reguła mówi teraz to, co znaczy:** potoki gałęziowe biegną **wyłącznie na gałęzi domyślnej**; wszystko inne dostaje swój potok merge requesta i nic więcej. Koszt wypowiedziany wprost: gałąź wypchnięta **bez** merge requesta nie dostaje żadnego potoku — co w tym projekcie i tak jest normalną ścieżką (§5.7).
  - **Wniosek, który zostaje w kontrakcie:** reguła, której zachowanie zależy od zmiennej, jakiej nie umiem u siebie zaobserwować, jest hipotezą, nie bramką — nawet jeśli pochodzi z dokumentacji dostawcy.
  - **Potwierdzone pomiarem, po przebiegu a nie przed nim:** push niosący nową regułę utworzył **dokładnie jeden** potok — `2817238547 merge_request_event`, bez potoku `push` obok — wobec **dwóch** przy każdym z trzech pushy wcześniej.
- **`PR-008` wysłane we wszystkich siedmiu repozytoriach produktowych — dowód z logów CI, nie z lokalnego przebiegu.** `eos-sheets` zip 10 435 145 B / tar.gz 7 665 915 B · `eos-slides` 10 435 227 / 7 666 041 · `eos-drive` 10 435 980 / 7 665 925 · `eos-store` 10 435 452 / 7 666 055 · `eos-notes` 11 786 760 / 9 716 432 · `eos-guard` Linux 9 759 997 B · `eos-control` tylko Linux, zgodnie z projektem.
  - **Guard nie ma budowy na Windows świadomie, nie z braku czasu.** Jego audyt uprawnień czyta bity POSIX (setuid/setgid/world-writable) przez `std::os::unix::fs::PermissionsExt`, których Windows nie ma. `#[cfg(unix)]` nad audytem wysłałby coś, co nosi nazwę Guard i **nigdy nie może zgłosić znaleziska** — kontrolę, która potrafi tylko przejść (§5.4), sprzedaną jako produkt bezpieczeństwa. To decyzja właściciela, nie zadanie portowania.
  - **`eos-notes` wycofał nieaktualne twierdzenie z własnego manifestu.** Stało w nim, że *„backend winit Slinta nie kompiluje się już na współczesnym host rustc"* — i od dawna nie było to prawdą. Buduje się na oba cele, razem z wbudowanymi źródłami C `rusqlite`. Twierdzenie poprawione tam, gdzie stało (§2 reguła 4).
  - **Strona roadmapy zaktualizowana w tej samej zmianie** (§5.8): pasek liczb mówi teraz 7/7 produktów i 2 defekty upstreamu Slinta, a warstwa dowodowa dostała siedem wierszy — w tym mój własny błąd w `release.sh`, który kierował przez `zigbuild` **tylko Windows**, więc Linux spadał na linker Apple i padał po ~20 minutach poprawnej kompilacji. Znalazły to **cztery niezależne agenty**, każda po własnej straconej godzinie.
- **Siedem repozytoriów produktowych ma pliki CI i nie ma CI (`RH-018`) — zmierzone na ostatnim potoku `main` każdego z nich.** `eos-sheets`, `eos-slides`, `eos-drive`, `eos-store`: po **7 zadań zabitych przez `ci_quota_exceeded`**, a jedyne zadanie na własnym runnerze (`host-gui`) wraca jako **`skipped`**, bo stoi w etapie **po** tych, które padły. `eos-guard`, `eos-control`, `eos-ui`: po 2 zabite kwotą i **zero, które się wykonało**.
  - **Co to znaczy konkretnie:** każdy produkt niesie podłogę pokrycia, `clippy -D warnings` i zestaw testów, które **nigdy nie wykonały się na żadnym merge requeście**. Podłoga `--fail-under-lines 55` w `eos-sheets` nie została oceniona **ani razu**. To §13 zastosowane do siedmiu repozytoriów naraz — a przeoczone dlatego, że YAML wygląda kompletnie, a potoki wyglądają zajęte.
  - **Zadania `package-*` z `PR-008` są tam jedynymi bramkami, które chodzą**, i tylko dlatego, że dostały `tags: [eos-heavy]` **oraz** `needs: []`. Bez drugiego wracały jako `skipped` — zmierzone na trzech repozytoriach, zanim to poprawiono.
- **Lustra GitHuba repozytoriów produktowych nie są lustrami (`RH-019`).** `remote_mirrors` zwraca dla nich **`(none configured)`**, podczas gdy repozytorium orkiestrujące ma lustro włączone i działające. Wszystkie scalone dziś produkty były **za** swoimi kopiami na GitHubie, dopóki nie wypchnąłem ich ręcznie. `ADR-0001` mówi, że GitHub jest lustrem tylko do odczytu; lustro aktualizowane wtedy, gdy ktoś sobie przypomni, jest forkiem o mylącej nazwie.
  - **Konfiguracja to zadanie operatora, nie narzędzia:** wymaga PAT-a GitHuba w adresie lustra, a §19 mówi wprost, że token nie idzie do narzędzia, które loguje.
- **Jeden potok na zmianę, nie dwa (`RH-017`).** Zmierzone: push na gałąź z otwartym merge requestem tworzy potok `push` **oraz** potok `merge_request_event`, i **oba** uruchamiają te same zadania.
  - **Dlaczego to było niewidzialne, dopóki nie zabolało.** Na współdzielonych runnerach oba padają na kwocie, więc duplikat nic nie kosztował. Przestał być niewidzialny w chwili, gdy zadanie zaczęło robić prawdziwą pracę: w repozytoriach produktowych ta sama ~20-minutowa budowa krosowa szła **dwa razy na jeden push**, na runnerze z `concurrent = 1`. Duplikat nie marnował więc tylko czasu — **opóźniał każde zadanie za sobą**, w tym dwuminutowy `local-gates`, od którego zależy scalanie.
  - **Mechanizmem jest środkowa reguła**, nie sama obecność bloku `workflow:`: `$CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUEST_IID` → `when: never`. Bez niej ostatnia reguła dopasowuje wszystko i nic się nie deduplikuje — dokładnie ten kształt „bramki, która nie potrafi odmówić”, o którym mówi §5.4.
  - **Zakres świadomie ograniczony.** Poprawione w tym repozytorium; siedem repozytoriów produktowych dostanie te same trzy linie **po** scaleniu ich MR-ów `PR-008` — dorzucenie ich teraz utworzyłoby nowy potok i **przestawiło na koniec kolejki** budowy, które właśnie stoją w niej od kilkudziesięciu minut.
- **Nowa reguła w §5.9 poziom 4: mierz to polecenie, które wykona się w produkcji, nie to, które wpisałeś ręcznie** — kupiona czterema niezależnymi porażkami tego samego dnia (`PR-008`).
  - **Co się stało.** Ustaliłem ręcznie, że `cargo zigbuild --target x86_64-unknown-linux-gnu` buduje produkt na Linuksa — i to była **prawda**, z artefaktem: `ELF 64-bit LSB pie executable, x86-64 … stripped`, 17 001 800 B. Potem napisałem `packaging/release.sh`, w którym gałąź `case` kierowała do `zigbuild` **wyłącznie** `*windows-gnu`, a Linux spadał do zwykłego `cargo build --target`. Cel ogłosiłem za udowodniony. Nagłówek skryptu twierdził przy tym, że kros na Linuksa **nie działa** — co również kiedyś było prawdą i przestało nią być w chwili włączenia `fontconfig-dlopen`. Komentarz przeżył swój pomiar.
  - **Ile to kosztowało.** Cztery niezależne przebiegi (`eos-sheets`, `eos-drive`, `eos-store`, `eos-guard`) skompilowały **całość poprawnie** i padły dopiero **na linkowaniu**, po 20–21 minutach każdy: `ld: unknown options: --as-needed -Bstatic -Bdynamic --eh-frame-hdr -z --gc-sections`. Każdy z osobna postawił poprawną diagnozę i każdy nazwał brakującą linię routingu.
  - **Dlaczego pomiar był prawdziwy i mimo to fałszywy.** Zmierzone polecenie i polecenie skryptu były **różne**. „Zmierzyłem, że to się buduje” uzasadnia zdanie o **poleceniu**, nie o **skrypcie** — a to skrypt uruchamia CI i operator. Reguła brzmi teraz wprost: dowodem na „cel X się buduje” jest przebieg **tej ścieżki, którą pójdzie CI**, nie równoważnego polecenia z palca.
  - **Poprawka.** `release.sh` decyduje teraz po trójce (`needs_zig`: `*windows-gnu`, `*linux-gnu`, `*linux-musl`), a `--selftest` **obejmuje tę decyzję** — 13 przypadków zamiast 7, w tym „`apple-darwin` nie potrzebuje ziga” i „`redox` nie potrzebuje ziga”. Reguły tak taniej do sprawdzenia nie wolno sprawdzać czekaniem na budowę. Zmierzone po poprawce: `eos-sheets-0.1.0-x86_64-unknown-linux-gnu.tar.gz`, **7 666 611 B**, w **3 m 31 s**.
- **Strona projektu wreszcie się publikuje — runner `eos-heavy` udostępniony reszcie grupy (`WS-001`, `WS-005`).** `eos-website` miał **zero wdrożeń** Pages, choć repozytorium wyglądało zdrowo. Przyczyna: runner został utworzony jako runner **projektu** przypięty do `e-os/e-os`, więc każde zadanie z `tags: [eos-heavy]` w innym repozytorium siedziało w `stuck_pending_no_matching_runners` — a to jest potok **zawieszony**, nie czerwony, więc wygląda jak nic.
  - **Po udostępnieniu runnera cały potok przeszedł w jednym przebiegu:** `i18n`, `changelog`, `contrast`, `pipeline`, `gates-negative`, `pages` — wszystkie zielone, 15 s najdłuższe zadanie. Pages zapisało pierwsze wdrożenie **2026-09-03 11:14:52 UTC**.
  - **Artefakt przeczytany, nie tylko zadeklarowany** (§5.3): pobrany artefakt zadania `pages` ma **27 stron HTML**, `public/pl/index.html` niesie tytuł *„E-OS — utwardzona dystrybucja pochodna Redox OS"*, `public/en/index.html` angielski odpowiednik, strona produktów wymienia **wszystkie siedem** aplikacji, a strona pobierania niesie bramkę *„Tylko dla developerów"* (`developerOnly`).
  - **Dostęp do Pages zostaje prywatny.** Właściciel napisał, że stronę „na razie można hostować lokalnie, dopiero później się wrzuci na serwer" — upublicznienie jest **jego** przełącznikiem, nie moim. Zmierzone: `pages_access_level=private`, więc anonimowe żądanie dostaje przekierowanie do logowania; dowód treści pochodzi z artefaktu, nie z publicznego adresu, i tak jest to tu zapisane.
  - **Dlaczego to bezpieczne poszerzenie, a nie hurtowa zgoda:** runner ma `locked=false` (można dopiąć projekt bez ponownej rejestracji) i `run_untagged=false` (zadanie **bez** tagu nigdy tu nie trafi przypadkiem). Włączony dla `eos-website`, `eos-support`, `eos-ui` i siedmiu repozytoriów produktowych. Cofnięcie: `glab api -X DELETE projects/<id>/runners/54369740`. Koszt wypowiedziany wprost: CI tych repozytoriów wykonuje się teraz na laptopie właściciela — wszystkie są jego własne i nie mają współautorów z zewnątrz, a alternatywa była zmierzona: potok, który nie może się uruchomić w ogóle.
- **Strona dokumentacji znów się publikuje — zadanie `pages` zeszło na własny runner (`WS-005`, decyzja Q7).** Zmierzone **przed** zmianą: Pages ma dokładnie **jedno** wdrożenie, z **2026-09-01 16:31 UTC**, i nic po nim. Każdy późniejszy przebieg ginął w ~0 s na `ci_quota_exceeded`, a `allow_failure: true` trzymał to poza oznaczeniem potoku — więc opublikowana strona **zamarła dwa dni za `main`** i wyglądała dobrze.
  - **`allow_failure` zdjęte, `needs: []` zostaje.** Komentarz przy zadaniu tłumaczył `allow_failure` obawą, że porażka etapu `docs` pominie ciężki `build-image`. Przed pominięciem chroni **odczepienie od kolejności etapów**, a nie tolerowanie porażki — dokładnie tak, jak robi to `build-image`. Krok publikujący, który nie może zaczerwienić się, publikuje ciszę (§13, `U-140`).
  - **Zadanie sprawdza teraz swój artefakt, nie kod wyjścia** (§5.3). `mdbook build` na **pustej** książce kończy się **kodem 0** — zmierzone. Między zielonym oznaczeniem a opublikowaną pustą stroną stoi więc wyłącznie `test -s public/roadmap/index.html` i `grep data-roadmap="M1"`. Kontrola negatywna: podmiana `src` w `book.toml` na pusty katalog → `mdbook build` **0**, asercja **1**.
  - **Wersje przypięte do tych, którymi książkę faktycznie zbudowano** (mdBook 0.5.4, mdbook-mermaid 0.17.1), a nie do linuksowych tarballi 0.4.40/0.14.0: dla 0.4.40 mdBook **nie publikuje** archiwum `aarch64-apple-darwin` (zmierzone: HTTP 404), więc ścieżka „pobierz–zweryfikuj–rozpakuj" z `U-118` nie ma na tym hoście czego pobrać. `cargo install --locked` weryfikuje sumę z rejestru — ta sama gwarancja inną drogą.
- **Piętnaście decyzji właściciela z 2026-09-03 zapisanych i wykonanych tam, gdzie wykonanie było w zasięgu.** Odpowiedzi w `ROADMAP.md` §3.0.7 (tabela pytanie → odpowiedź → wiersz), wiersze `R-602a`…`g`, `PR-002/003`, `WS-001/005/009/010`, `CS-009/201`, `RH-003/008/009` z adnotacją *(decided 2026-09-03: …)*; platforma chmurowa nazywa się **E-Cloud** (§11.4); PIN wyłącznie jako odblokowanie ekranu, nigdy `sudo`/`passwd`/FDE; `redox_users` naprawiamy własnym forkiem `eos-users` i **własnym wydaniem**, nie czekaniem na upstream; biblioteka polityki haseł jako cel `lib` w forku `eos-userutils`, próg 12 znaków / PIN 6 cyfr, licznik prób w pliku per konto (root może go skasować); strona w osobnym repozytorium na GitLab Pages (job `pages` na runnerze `eos-heavy`); **tickety od razu** jako issues w `eos-support`; czat AI na **własnym modelu** — na razie projekt; hipernadzorca — **RFC do upstreamu** przed pierwszą linią kodu.
  - **Utworzone repozytoria** (Q13): `eos-sheets`, `eos-slides`, `eos-drive`, `eos-store`, `eos-website`, `eos-support`, `eos-users` w grupie `e-os` na GitLabie oraz sześć luster na GitHubie (bez `eos-support` — tickety nie mają lustra). Na razie **puste**; szkielety, piny w `repos.toml` i wiersze w `CLAUDE.md` §11 wchodzą razem z pierwszym pushem, żeby `pins --strict` miało co porównywać.
  - **Porządki wykonane** (Q14, Q15): biblioteka promptów wyprowadzona poza repozytorium; 18 niecytowanych zrzutów ekranu, trzy dokumenty bez ani jednego odwołania, katalog `patches/` i dwie jednorazowe sondy `R-F19` usunięte — **każdy plik najpierw skopiowany** do lokalnego archiwum właściciela (26 plików), zgodnie z §21.2; `EOS_BUILD_STATE.md` przeniesiony do `docs/archive/` z przepiętymi dwoma cytowaniami.
  - **Kontrola 17 zamyka się teraz na czerwono na sierotach.** Siedem plików marki (logo, tapeta, tło greetera, ikona) jest cytowanych z `docs/guides/screenshots.md`, więc `--warn-orphans` zniknęło z `ci-integrity.sh`: **45 zasobów, 0 sierot, 0 duplikatów**. Wcześniej reguła istniała tylko jako ostrzeżenie, bo 26 plików nie miało właściciela.
  - **`RH-008` domknięte inaczej, niż proponował Annex C.2** — i to jest korekta, nie wykonanie planu. Scalenie `docs/architecture/overview.md` do `ARCHITECTURE.md` **usunęłoby rozdział o wnętrzu systemu z książki**, bo mdBook renderuje wyłącznie `docs/`. Zamiast tego `ARCHITECTURE.md` deklaruje się punktem wejścia, a `overview.md` rozdziałem o wnętrzu; oba się do siebie odsyłają. Dwa dokumenty, jedna zadeklarowana kolejność.
  - **`docs/img/README.md` mówił nieprawdę o własnych plikach:** „~20× mniejsze". Zmierzone na 16 parach: **1,2–2,6×**, a `eos-bootloader.png` jest tam **6,2× większy** od „oryginału" i ma inne wymiary. Tekst poprawiony, plik do regeneracji.
  - Zweryfikowane: `doc-paths` 0 · `roadmap-check` 0 defektów, 0 ostrzeżeń · `summary-check` 0 · `assets-check` 0 sierot · `ci-integrity` 18 ok · `verify.sh` i zadanie `local-gates` — wynik w opisie MR.
- **Jedna roadmapa, porządek w repozytorium, trzy nowe bramki, reguła „każda zmiana ma swoją kontrolę" (2026-09-03).** Na polecenie właściciela („jeden szczegółowy plik jako plan i roadmapa, nie kilka; usuń z repo, co niepotrzebne; lista od najszybszych do najcięższych z tym, co każda zmiana dodaje"):
  - **`ROADMAP.md` jest jedynym planem** (3137 linii, 374 KB; było 2323 / 285 KB). Sześć archiwalnych planów scalono **w całości**, nie streszczono, jako §17–§21: `docs/archive/plan.md` → §17 (edycje, kompartmentalizacja, kolejność), `hardware-plan.md` → §18 (droga na sprzęt, tabela objawów §18.0.5), `roadmap-connectivity.md` → §19 (USB/LAN/Bluetooth, fazy B0–B5), `hardware-capabilities-roadmap.md` + `acpi-off-removal-plan.md` → §20 (zakres i nie-cele dostarczonych `R-50x`, `R-401f`), `feature-proposals.md` → §21 (tabela losów 14 propozycji: 3 ✅, 3 częściowo ✅, 6 → otwarte wiersze rejestru, 2 bez wiersza — doctor, crash-reporter). Pliki źródłowe i stub `ROADMAP-v2.md` usunięte; **48 cytowań w 23 plikach** (komentarze `config/*/eos.toml`, profile, ADR-0008/0011, `installer*.md`, `HARDWARE.md`, `SUMMARY.md`) przepisanych na nowe sekcje; zapisy datowane (`docs/audit/`, `docs/archive/reality-ledger.md`, ten plik) zostają nietknięte.
  - **§3.0 to teraz lista wszystkiego, co otwarte, w pięciu pasmach kosztu** (A: minuty–dzień … E: zablokowane nie pracą), każdy wiersz z tym, co dodaje/zmienia i co odblokowuje; 15 decyzji właściciela w §3.0.7.
  - **Nowe rejestry:** `PR-001`…`PR-014` (§7.5: produkty w obrazie, na Windows/Linux — **zmierzone:** `eos-ui`/`eos-notes`/`eos-control` przechodzą `cargo check` na `aarch64-apple-darwin`, rc 0, ale nie mają backendu okienkowego hosta; oba produkty przypinają `eos-ui` z lustra GitHub, nie z GitLaba — `PR-006`; 0 testów w trzech crate'ach; odpowiedź o antywirusie: **E-OS Guard**, nie „antywirus", §7.5.3; cztery nowe produkty `eos-sheets`/`eos-slides`/`eos-drive`/`eos-store` z nazwami repozytoriów do decyzji), `TQ-001`…`TQ-013` (§11.3: automatyczne pokrycie testami i **pokrycie bezpieczeństwa** jako pięć mierzalnych proxy SC-1…SC-5), `RH-001`…`RH-016` (§11.7: księga porządków).
  - **Trzy bramki `ci-integrity.sh` 16–18**, każda z testem negatywnym i każda czerwona na pierwszym uruchomieniu: `scripts/eos-check-roadmap.py` (kotwice, zdublowane numery sekcji, jeden status na ID, ✅ tylko z dowodem — 2 wiersze: `R-301`, `R-502b`, poprawione), `scripts/eos-check-assets.sh` (duplikaty bajtowe, > 5 MB, sieroty — 1 duplikat usunięty, 26 sierot 1,7 MB do decyzji, do tego czasu ostrzeżenie), `scripts/eos-check-summary.py` (14 stron `docs/` nigdy nie było w `SUMMARY.md` — dodane, w tym ADR-0007…0011 i cztery specyfikacje instalatora).
  - **`CLAUDE.md` §5.11** — tabela rodzaj zmiany → kontrola → polecenie → test negatywny (kod własny, vendorowany, config, mk, skrypty, CI yaml, dokumentacja, CHANGELOG, CLAUDE.md, klucze, zasoby, nowy produkt); §6 DoD ma odpowiedni punkt; §9 poprawione — od tygodni twierdziło, że `verify.sh` „jeszcze nie istnieje"; liczba kontroli 14 → 18.
  - **Zmierzone na hoście i zapisane:** żaden hak gita nie był zainstalowany (`.git/hooks` tylko `*.sample`, `core.hooksPath` pusty) — opisywany jako „zamknięty na czerwono" skan sekretów przed commitem nie chodził na tej maszynie (`RH-006`); 28 025 sidecarów `._*` i 5 `.DS_Store` w drzewie oraz 308 w `.git/objects` usunięte (0 śledzonych, `git status` identyczny przed i po).
  - **Skrypty, partia pierwsza:** 5 trafień `shellcheck -S warning` w skryptach własnych — trzy SC2054 to fałszywe alarmy (przecinek wewnątrz jednego argumentu QEMU) wyciszone z uzasadnieniem, `eos-secureboot-proof.sh` nieużywane `ARCH` usunięte, `eos-sync-buildtree.sh` `cd` z kodem 2 przy błędzie; 8 skryptów upstreamowych zostaje na formie upstreamu (ADR-0003).
  - **Skrypty, partia druga — sześć wad z inwentaryzacji (117 → 468 agentów; weryfikacja adwersaryjna padła na limicie sesji, więc każdą z tych sześciu sprawdzono ręcznie na cytowanej linii przed poprawką):** `sync-forks.sh` kończył się zawsze zerem — `CLONE_FAIL`, `NO_UPSTREAM_HEAD` i `push_fail` były tylko tekstem tabeli (teraz `rc=1` i podsumowanie); `mk/prefix.mk:308` wykonywał katalog `relibc-freestanding-install` jako polecenie zamiast go usunąć, przez co `exit 1` był nieosiągalny; `mk/repo.mk:388,401` `cd $(shell make find.$*)` — pod `PODMAN_BUILD=1` podstawienie było puste i `cd` szło do `$HOME` (teraz `$(MAKE) -s` ze strażnikiem); P-15 w `rx-proof-harness.sh` (`TIMEOUT`, `SETTLE`) i `eos-boot-verify-proof.sh` (`EOS_PROOF_TIMEOUT`) — wartości nienumeryczne w `$(( ))` kończyły dowód zerem (teraz `exit 2`); `eos-check-doc-paths.py` nie widział ścieżek zagnieżdżonych `docs/<katalog>/<plik>.md` — 69 odwołań w drzewie nigdy nie było sprawdzanych (regex z `/`); `mk/config.mk` mapuje `arm64` → `aarch64` obok istniejącego `i686` → `i586`, bo `uname -m` na tym hoście daje `arm64`, a `config/arm64/` nie istnieje.
  - **`.gitignore`:** negacja `!keys/**/*.pub` (linia 95) ponownie wpuszczała sidecary `keys/._eos-release.pub` — zmierzone `git check-ignore -v --no-index`; strażnik `**/._*` dopisany **po** wszystkich negacjach, ten sam pomiar po poprawce daje „ignorowany".
  - **`TQ-011` — zadanie `local-gates` na runnerze `eos-heavy`** (`.gitlab-ci.yml`): zmierzone, że to jedyny runner, który cokolwiek wykonał od 2026-09-01 — wszystkie 10 zadań lekkiej warstwy padło na `ci_quota_exceeded`, a workflowy GitHuba na lustrze **nie chodzą** (`security.yml`: 0 uruchomień kiedykolwiek, `ci.yml` ostatnie 2026-06-15; 294 „runy" to Dependabot). Wcześniejszy szkic roadmapy twierdził, że GitHub wykonuje bramki — poprawione. Zadanie uruchamia `verify.sh` na każdym MR i na `main`, bez `| tee` (P-3), z logiem jako artefaktem; `glab ci lint` ✓.
  - **Roadmapa — siedem niespójności wewnętrznych zmierzonych przez inwentaryzację i poprawionych w scaleniu:** `S-9` „open" przy obu celach ✅ (`R-701a`, `V2-MS13`); `R-604a` z dwoma glifami statusu w jednej komórce; `R-601e` 🔴 z FDE na liście braków, choć §1.3 i §3.0 mówiły „FDE pokryte" (teraz 🟡 z dowodem z 2026-09-02); §1.2 „NetSurf jako PIE dostarczony" bez adnotacji o #28; `V2-MS07` w dwóch horyzontach (`M-8` i `L-3`) — zostaje `L-3`; `R-F30` cytowany dwa razy, nigdy nie wybity — wiersz dodany do §10; §15 z podwójnymi numerami 6/7/8 (moja wczorajsza wstawka) — przenumerowane 1–23; §0.3 wymieniało 5 rodzin identyfikatorów przy 11 używanych. `RH-010`…`RH-016` i `TQ-011`…`TQ-013` zapisują to, czego ta partia nie robi: numerację `CLAUDE.md` (§18 → §20 → §21 → §19) z 43 wiszącymi cytowaniami podsekcji, trzy rozjeżdżające się listy DoD, akapity kopiowane w 9 plikach, wspólne helpery shellowe, 29 ścieżek `/opt/homebrew`, brak lintera Pythona, `semgrep` skanujący 11 z 71 plików.
- **Synteza architekta i piąta partia weryfikacji (117 agentów, 111 werdyktów, 24 obalenia) — rejestr nowego zakresu doprecyzowany tam, gdzie pierwsze zapisy były za łagodne** — (1) `R-602g`: **podbicie wersji nie naprawia** #27, bo `redox_users 0.5.2` nadal przypina `rust-argon2 = "0.8"` i woła `Config::default()`; droga to fork `redox_users` jako 31. repozytorium typu C z `[patch.crates-io]` albo MR do upstreamu — decyzja właściciela (§3.0 Q2), rozmiar M nie S; stare hasze `$argon2id$` z obrazu nadal się weryfikują (`rust-argon2 0.8.3` parsuje ten wariant). (2) `R-602d`: `redox_users` śpi stałe **3 s** po nieudanej weryfikacji, per proces (`lib.rs:518,526`) — obchodzone równoległymi sesjami, nie blokada. (3) `R-602e`: harness sam ustawia hasło **`eos`** (3 znaki, `install-smoke-drive.py:27`) — każdy próg długości zaczerwieni `R-601c`, dopóki hasło harnessu nie zmieni się w tym samym MR. (4) §6.6: „brak blocklisty" doprecyzowane do „jeden literał `password` w dwóch miejscach bez wspólnego kodu" (`orblogin/main.rs:164-179`, `login.rs:195-243`). (5) `API-001`: lista schematów z drzewa jądra, `sys:env`/`sys:kstop` rejestrowane inline w `sys/mod.rs:81,97`. (6) `API-003`: zadania `rustdoc`/`pages` mają `allow_failure: true` i nie biegną od 2026-08-28. (7) Nowe wiersze `CS-009` (bramka „receptura z `wip/` buduje się" przed jakimkolwiek Tier 1) i `CS-010` (headless smoke dla edycji serwerowej); `CS-001` z kluczami hosta SSH (`R-606`) i filtrem pakietów (`R-904`). (8) `R-D06` ✅ → 🟡 wobec #28. (9) §15: trzy nowe pozycje do zweryfikowania z poleceniem (`/etc/shadow` po OOBE, ostatnia publikacja Pages, parametry `crypt(3)` w relibc). (10) §3.0: dwanaście decyzji właściciela zamiast dziesięciu. Ta sama wada co w README poprawiona w trzech kolejnych dokumentach: `SECURITY.md:125`, `ARCHITECTURE.md:188`, `docs/architecture/installer-wizard.md:554` — każdy twierdził argon2id dla haseł kont; raporty audytu z 2026-08-30 zostają jako zapisy datowane.
- **Czwarta partia zawężeń (111 werdyktów, 24 obalenia łącznie) — powierzchnia haseł jest szersza, niż zapisałem** — (1) `sudo.rs:22` ma `MAX_ATTEMPTS = 3`: jedyny limit prób w systemie, w procesie i zapominany przy następnym wywołaniu, więc nie blokada; `R-602d` mówi to wprost. (2) Ścieżek uwierzytelniania hasłem jest **pięć**, nie trzy: `passwd`, `login`, `orblogin`, demon sudo za `/scheme/sudo` (użytkowany przez `sudo` i `su`) oraz podnoszenie uprawnień w `eos-control` (`elevate.rs:27`, hasło ze stdin w `power.rs`/`netcfg.rs`) — `R-602f` poprawione. (3) `relibc` niesie **trzecią** implementację argon2 dla `crypt(3)` (`src/header/crypt/argon2.rs`, RustCrypto `argon2 0.5.3` przez `password-hash`); jej parametrów nie mierzyłem i tak jest zapisane — `R-602g` musi ją objąć albo powiedzieć, czemu nie. (4) `CS-001`: poza `config/server.toml` upstream ma `config/x86_64/server-demo.toml`, a obraz desktopowy **już** wciąga pakiety `server.toml` przez `desktop.toml:3`; nadal żaden obraz headless nie był tu budowany.
- **Trzecia partia zawężeń po weryfikacji adwersaryjnej (88 werdyktów, 18 obaleń łącznie) — dwie o dokumentacji API, jedna o piaskownicy, plus wiszące odwołanie w CI** — (1) §11.6/`API-003`: `eos-ui` na przypiętej rewizji **ma** `#![warn(missing_docs)]` (`lib.rs:19`), dokumentację crate'u z doctestem i każdy element publiczny udokumentowany — więc luką jest **publikacja** rustdoc i cztery aplikacje bez tego lintu, a nie „brak dokumentacji"; pierwsze przejście szukało tylko w plikach CI i Makefile, nigdy w źródłach. (2) `CS-101`: `recipes/core/contain/recipe.toml` to 5-wierszowy, **nieprzypięty** stub bez `source/` ani `target/` w drzewie; istnieje za to strona sesji — `config/desktop-contain.toml` (`pass_schemes`, `sandbox_schemes`) i obsługa `-C/--contain` w `getty.rs` forka `eos-userutils`. (3) `.gitlab-ci.yml:112` i `:120` odsyłały do „CLAUDE.md §3" po twardą regułę o doc-comments — §3 to „Polecenia", a reguły tam nie było; szablon MR (`Default.md:19`) twierdził, że powtarza listę DoD z CLAUDE.md, a lista tej pozycji nie miała. Odwołania poprawione, pozycja **Doc-comments** dodana do §6 Definicji ukończenia, żeby szablon mówił prawdę.
- **Trzy kolejne rozjazdy dokumentacji z obrazem, złapane przez adwersarzy i poprawione** — (1) `docs/architecture/ecosystem.md:60-61` i mermaid w `README.md:97` wymieniały `eos-guard` i `eos-sysmon` jako aplikacje natywne; konfiguracja nie wysyła żadnej jako pakietu, a baza blake3 guarda żyje jako zakładka Security w `eos-control` — oba miejsca mówią to teraz wprost i odsyłają do `PR-002`. (2) README *„Deliberately absent … choices, not omissions"* było prawdziwe dla antywirusa i MAC (ROADMAP §13 odrzuca oba), ale **nie** dla kopii zapasowych i VPN/Tor — roadmapa prowadzi je jako lukę (`L-4`, `CS-002`) i nowe podsystemy (`R-616c`, `CS-006`), nie jako wybór; zdanie doprecyzowane. (3) `PR-001` twierdziło „brak stron produktów"; istnieją trzy ręcznie pisane listy, które **już się rozjechały** z konfiguracją — i to jest właśnie argument za generowaniem ich z `config/*/eos.toml`. Do tego §11.6: w repozytorium jest nieśledzony host-build `eos-control` pod `build/`, a format `.sig` opisuje ADR-0004, nie tylko kod.
- **Trzy twierdzenia inwentaryzacji zawężone po weryfikacji adwersaryjnej, zanim trafiły do roadmapy** — pierwsze przejście napisało „brak jakiejkolwiek noty prawnej / polityki wsparcia / strony pobierania"; drugie, nastawione na obalenie, znalazło `NOTICE` z zastrzeżeniem znaku towarowego (`:33-35`), politykę wsparcia w `SECURITY.md:29-52` i `stability.md:37-45`, oraz publiczny, **niebramkowany** indeks pakietów na GitHub Pages i obrazy jako tygodniowe artefakty CI. §11.5 i `WS-004`/`WS-005` mówią o tym, co jest. Podobnie §7.5: `eos-guard` nie jest pakietem obrazu, ale jego baza blake3 została **przeniesiona do zakładki Security w `eos-control`** (`src/security/mod.rs:1-3`), więc „nie ma go w obrazie" wymagało doprecyzowania „jako aplikacji".
- **Roadmapa przyjęła nowy zakres — zapisany, wyceniony, nierozpoczęty** — na życzenie właściciela z 2026-09-02, z założeniem *„wszystko na Redox i E-OS, jeśli się da"*. Cztery nowe rejestry, każdy z inwentaryzacją tego, co istnieje (z plikiem i wierszem), co trzeba zbudować i co jest dziś niemożliwe: **§6.6** hasła i PIN (`R-602a`…`g` — wskaźnik siły liczony z **zmierzonego** kosztu haszowania 15,3 ms, blocklista, podpowiedź, PIN jako osobny slot z licznikiem prób; 4-cyfrowy PIN pada offline w 153 s, więc nigdy jako klucz FDE); **§11.4** edycja serwerowa i chmura na E-OS jako hoście (`CS-*`, trzy warstwy: buildowalne dziś / izolacja bez hipernadzorcy / maszyny wirtualne za hipernadzorcą w jądrze, którego **nie ma** — zmierzone na forku: zero VMCS/VMCB/`vmlaunch`/`vmrun`, tylko `hvc #0` do PSCI firmware'u); **§11.5** strona projektu (`WS-001`…`012`, dwie połowy: statyczna na E-OS z nginx, dynamiczna dopiero po bazie i poczcie na E-OS); **§11.6** udokumentowane API (`API-001`…`006` — dokumentować odziedziczoną powierzchnię teraz, stabilizować własną w 1.0); **§7.5** produkty w obrazie (`PR-*`: `eos-guard` i `eos-sysmon` istnieją jako repozytoria, ale **nie** są pakietami obrazu; antywirusa nie ma nigdzie). Do tego **§14.7** — czego to nie obiecuje — i dziesięć decyzji w §3.0, które może podjąć tylko właściciel.
- **`docs/guides/screenshots.md` mówi prawdę o obrazie** — podpisy pod `eos-guard` i `eos-sysmon` pokazywały je jako aplikacje natywne; konfiguracja nie wysyła żadnej z nich. Podpisy odsyłają teraz do `PR-002`, gdzie właściciel decyduje, czy trafiają do obrazu, czy znikają z listy produktów.

- **U-154** — R-F18` located to the second: an 84-second stall inside USB HID bring-up ([`881c23330`](https://gitlab.com/e-os/e-os/-/commit/881c23330), `docs(R-F18): locate the 84-second USB HID stall by wall clock (U-154)`)
- **U-155** — R-F18` measured to the root: a shared-INTx interrupt storm, and my `U-154` hypothesis was wrong ([`3449d8e80`](https://gitlab.com/e-os/e-os/-/commit/3449d8e80), `docs(R-F18): measure the shared-INTx storm; my USB hypothesis was wrong (U-155)`)
- **U-157** — R-F18`: the storming line identified by counter, and a kernel fix I wrote, tested and then reverted because it did nothing ([`c0249e9fa`](https://gitlab.com/e-os/e-os/-/commit/c0249e9fa), `docs(R-F18): count the storming line; revert a kernel fix that did nothing (U-15`)
- **U-159** — R-F14` and `R-F15` closed ([`cccead168`](https://gitlab.com/e-os/e-os/-/commit/cccead168), `ci(lint): gate shell scripts and the root manifest (U-159, R-F14, R-F15)`)
- **U-160** — CLAUDE.md gains the testing, auditing and record-keeping discipline this session kept learning the hard way ([`71b99feb7`](https://gitlab.com/e-os/e-os/-/commit/71b99feb7), `docs(claude): add the testing, auditing and record-keeping discipline (U-160)`)
- **U-162** — R-F19` halved: the filesystem is created, the failure is in populating it ([`b42730a0c`](https://gitlab.com/e-os/e-os/-/commit/b42730a0c), `docs(R-F19): the filesystem is created; the failure is in populating it (U-162)`)
- **U-163** — R-F20`: seven core recipes ship as UPSTREAM prebuilt binaries, not from the pinned forks ([`e0178636b`](https://gitlab.com/e-os/e-os/-/commit/e0178636b), `docs(R-F20): seven core recipes ship as upstream binaries, not the pinned forks `)
- **U-164** — R-F20` has teeth: the client-side manifest signature verification is NOT in the image ([`795be5c22`](https://gitlab.com/e-os/e-os/-/commit/795be5c22), `docs(R-F20): the client-side manifest verification is not in the image (U-164)`)
- **U-166** — R-F19` root-caused: the install completes and then `unmount_path` fails ([`e131f6adb`](https://gitlab.com/e-os/e-os/-/commit/e131f6adb), `docs(R-F19): the install completes; unmount_path is what fails (U-166)`)
- **U-167** — CLAUDE.md` przepisany po polsku, rozszerzony o typy repozytoriów i cele docelowe ([`8e39f4068`](https://gitlab.com/e-os/e-os/-/commit/8e39f4068), `docs(claude): rewrite CLAUDE.md in Polish; add repo types and target state (U-16`)
- **U-168** — braki z listy TODO domknięte: `cookbook.lock` śledzony, bramka pokrycia, SBOM, rustdoc, ADR-y i diagramy ([`b373e8cca`](https://gitlab.com/e-os/e-os/-/commit/b373e8cca), `ci(gates): track cookbook.lock, gate coverage, add SBOM/rustdoc/ADRs (U-168)`)
- **U-173** — końce linii przestały być nawykiem edytora, a stały się polityką w `.gitattributes`; po drodze dwa moje wejściowe założenia okazały się fałszywe ([`3eb3590ac`](https://gitlab.com/e-os/e-os/-/commit/3eb3590ac), `ci(gates): pin line endings with .gitattributes; gate them as check 8 (U-173)`)
- **U-177** — kontrola 7 wypisywała „typy się nie zgadzają" na KAŻDY niezerowy kod wyjścia ([`026387523`](https://gitlab.com/e-os/e-os/-/commit/026387523), `docs(gates): record U-177 -- check 7 tells "the instrument did not run" from "br`)
- **U-178** — domknięcie `U-173`: trzy rzeczy zapisane tam jako niezweryfikowane są zmierzone, a przegląd ekosystemu pokazał, gdzie ta sama pułapka czeka ([`cad953dd8`](https://gitlab.com/e-os/e-os/-/commit/cad953dd8), `docs(U-173): close the three unverified claims and record the ecosystem survey (`)
- **U-182** — R-F19` domknięte ([`b378df96a`](https://gitlab.com/e-os/e-os/-/commit/b378df96a), `docs(roadmap): close R-F19 -- the entry had been contradicting itself (U-182)`)
- **U-183** — R-701a` zweryfikowane w obrazie i zamienione w bramkę ([`9b78e3b06`](https://gitlab.com/e-os/e-os/-/commit/9b78e3b06), `ci(gates): keep the upstream package source disabled -- check 9 (U-183)`)
- **U-186** — Audyt wszystkich drzew: 80 nieaktualnych migawek, zero utraconej pracy, i reguły w `CLAUDE.md` §20, żeby to się nie powtarzało ([`1522bf6bf`](https://gitlab.com/e-os/e-os/-/commit/1522bf6bf), `docs(hygiene): audit every tree, archive 80 stale snapshots, add CLAUDE.md 20 (U`)
- **U-190** — docs/tokeny.md ([`1a8abddb8`](https://gitlab.com/e-os/e-os/-/commit/1a8abddb8), `docs: one place for every token, starting with the one that needs none (U-190)`)
- **U-201** — Audyt 143 twierdzeń o stanie projektu: 75 prawdziwych, 34 częściowo, 32 fałszywe ([`a7945205e`](https://gitlab.com/e-os/e-os/-/commit/a7945205e), `docs(audit): 143 claims checked -- 32 false; two hardware blockers were on no li`)
- **U-202** — docs/plan-do-sprzetu.md ([`c0abcc1c6`](https://gitlab.com/e-os/e-os/-/commit/c0abcc1c6), `docs(plan): the road from QEMU to a physical PC, ordered by what unblocks what (`)
- **U-203** — ROADMAP-v2.md` + strona ([`b0b6805fa`](https://gitlab.com/e-os/e-os/-/commit/b0b6805fa), `docs(roadmap): ROADMAP-v2 -- second-generation plan grounded in measurement (U-2`)
- **U-204** — ROADMAP-v2` rozszerzone o rdzeń samego systemu E-OS ([`a3af5c2a6`](https://gitlab.com/e-os/e-os/-/commit/a3af5c2a6), `docs(roadmap): extend ROADMAP-v2 with the E-OS system core itself (U-204)`)
- **U-214** — Porządki na dysku ([`1dd003b6e`](https://gitlab.com/e-os/e-os/-/commit/1dd003b6e), `chore(disk): reclaim 121 GB, and record the cleanup rule the measurement forced `)
- **U-223** — V2-MS13`/`V2-MS14`/`V2-MS15 ([`990247ca5`](https://gitlab.com/e-os/e-os/-/commit/990247ca5), `docs: record U-223 -- V2-MS13/14/15 landed, and the frozen-serial defect found w`)
- **U-225** — Testowanie wielostopniowe i pokrycie mierzone na bieżąco wpisane do kontraktu: `CLAUDE.md` §5.9 (osiem poziomów, z tabelą sześciu przypadków z tej sesji, gdzie jeden kierunek testowania świecił na zielono przy zepsutej rzeczy) i §5.10 (pokrycie przy każdym przebiegu `verify.sh`, podłoga 38 % na kodzie własnym, doradczo na vendorowanym); §6 rozszerzone o cztery pozycje kontrolne ([`fdb1ad91b`](https://gitlab.com/e-os/e-os/-/commit/fdb1ad91b), `docs(claude): testowanie wielostopniowe i pokrycie mierzone na bieżąco`)
- **U-227** — `ROADMAP.md` i `ROADMAP-v2.md` scalone w **jedną** roadmapę po angielsku, 1861 linii: jeden rejestr przedmiotowy jako źródło statusu, widok czasowy wyprowadzony z niego, jedna legenda zamiast trzech alfabetów. 236 z 237 identyfikatorów obecnych (brakujący `V2-Nx` to wzorzec rodziny); kolizje `R-70x`/`R-80x` rozstrzygnięte w Aneksie B, wycofane identyfikatory wypisane w Aneksie C z powodem. `ROADMAP-v2.md` zastąpiony wskaźnikiem — **nie skasowany**, pełny tekst pod `git show 87e8194b1:ROADMAP-v2.md` (zweryfikowane: 122 262 bajty, zgodne z Aneksem C.2) ([`85acaffef`](https://gitlab.com/e-os/e-os/-/commit/85acaffef), `docs(roadmap): scal ROADMAP i ROADMAP-v2 w jedną roadmapę po angielsku`)

### Fixed
- **Bramka rozruchowa nie melduje już sukcesu, nie wystartowawszy** — `ci-boot-smoke.sh <obraz> --arch x86_64`, forma opisana w linii użycia tego samego pliku jako poprawna, wstawiała `--arch` do `TIMEOUT`, a stamtąd do arytmetyki. Sedno: **bash 3.2, powłoka systemowa macOS, kończy SKRYPT kodem 0**, gdy `set -u` trafi na nieustawioną nazwę wewnątrz `$(( ))`. Zmierzone: `echo "$NIE_MA"` → 1, ale `x=$(( 1 + NIE_MA ))` → **0**. Skrypt wypisywał `arch: unbound variable`, nie wchodził do pętli ani razu i meldował sukces. Zapisane jako pułapka P-15.
- **Status `make` nie ginie już w potoku do `tail`** — `eos-build.sh` uruchamiał jedyny krok budujący obraz jako `make ... | tail -3` w świeżej powłoce **w kontenerze**, która startuje bez `pipefail`, więc `podman run` zwracał status `tail`. Zmierzone w tym samym obrazie: `bash -lc "false | tail -3"` → 0, z `set -o pipefail` → 1. Skutek był realny: build padał na trzech błędach kompilacji Rusta, a skrypt meldował „make produced NOTHING… A Secure Boot key IS in place" — prawdę bezużyteczną i wskazującą w złą stronę, bo `tail -3` ucinał przyczynę.
- **Kontrole 12–15 w `ci-integrity.sh` wyszły spod gałęzi `else` kontroli 11** — zamykające `fi` leżało na samym końcu pliku, więc piny blake3 tarballi, puste tablice pod `set -u`, martwe ścieżki docs i śledzone artefakty budowania sprawdzały się **wyłącznie wtedy, gdy kontrola 11 przechodziła**. Czyli w jedynej sytuacji, dla której kontrola 11 istnieje, cztery niezwiązane bramki milkły. Zmierzone: przed — 10 wierszy `ok` i zero z tych czterech; po — 14 i wszystkie działają.
- **`pin-check` nie może przejść, nie porównawszy ani jednego forka** — pętla kończy się `done < <(_parse)`, a status procesu w substytucji nie jest widoczny dla powłoki. Przy pustym manifeście pętla wykonywała się zero razy i `--strict` zwracał 0, wypisując przy tym `pins ok=0 drift=0 split-pin=0`. Liczba była na wierzchu, tylko nikt jej nie egzekwował.
- **Strażnik regresji rozruchu może wreszcie zawieść wywołującego** — `repro-intx-lines.sh` kończył się **bezwarunkowym `exit 0`** zaraz po wypisaniu, że wykryto regresję R-F16, choć `CLAUDE.md` nazywa go strażnikiem, a `docs/architecture/build-path.md` rysuje jako węzeł GUARD potoku. Druga wada w tym samym pliku: werdykt wiersza zależał od tego, co **wypisał** poller, a nie co **znalazł** — padnięty poller (pusty łańcuch) przechodził test `[ "$secs" != "-" ]` i był raportowany jako udany rozruch.
- **Trzy skrypty sprawdzające przestały cicho pomijać wpisy** — `eos-check-tar-pins.py` wyciszało błąd parsowania `recipe.toml` przez `except Exception: continue`, a `name not in index` odczytywało powstałą nieobecność jako „nie należy do obrazu" (zepsuty przepis: licznik 77 → 76, status 0); brak konfiguracji obrazu zawężał domknięcie do 20 przepisów i nadal mówił `ok`. `eos-check-doc-paths.py` pomijało **każde** trafienie, przed którym gdziekolwiek w wierszu stał URL — po zawężeniu bramka od razu znalazła prawdziwe martwe odwołanie w `.github/workflows/docs.yml:6`. `eos-check-repo-types.py` porównywał wyłącznie typy z literalnego `"ABCD"`, więc repozytorium z typem `Z` dawało `OK — 31 repozytoriów, typy zgodne`.
- **Kod wyjścia odzwierciedla wynik w obu skryptach lustrzanych** — `eos-mirror-drift.sh` zliczał repozytoria nierozstrzygnięte po nieudanym `clone`/`fetch`, wypisywał je i **ignorował w kodzie wyjścia**, kończąc zdaniem „Każdy fork ma typ zgodny"; trwała awaria wyglądała identycznie jak chwilowy problem z siecią. Zgodnie z konwencją `cannot()` z `ci-integrity.sh` (U-177) „nie dało się zmierzyć" jest teraz czerwone. `eos-setup-mirrors.sh` kończył się **jedynką mimo pełnego powodzenia**, bo ostatnim poleceniem był `[ "$APPLY" = "0" ] && echo …`.
- **Oba skrypty synchronizujące meldują, czego nie zrobiły** — `git rev-parse up/master` na nieistniejącym refie wypisuje **sam argument na stdout** i dopiero potem zwraca 128, więc `sync-forks.sh` dostawał dwuwierszowe `u` i **nigdy** nie raportował forka jako `up-to-date`. `eos-sync-buildtree.sh` prowadził staging w podpowłoce potoku bez `set -e`: nieudane `cp` ignorowane, plik śledzony a nieobecny wypadał bez słowa, a porównanie szło na zbiorze mniejszym niż zapowiedziany.
- **Trzy bramki warstwy budowania mogą wreszcie zadziałać** — `mk/prefix.mk` miał **dwukropek na końcu ścieżki** w `rm -f`, przez co tarball `rust-src` przeżywał każdą zmianę wersji łańcucha narzędzi; `mk/repo.mk` odrzucał status `$(MAKE) mount` średnikiem, więc nieudane montowanie zostawiało znacznik sukcesu; a eskalacja pinów SHA256 kompilatorów (`EOS_STRICT_FETCH=1`) była **nieosiągalna**, bo `PODMAN_ENV` nie przekazywał zmiennej do kontenera, w którym odbywa się pobieranie.
- **Wykrywanie możliwości wygrywa z flagą z linii poleceń** — GNU make daje pierwszeństwo zmiennym z linii poleceń nad przypisaniem w pliku. `make PODMAN_BUILD=0 PREFIX_BINARY=1 SCCACHE_BUILD=1` zostawiał obie flagi na **1**, mimo że make wypisywał „only built for Linux hosts" i „sccache not found in PATH". Komunikat się pojawiał, flaga zostawała włączona. Trzy przypisania wykrywające zmienione na `override`.
- **`install-smoke` pyta o komunikaty w kolejności, w jakiej gość je wypisuje** — dwa sprawdzenia po otwarciu dysku były ustawione odwrotnie do wyjścia instalatora, więc pierwsze konsumowało strumień poza drugie. Przebieg, który PRZESZEDŁ, wypisywał jednocześnie „saw the EFI bootloader…" i „(not seen) the install starting" — niemożliwe, gdyby drugie mogło kiedykolwiek trafić. Do tego przepalało pełne okno 120 s (×3 pod TCG).

- **Jedno getty na konsolę szeregową** (#24) — konfiguracja x86_64 (a od 2026-09-02 także aarch64) startowała getty na konsoli jądra w **dwóch** miejscach naraz: `nowait getty /scheme/debug/no-preserve -J` w `30_console` oraz `30_serial-getty.service` z `args = ["/scheme/debug"]`. Dwa getty to dwa procesy `login` czytające to samo wejście: po zalogowaniu pierwszy oddawał TTY shellowi, a drugi nadal czytał — i połykał kolejną wpisaną linię, odpowiadając *„Login incorrect"*. Widać to było w logu jako baner `issue` wypisany **dwa razy** (`login` wypisuje go raz na instancję) i dwa prompty `eos login:` odmalowane obok siebie. Zmierzone: z duplikatem `ci-install-smoke.sh` na x86_64 przechodził **2 razy na 7**, bez niego **5 na 5**. Przy okazji sprostowany komentarz twierdzący, że wejście po szeregowym nie dociera do gościa pod QEMU na macOS — cały harness steruje gościem właśnie tą drogą. Na aarch64 ten objaw **nie wystąpił** — przechodził z duplikatem — więc usunięcie go tam zamyka wyścig utajony, a nie zmierzoną awarię; sprawdzone jednym przebiegiem przed i jednym po zmianie, oba PASS, żeby potwierdzić, że logowanie po szeregowym nadal działa.
- **`install-smoke` przechodzi na x86_64 od początku do końca** (#6) — harness domykał się dotąd tylko na aarch64; na x86_64 gubił się w wymianie haseł pierwszego rozruchu (dwa odrzucone logowania, pusta linia zamiast numeru dysku). Czysty build z 2026-09-02 melduje *„PASS — installed to a second disk and booted it to a login prompt"*, kod wyjścia **0**, ze stage 2 włącznie — niezawodnie dopiero po domknięciu #24 (patrz wpis wyżej); sama ta poprawka dawała 2 przejścia na 7. Przyczyną **nie była** zalegająca nowa linia po stronie `login` — instrumentacja opróżniająca stdin tuż przed `passwd` zastała kolejkę **pustą** (`drained before passwd: []`) w dwóch niezależnych przebiegach; to obala hipotezę, którą sam wcześniej postawiłem. Odpowiada `266c4f4` w `eos-userutils` (sonda rozmiaru terminala w `getty`, !49). Kontrola negatywna: cofnięcie tego jednego commitu **wyłącznie w drzewie budowania**, przebudowa i ten sam harness → *„saw a rejected login"*, FAIL. Z poprawką PASS, bez niej FAIL.
- **aarch64 znowu bootuje** (#15) — przyczyną było `lto = true` w **globalnym** `[profile.release]` bootloadera: LTO scala ramki wywoływanych funkcji, więc zmienne żyjące dotąd po kolei żyją naraz, a ścieżka ładowania jądra przekraczała ~124 KiB stosu DXE firmware'u (`ESR 0x9600000B`, access-flag fault poziom 3). **Suma zapotrzebowania jest większa bez LTO** (235 456 B wobec 229 808 B) — liczy się szczyt, nie suma. Naprawione punktowo przez `CARGO_PROFILE_RELEASE_LTO=false` wyłącznie dla aarch64; BIOS i x86_64 zachowują dotychczasowe ustawienia.
- **230 martwych odwołań `docs/*` naprawionych** (#17) — zgłoszenie mówiło o ~141 w ośmiu ścieżkach; pełny skan dał **230 w dwudziestu dwóch**. Surowy skan dawał 326: odjęte 16 ogonów cudzych URL-i, 66 zapisów datowanych i 7 pozycji, które nie są dryfem.
- **`pin-check` widzi rozjazd receptura ↔ `repos.toml`** (#18) — bramka porównywała wyłącznie manifest z wierzchołkiem forka, więc receptura wskazująca gdzie indziej była niewidoczna, a to **recepturę czyta build**. Nowy stan `SPLIT-PIN` nie jest uciszalny listą wyjątków.
- **Bramka rewizji źródeł receptur** (#19) — nic nie sprawdzało, czy pobrane źródła stoją na przypiętych rewizjach; build wypuścił stary bootloader i zameldował `Done.` Bramka **raportuje, ale nie usuwa** modyfikacji pod `source/`, bo receptura bootloadera legalnie wstrzykuje tam klucz operatora.
- **Nocna budowa wykonuje się** (#5) — trzydzieści kolejnych przebiegów z harmonogramu padało od co najmniej 2026-08-25 na wyścigu: `podman machine start` wraca, gdy maszyna **się uruchamia**, a następny wiersz wołał `podman exec` jedną dziesiątą sekundy później. Jedyna bramka, która realnie bootuje system, nie wykonała się przez ponad tydzień — padając na czymś, czego nie miała sprawdzać.
- **`R-604a`: wybór dysku pokazuje ścieżkę urządzenia i odrzuca błędną nazwę** (#9) — ekran po raz pierwszy uruchomiony na Redoksie, z kontrolą negatywną. Druga połowa kryterium — *zero zapisów przy odmowie* — jest teraz **mierzona**, a nie wnioskowana: driver bada dysk po obu stronach odmowy i wymaga zera przydzielonych bloków.
- **`install-smoke` idzie szybką ścieżką** (#21) — kopiowanie plik po pliku (1292 z 13681 plików w 26 minut, ~4,5 h docelowo) brało się stąd, że harness startował z `harddrive.img`, a nie z nośnika. Szybka ścieżka wymaga startu **live**, bo kopiuje z obrazu wczytanego do RAM-u. Z nośnika: **11 minut**.
- **Dowody z porażki nie zapychają dysku systemowego** (#22) — lądowały obok obrazu źródłowego, czyli w CI na dysku rozruchowym: 1,7 GB przy 2,2 GB wolnego. Idą teraz tam, gdzie wskazuje `EOS_SMOKE_WORK`, i ważą **64 MB**.
- **`getty` nie pożera już wpisanych znaków** — sonda rozmiaru terminala czytała TTY przez 500 ms w poszukiwaniu odpowiedzi na `\x1B[6n` i **wyrzucała każdy bajt, który nią nie był**. Na konsoli szeregowej nikt nie odpowiada, więc robiła to zawsze, a `daemon()` woła ją tuż przed uruchomieniem `login`. Bajty wracają teraz do pty. **Sprostowanie 2026-09-02:** ten wpis twierdził wcześniej, że poprawka *nie* usuwa awarii `install-smoke` na x86_64 — to było błędne. Usuwa ją; dowód i kontrola negatywna w osobnym wpisie o #6 poniżej.
- **Test vendorowanego cookbooka nie zależy już od kolejności testów** — `file_system_loop_no_infinite_loop` czytał globalną konfigurację, którą ustawiał **inny** test, więc przechodził pod `cargo test` i padał pod `cargo test --tests` z `Configuration is not initialized`. Jest teraz **jedna** konfiguracja testowa, inicjowana przez tego, kto pierwszy jej potrzebuje. Kontrola: w izolacji test bez poprawki pada, z poprawką przechodzi. #20
- **`install-smoke` nie melduje już braku, którego nie ma** — wiersz `(not seen) the EFI bootloader being written` pojawiał się także w przebiegach zakończonych **sukcesem**, łącznie z etapem 2 bootującym zainstalowany dysk. Szybka ścieżka kopiuje obraz **blokowo, razem z ESP**, więc nazwa pliku nigdy nie pada — nieobecność jest tam regułą, nie usterką. Oczekiwanie nazywa się teraz tym, czego naprawdę pilnuje.
- **`install-smoke` przechodzi przez OOBE** — driver czekał na prompt `new password:`, który **sam już skonsumował** wyścigiem powyżej pętli ponowień, podczas gdy gość stał przy tym prompcie i czekał na wejście. Obie strony czekały na siebie. To **regresja harnessu** z `6c62b0ff9`, nie usterka gościa i nie „przepełnienie bufora szeregowego", od którego nazwano gałąź. Odtworzone na aarch64 — czyli nigdy nie było specyficzne dla architektury. #16
- **Etap `coverage` nazywa awarię testu, zamiast ją przemilczeć** — noga vendorowanego cookbooka jest *advisory* z rozmysłem (to kod upstreamu, `.gitlab-ci.yml` kończy tę linię `|| true`), ale połykała też **awarię testu**: `cargo test` wychodził z kodem 101, a człowiek widział jedno zdanie *„advisory leg did not complete"* — bez nazwy testu i bez liczby. Teraz padające testy są wypisane z nazwami. Etap nadal **nie blokuje** — to zamierzone i niezmienione. #20
- **`pin-check` rozróżnia kierunek rozjazdu** — każdą różnicę między pinem a wierzchołkiem nazywał *„pin behind fork tip"*, także wtedy, gdy pin był o commit **do przodu**, a to lustro GitHuba nie zdążyło zreplikować. Zmierzone 2026-09-01 przy podbiciu pinu bootloadera: komunikat kazałby podbić pin, który był już poprawny. Nowy stan **MIRROR-BEHIND** pyta GitLaba (źródło prawdy) i podaje obie wartości. Kontrola negatywna: prawdziwy pin za wierzchołkiem nadal raportuje się jako `DRIFT (pin behind fork tip)`.
- **aarch64 znowu bootuje** — od `87b214b5` bootloader ginął w sondzie stosu `__chkstk` na stosie DXE firmware'u, **przed** `ExitBootServices` (`ESR 0x9600000B`, access-flag fault poziom 3). Przyczyną było `lto = true` w **globalnym** `[profile.release]`, dodane z powodu twardego limitu 384 KiB dla stage3 BIOS-u: LTO scala ramki wywoływanych funkcji w ramkę wywołującego, więc zmienne żyjące dotąd po kolei żyją naraz. Suma zapotrzebowania jest **większa** bez LTO (235 456 B wobec 229 808 B) — liczy się szczyt, nie suma. Poprawione punktowo przez `CARGO_PROFILE_RELEASE_LTO=false` wyłącznie dla aarch64; BIOS i x86_64 zachowują dotychczasowe ustawienia. Dowód z kontrolą dopasowaną: przebudowa z LTO pada identycznie przy 2048 i 6144 MiB, poprawka przechodzi przy obu.
- **`ci-integrity` check 14 nie pada już na własnym źródle** — `scripts/eos-check-doc-paths.py` wymienia martwe ścieżki jako **przykłady** tego, co wykrywa, więc skanując siebie padał na własnej prozie. Na gałęzi tego nie było widać, bo plik był jeszcze **nieśledzony**, a bramka czyta `git ls-files` (klasa **P-9**). Wykluczony jest jeden plik, z warunkiem wycofania wpisu; wykrywanie w pozostałych plikach sprawdzone kontrolą negatywną.
- **`verify.sh` uruchamia teraz `coverage` i `cargo-deny`** — oba etapy od dawna meldowały `SKIPPED — not installed`, choć narzędzia **były zainstalowane**. `cargo` pochodzi z homebrew i nie dodaje `~/.cargo/bin` do `PATH`, czyli dokładnie katalogu, w którym `cargo install` je umieszcza. Zmierzone po poprawce: `coverage` 4 s i `cargo-deny` 3 s zamiast `0s SKIPPED`, `16 PASS · 0 SKIPPED`.
- **U-153** — R-F16` root-caused and fixed ([`1662771aa`](https://gitlab.com/e-os/e-os/-/commit/1662771aa), `fix(gic): root-cause R-F16 -- a read-modify-write on a W1C register (U-153)`)
- **U-156** — R-F10` closed: the bootloader now unlocks the filesystem it was actually built against ([`5709a5921`](https://gitlab.com/e-os/e-os/-/commit/5709a5921), `fix(tcb): the bootloader now unlocks the filesystem it was built against (U-156,`)
- **U-158** — eos-setup-mirrors.sh` would have destroyed the published package repos, and could not be audited without a credential ([`ba254d21d`](https://gitlab.com/e-os/e-os/-/commit/ba254d21d), `fix(mirrors): stop --apply from overwriting the published package repos (U-158)`)
- **U-165** — R-F20` fixed: every E-OS-forked recipe is built from its fork again, and `R-703`'s client half is back in the image ([`6067b8e47`](https://gitlab.com/e-os/e-os/-/commit/6067b8e47), `fix(build): build E-OS forks from source again; R-703's client half is back (U-1`)
- **U-170** — R-F19` rozwiązane ([`034ac22ce`](https://gitlab.com/e-os/e-os/-/commit/034ac22ce), `fix(R-F19): root-cause and fix the unmount EPERM; unmask the real install failur`)
- **U-171** — R-F19` odsłoniło łańcuch: instalacja przeszła z „0 plików" do „13 679 i kopiuje"; po drodze trzy nowe usterki i jedno moje błędne twierdzenie ([`def3c8134`](https://gitlab.com/e-os/e-os/-/commit/def3c8134), `fix(R-F21,R-F22): install now runs; record R-F23 and correct my own speed claim `)
- **U-172** — dlaczego `R-601` nie da się dziś udowodnić: to koszt, nie usterka ([`60def9e37`](https://gitlab.com/e-os/e-os/-/commit/60def9e37), `fix(harness): stop the driver burning a core after the VM dies; record R-F24 (U-`)
- **U-174** — .gitlab-ci.yml` był niepoprawny od `U-168` i każdy pipeline od 2026-08-27 14:47 tworzył ZERO zadań ([`69e7401e9`](https://gitlab.com/e-os/e-os/-/commit/69e7401e9), `fix(ci): quote script items so .gitlab-ci.yml parses; pipelines create jobs agai`)
- **U-175** — zadanie `integrity` w CI nie instalowało `python3`, więc kontrola 7 nie mogła się wykonać ([`d2bbdfbb6`](https://gitlab.com/e-os/e-os/-/commit/d2bbdfbb6), `fix(ci): install python3 in the integrity job so check 7 can run (U-175)`)
- **U-179** — R-F18` zmierzone i przeformułowane ([`c75422b99`](https://gitlab.com/e-os/e-os/-/commit/c75422b99), `fix(R-F18): measure it -- any two PCI devices sharing an INTx line storm, not ju`)
- **U-180** — R-F18` naprawione ([`93d206a7d`](https://gitlab.com/e-os/e-os/-/commit/93d206a7d), `fix(R-F18): the driver acked interrupts that were not its own (U-180)`)
- **U-181** — Sprostowanie do `U-180`: `install-smoke` PRZECHODZI, a `R-F25` nie było usterką ([`5817ca3b2`](https://gitlab.com/e-os/e-os/-/commit/5817ca3b2), `fix(record): retract R-F25 -- install-smoke passes; the instrument was broken (U`)
- **U-185** — make` nie buduje z tego repozytorium ([`5b4f00722`](https://gitlab.com/e-os/e-os/-/commit/5b4f00722), `fix(build): make does not build from this repo -- sync tool + doc drift (U-185)`)
- **U-187** — Trzy usterki naprawione, a najciekawsza znalazła się sama ([`bbe81c758`](https://gitlab.com/e-os/e-os/-/commit/bbe81c758), `fix(gates): close R-F02 and R-F13; the new gate caught a bug I had just written `)
- **U-188** — R-F06` naprawione ([`6da5b9fee`](https://gitlab.com/e-os/e-os/-/commit/6da5b9fee), `fix(pkg-lib): a keyless remote is an error, not a panic -- R-F06 (U-188)`)
- **U-189** — Bramka, która nigdy nie zadziałała, i zarzut, który okazał się nietrafiony ([`f7f52fa5e`](https://gitlab.com/e-os/e-os/-/commit/f7f52fa5e), `fix(ci): docs-currency had never run once; correct a stale x86_64 note (U-189)`)
- **U-200** — Publikacja przygotowana ([`6330140b0`](https://gitlab.com/e-os/e-os/-/commit/6330140b0), `fix(publish): one variable name, a stale comment, and a README for the artefact `)
- **U-219** — Poprawka bezpieczeństwa w jądrze: `Iopl` wymaga teraz roota ([`6799db569`](https://gitlab.com/e-os/e-os/-/commit/6799db569), `fix(kernel): bump to the Iopl privilege fix; raw port I/O now requires root (U-2`)
- **U-224** — Klucz `R-702` istnieje ([`51cac0382`](https://gitlab.com/e-os/e-os/-/commit/51cac0382), `fix(build): rebuild the host tools, stop exporting empty artifacts, and correct `)
- **U-226** — Zapis stanu łańcucha weryfikacji był nieprawdziwy w **obu** dokumentach: roadmapa twierdziła `verify.sh: 15 PASS`, `CLAUDE.md` §13.1 `12 PASS · 3 SKIPPED`; pomiar dał 13 PASS · 2 SKIPPED i kod 2. Po doinstalowaniu `cargo-llvm-cov` i `cargo-deny`: **15 PASS · 0 FAIL · 0 SKIPPED**. Drugie znalezisko: `CLAUDE.md` i `verify.sh:109` mówiły, że bramki `scripts/eos-check-tar-pins.py` „w drzewie nie ma" — bramka jest. Przeszła kontrolę mutacyjną dwustopniowo, bo pierwsza mutacja (`recipes/libs/atk`) trafiła **obok domknięcia obrazu** i dała exit 0 ([`cd323056a`](https://gitlab.com/e-os/e-os/-/commit/cd323056a), `docs(verify): popraw zapis stanu łańcucha -- bramka tar-pins istnieje`)
- **U-229** — `R-608`: `docs/getting-started/install.md` mówi prawdę o nazwie nośnika, procedurze zapisu i **składni instalatora**. Trzy usterki: (1) dokument w dwóch miejscach kazał zbudować `redox-live.iso`, cel **nieistniejący** od `R-611a`; (2) §3 podawało `redox_installer <config.toml> <disk>` — **odwrotnie**, bo `src/bin/installer.rs:208` w przypiętej rewizji `74726c889b` bierze `parser.args.first()` jako ścieżkę do `install(config, path)`, więc pozycyjny argument to **dysk**, a użytkownik wycelowałby instalator w swój plik TOML; (3) blok weryfikacji pobrania nie znał nośnika pakowanego przez `R-611b`. Nie zmieniona świadomie rozbieżność wersji (obraz stempluje `0.1.0 (Genesis)`, nośnik nazywa się `0.2.0`) — podbicie liczby uczyniłoby dokument kłamliwym, więc jest **opisana**. Sprawdzony i **odrzucony** czwarty, pozorny zarzut: binarka `redox_installer_gui` istnieje, buduje się z podkatalogu `gui/`. Domknięte też dwie specyfikacje, które zapisywały odwróconą składnię jako rozjazd otwarty ([`cae1445dc`](https://gitlab.com/e-os/e-os/-/commit/cae1445dc), `docs(R-608): install.md mówi prawdę o nazwie nośnika, procedurze zapisu i składni instalatora`)

### Security
- **NetSurf w obrazie to upstreamowy prebuilt, nie budowa PIE z przepisu** (#28) — README ogłaszało „built from source as PIE", a `docs/architecture/netsurf-pie.md` opisywało łatki E-OS. Zmierzone w drzewie budowania, obie architektury: `readelf -h` → **EXEC** pod stałym `0x400000`, brak flagi PIE, **zero** wystąpień `about:welcome` (łatka z `recipe.toml:55`), `stage.toml` z obcym `commit_identifier 34eb9f34…` przy HEAD drzewa `6e7f6432`. Cookbook wziął prebuilt z `static.redox-os.org`; wrapper `-fPIC/-pie`, `sed` na `SDL_RESIZABLE` i strona startowa są martwe w artefakcie. PIE to warunek ASLR dla największego niezaufanego parsera w obrazie. Ta sama klasa wady, którą kontrola 6 `ci-integrity.sh` łapie dla forków — `netsurf` forkiem nie jest, więc nikt nie pilnował. README i `netsurf-pie.md` mówią teraz o artefakcie; kontrola na artefakcie (`Type: DYN`, `about:welcome`) zaproponowana w zgłoszeniu. Znalezione przez adwersarza w inwentaryzacji nowego zakresu, potwierdzone własnym pomiarem przed zmianą dokumentów.
- **Hasła ustawiane w systemie są haszowane słabiej, niż deklarowało README** (#27) — README obiecywało argon2id (m=19456, t=2, p=1). To prawda dla haseł zahaszowanych w **czasie budowy obrazu** (`installer` → `rust-argon2 3.0.0`). Każde hasło ustawione **w działającym systemie** — `passwd`, wymuszone hasło pierwszego rozruchu (R-602), greeter `orblogin` — przechodzi przez `redox_users 0.4.6` → `rust-argon2 0.8.3`, którego `Config::default()` to **argon2i, m=4096, t=3** (`lib.rs:271`; własny test crate'u `config.rs:97-102`). Zmierzone w kontenerze budującym: **4,03 ms** na próbę zamiast 14,06 — 10 000 popularnych haseł pada offline w 40 s zamiast 141. Mechanizm, który ma podnieść bezpieczeństwo (R-602), zapisuje wynik gorzej niż obraz, który zastępuje. README poprawione tego samego dnia, żeby mówiło o obu ścieżkach; poprawka kodu to `R-602g` w roadmapie.
- **Skan sekretów w `pre-push` zawodzi zamknięcie** — brak `gitleaks` na PATH pomijał cały blok `if`, skrypt kończył się na `fi` i zwracał 0: push z sekretem przechodził, a bramka meldowała sukces. Zmierzone: przed poprawką status 0 i **zero wzmianek o gitleaks w wyjściu** — nie zostawał nawet ślad, że skan się nie odbył. `lefthook.yml` w tym samym repozytorium pisze przy swoim skanie wprost, że „brak gitleaks też jest awarią"; ten hook robił odwrotnie. Dodane obejście `EOS_SKIP_SECRET_SCAN=1`, jak w lefthooku. Poprawiona też instrukcja włączania: `git config core.hooksPath scripts/hooks` nie **dodaje** hooka, tylko **zastępuje** cały katalog, więc wyłączała lefthooka razem z jego mocniejszą bramką pre-commit.
- **Bramka źródeł pakietów przestała trafiać we własną prozę** — kontrola 9 w `ci-integrity.sh` (R-701a / U-210) wymaga, by obraz z aktywnym repozytorium E-OS przypinał klucz podpisujący, ale sprawdzała to gołym `grep` po całym pliku. Obie konfiguracje wymieniają tę ścieżkę **dwa razy we własnych komentarzach** (aarch64: 721 i 740; x86_64: 749 i 768), zanim przypną ją naprawdę. Asercja trafiała w prozę i nie mogła zawieść na żadnej konfiguracji z tego szablonu. Zmierzone: po podmianie jedynego realnego pinu na atrapę bramka nadal mówiła `ok`; po poprawce — `FAIL`.
- **Pipeline weryfikuje podpis, który sam złożył** — `cosign sign-blob` i `cosign attest-blob` meldowały sukces wyłącznie kodem wyjścia, a jedyne wystąpienia `cosign verify-blob` w `release.yml` były w **komentarzu** adresowanym do pobierającego. Wystarczyło przemianować repozytorium albo przenieść je do innej organizacji, żeby podpis powstał bez błędu z **inną** tożsamością OIDC, run był zielony, a pobierający dostał `none of the expected identities matched`. Dodane dwa kroki weryfikacji, każdy zaraz po swoim bundle'u, z **dosłownie tym samym** wzorcem tożsamości, który plik publikuje. Zmierzone na atrapie cosign: sam podpis z rozjechaną tożsamością → 0; z weryfikacją → 1.
- **Bramka Secure Boot nie może już przejść, nie weryfikując niczego** — `for b in $(find ... 2>/dev/null)` po zerze trafień wykonywał się zero razy, `rc` zostawało 0 i bramka meldowała sukces bez sprawdzenia ani jednego pliku; wyciszony stderr zamieniał „nie ma katalogu" w „brak trafień". Zmierzone na ciele bramki w izolacji: katalog schowany → **status 0 i zero wierszy wyjścia**; po poprawce → status 1 z nazwaną przyczyną.
- **Trzy skrypty kluczowe sprawdzają artefakt, nie kod wyjścia** — `eos-sign-bootloader.sh` wypisywał komunikat o selfteście także w trybie prawdziwego podpisywania i kończył się zerem, więc brak podpisanego bootloadera ujawniłby się dopiero na pececie z Secure Bootem jako `Access Denied` (U-208). `eos-sb-setup-key.sh` i `eos-boot-setup-key.sh` nie sprawdzały statusu kontenera umieszczającego klucz (`set -uo pipefail` bez `-e`) i drukowały bezwarunkowe „Done". Wszystkie trzy sprawdzają teraz obecność artefaktu w drzewie; dla klucza rozruchowego dodatkowo, że `boot.pub.bin` ma dokładnie 32 bajty.
- **Bramki spisu materiałów sprawdzają treść, nie samo istnienie** — `gen-sbom.py` na pustym katalogu kończy się **zerem** i produkuje poprawny CycloneDX z `"components": []`; `make-release.sh` hashował taki plik do `SHA256SUMS` i obejmował **podpisem wydania**. Wydanie mogło nieść podpisany spis materiałów, który nie wymienia niczego — gorzej niż brak SBOM-u, bo to artefakt zapewnienia twierdzący, że zawartości nie ma. W `.gitlab-ci.yml` jedynym sprawdzeniem było `ls -la` na katalogu utworzonym wiersz wyżej przez `mkdir -p`. Obie bramki liczą teraz składniki.

- **U-184** — Klucz podpisujący: jedno polecenie dla człowieka, reszta zautomatyzowana ([`930738a2b`](https://gitlab.com/e-os/e-os/-/commit/930738a2b), `feat(keys): one command for the human, everything else automated (U-184)`)
- **U-191** — Mapa pięciu warstw autentyczności ([`e121edc1e`](https://gitlab.com/e-os/e-os/-/commit/e121edc1e), `docs(keys): map the five authenticity layers; record the lost release key (U-191`)
- **U-192** — Procedura obu kluczy krok po kroku ([`6fa2f5b35`](https://gitlab.com/e-os/e-os/-/commit/6fa2f5b35), `docs(keys): step-by-step for both keys; correct my own claim from U-191 (U-192)`)
- **U-193** — Krok 0 procedury kluczy był niekompletny i zapętliłby operatora ([`49f0d32dc`](https://gitlab.com/e-os/e-os/-/commit/49f0d32dc), `docs(keys): step 0 was incomplete and would have looped the operator (U-193)`)
- **U-194** — Katalog projektu stoi na exFAT z `noowners ([`69db24429`](https://gitlab.com/e-os/e-os/-/commit/69db24429), `fix(keys): the project volume is exFAT+noowners, so a secret cannot be protected`)
- **U-195** — Wznawianie z `U-194` było blokowane przez własną kontrolę skryptu ([`d8098a196`](https://gitlab.com/e-os/e-os/-/commit/d8098a196), `fix(keys): resume was blocked by the script's own guard, and demanded a compiler`)
- **U-196** — Klucz podpisujący indeks pakietów istnieje i jest przypięty ([`d8957773b`](https://gitlab.com/e-os/e-os/-/commit/d8957773b), `fix(keys): the repo-signing key is pinned; three defects found while checking th`)
- **U-197** — R-702` domknięte po stronie obrazu ([`becfce6cf`](https://gitlab.com/e-os/e-os/-/commit/becfce6cf), `feat(R-702): the pinned key is in the image, byte-exact -- and what that does no`)
- **U-205** — R-F26` ZAMKNIĘTE ([`7024bff00`](https://gitlab.com/e-os/e-os/-/commit/7024bff00), `docs(keys): R-F26 closed -- operator rotated the minisign release key (U-205)`)
- **U-206** — V2-N03 ([`a30520680`](https://gitlab.com/e-os/e-os/-/commit/a30520680), `feat(secureboot): sign the bootloader and prove it under Secure Boot, no Microso`)
- **U-207** — Integracja podpisu bootloadera domknięta ([`f518a4cc8`](https://gitlab.com/e-os/e-os/-/commit/f518a4cc8), `feat(secureboot): sign the bootloader in its recipe -- the live ISO boots under `)
- **U-213** — V2-MS12 ([`03aa86a93`](https://gitlab.com/e-os/e-os/-/commit/03aa86a93), `fix(pkg): correct V2-MS12's premise, guard the package key, stop promising prote`)
- **U-218** — Tor B: `V2-MS01` (SBAT) i `V2-MS05` (hermetyczne podpisywanie) zrobione ([`8183c9c4b`](https://gitlab.com/e-os/e-os/-/commit/8183c9c4b), `feat(boot): SBAT in both UEFI bootloaders, hermetic signing, and the SELinux lab`)

---

## [0.2.0] - 2026-08-22

Tag: [`v0.2.0`](https://github.com/Gh0s777tt/E-OS/releases/tag/v0.2.0) — annotated and **signed**; no release object exists for it.

233 commits since `v0.1.0`. This is the wave that added the E-OS applications, the verified boot chain, Secure Boot signing, the hybrid package-index signature, and the supply-chain gates.

### Added

- **U-071** — E-OS Settings ([`f3ef3ca4d`](https://gitlab.com/e-os/e-os/-/commit/f3ef3ca4d), `feat(settings)+fix(greeter): E-OS Settings [R-D01] + audiod nie blokuje sesji [R`)
- **U-072** — Graphical session no longer blocked by audio (`R-F07`) + display regression surfaced (`R-F08`) ([`f3ef3ca4d`](https://gitlab.com/e-os/e-os/-/commit/f3ef3ca4d), `feat(settings)+fix(greeter): E-OS Settings [R-D01] + audiod nie blokuje sesji [R`)
- **U-076** — First-boot forces a password on the shipped passwordless account (`R-602`) ([`6826e3a27`](https://gitlab.com/e-os/e-os/-/commit/6826e3a27), `feat(oobe): kreator first-boot wymusza haslo (R-602 text/getty) + bump eos-useru`)
- **U-077** — First-boot also forces a change of the default `root/password` (`R-602`) ([`2e103eb0f`](https://gitlab.com/e-os/e-os/-/commit/2e103eb0f), `feat(oobe): first-boot wymusza zmiane domyslnego root/password (R-602) [U-077]`)
- **U-079** — The graphical greeter now enforces the first-boot password too (`R-602`) ([`afd4387d9`](https://gitlab.com/e-os/e-os/-/commit/afd4387d9), `feat(oobe): graficzny greeter wymusza haslo — P0 default-creds zamkniete na KAZD`)
- **U-086** — E-OS Notes ([`fd6ac270d`](https://gitlab.com/e-os/e-os/-/commit/fd6ac270d), `feat(apps)+recipes: E-OS Notes — first E-OS original app (U-086)`)
- **U-088** — eos-ui ([`f9d415bd7`](https://gitlab.com/e-os/e-os/-/commit/f9d415bd7), `feat(ui)+recipes: extract eos-ui shared Slint backend; eos-notes uses it (U-088)`)
- **U-089** — E-OS Guard ([`1cd284009`](https://gitlab.com/e-os/e-os/-/commit/1cd284009), `feat(apps)+recipes: E-OS Guard — second original app, integrity monitor (U-089)`)
- **U-094** — E-OS Sysmon ([`d7c95fa86`](https://gitlab.com/e-os/e-os/-/commit/d7c95fa86), `feat(apps)+recipes: E-OS Sysmon — third original app, system monitor (U-094)`)
- **U-096** — E-OS Control v2 ([`6d3dc87e5`](https://gitlab.com/e-os/e-os/-/commit/6d3dc87e5), `feat(eos-control): process grouping + force-kill — bump pin af7a932→fed7e32 (U-0`)
- **U-097** — E-OS Control v3 ([`d9968546c`](https://gitlab.com/e-os/e-os/-/commit/d9968546c), `feat(eos-control): rank processes by memory + total-memory readout — bump pin fe`)
- **U-098** — Launcher clock: local date + timezone (was UTC `HH:MM` only) ([`84db806f5`](https://gitlab.com/e-os/e-os/-/commit/84db806f5), `feat(launcher): local date+timezone clock — bump orbutils cf121dc→94dcc91, ship `)
- **U-099** — Launcher Start-menu type-to-search ([`8dd7f17a0`](https://gitlab.com/e-os/e-os/-/commit/8dd7f17a0), `feat(launcher): Start-menu type-to-search — bump orbutils 94dcc91→7b1268b, compl`)
- **U-100** — Compositor screenshot ([`7c90fd48f`](https://gitlab.com/e-os/e-os/-/commit/7c90fd48f), `feat(compositor): Super-P screenshot — bump orbital 7ee7c04→38226c7, R-D04 done `)
- **U-101** — Status tray: real icons + click-to-Settings ([`22aecc805`](https://gitlab.com/e-os/e-os/-/commit/22aecc805), `feat(launcher): functional status tray — icons + click-to-Settings, bump orbutil`)
- **U-102** — Notifications: a minimal daemon + client (`eos-notifyd` / `eos-notify`) ([`6c5de19ae`](https://gitlab.com/e-os/e-os/-/commit/6c5de19ae), `feat(notifications): minimal eos-notifyd/eos-notify — bump orbutils 60c262d→8ad7`)
- **U-103** — netsurf: build from source as a PIE (partial ([`d0b6aafff`](https://gitlab.com/e-os/e-os/-/commit/d0b6aafff), `feat(netsurf): build from source as PIE + fix host-toolchain 404 (U-103, partial`)
- **U-105** — netsurf: local default homepage + click-to-launch verified ([`5f7a48afa`](https://gitlab.com/e-os/e-os/-/commit/5f7a48afa), `feat(netsurf): local about:welcome homepage + verify click-to-launch (U-105)`)
- **U-106** — eos-control: Network tab (live `/etc/net` config + stack status) ([`b102cb5cd`](https://gitlab.com/e-os/e-os/-/commit/b102cb5cd), `feat(control): Network tab — live /etc/net config + stack status (U-106)`)
- **U-107** — eos-control: Storage tab (root filesystem usage via `statvfs`) ([`a17fe6fe2`](https://gitlab.com/e-os/e-os/-/commit/a17fe6fe2), `feat(control): Storage tab — root filesystem usage via statvfs (U-107)`)
- **U-108** — eos-control: Power tab (reboot / shutdown) ([`d6a8a22d9`](https://gitlab.com/e-os/e-os/-/commit/d6a8a22d9), `feat(control): Power tab (reboot/shutdown) — UI + confirm work, action needs pri`)
- **U-109** — eos-control: power actions now WORK ([`1bbd712b2`](https://gitlab.com/e-os/e-os/-/commit/1bbd712b2), `feat(control): power actions work — eos-power shim + password dialog (U-109, R-D`)
- **U-110** — eos-control: Sound tab (master volume via audiod's `audio:volume`) ([`3e759bcb3`](https://gitlab.com/e-os/e-os/-/commit/3e759bcb3), `feat(control): Sound tab via audiod audio:volume (U-110) + fix container build f`)
- **U-111** — build: fix the Podman container build for repo paths containing a space ([`3e759bcb3`](https://gitlab.com/e-os/e-os/-/commit/3e759bcb3), `feat(control): Sound tab via audiod audio:volume (U-110) + fix container build f`)
- **U-132** — the two R-902 pins are bumped and gated ([`ab19dd552`](https://gitlab.com/e-os/e-os/-/commit/ab19dd552), `feat(pins): bump the two R-902 pins after a full gate run; allowlist now empty (`)
- **U-133** — x86_64 boots, boot-smoke now covers it, and the live-USB path onto real hardware is documented ([`ba364edc3`](https://gitlab.com/e-os/e-os/-/commit/ba364edc3), `feat(ci): boot-smoke x86_64 too, and document the live-USB path to metal (U-133)`)
- **U-146** — a second disk silently stops the boot ([`0ddb394b2`](https://gitlab.com/e-os/e-os/-/commit/0ddb394b2), `test(boot): reproduce the silent second-disk boot stall (U-146, R-F16)`)
- **U-150** — R-F16` located exactly, by making `init` say where it is ([`92ec5ab44`](https://gitlab.com/e-os/e-os/-/commit/92ec5ab44), `test(boot): locate R-F16 inside redoxfs, and attach the target over USB (U-150)`)

### Changed

- **U-073** — R-D01 Settings render-verified end-to-end + `R-F08` root-caused ([`21551e227`](https://gitlab.com/e-os/e-os/-/commit/21551e227), `docs(R-D01): render Settings zweryfikowany + R-F08 root-cause (auto-aktywacja VT`)
- **U-074** — R-F08` fully root-caused (docs only) ([`ee2bf4e0c`](https://gitlab.com/e-os/e-os/-/commit/ee2bf4e0c), `docs(R-F08): pełna diagnoza root-cause (fbcond kradnie VT2 po greeterze) [U-074]`)
- **U-082** — Installer GUI produced a non-bootable disk; randd trusted failed rdrand (`G1`, entropy) ([`f2e480e9a`](https://gitlab.com/e-os/e-os/-/commit/f2e480e9a), `recipes: bump installer + base pins for G1 + rdrand fixes (U-082)`)
- **U-083** — aarch64 system clock no longer stuck at 1970 on an ACPI boot ([`65b1786b8`](https://gitlab.com/e-os/e-os/-/commit/65b1786b8), `recipes: bump bootloader + base pins for aarch64 clock fix (U-083)`)
- **U-084** — Fork CI revived ([`71b059540`](https://gitlab.com/e-os/e-os/-/commit/71b059540), `ci+recipes: revive fork CI in the e-os namespace — bump 9 pins (U-084)`)
- **U-087** — E-OS Notes verified fully interactive ([`c3aa2ad36`](https://gitlab.com/e-os/e-os/-/commit/c3aa2ad36), `recipes: bump eos-notes pin — TextInput keyboard fix (U-087)`)
- **U-091** — pages` must not block the OS build (`allow_failure`) ([`a3a2637c1`](https://gitlab.com/e-os/e-os/-/commit/a3a2637c1), `docs+ci: documentation standard, ARCHITECTURE, app guide, docs PDF + enforcement`)
- **U-092** — Heavy `build-image` detached from the shared-runner light tier (`needs: []`) ([`a3a2637c1`](https://gitlab.com/e-os/e-os/-/commit/a3a2637c1), `docs+ci: documentation standard, ARCHITECTURE, app guide, docs PDF + enforcement`)
- **U-093** — Documentation standard + tooling ([`a3a2637c1`](https://gitlab.com/e-os/e-os/-/commit/a3a2637c1), `docs+ci: documentation standard, ARCHITECTURE, app guide, docs PDF + enforcement`)
- **U-112** — eos-control: Network settings pane ([`00ef512ef`](https://gitlab.com/e-os/e-os/-/commit/00ef512ef), `docs(control): record R-902 Network settings pane (U-112) + bump eos-control pin`)
- **U-114** — ci(pins): un-redden the daily pipeline ([`0260ef8a2`](https://gitlab.com/e-os/e-os/-/commit/0260ef8a2), `ci(pins): allowlist the two R-902 pin holds; un-redden the daily pipeline (U-114`)
- **U-115** — docs: the great re-sync ([`c3a55959c`](https://gitlab.com/e-os/e-os/-/commit/c3a55959c), `docs: great re-sync to U-113 reality; ship screenshots on the docs site (U-115)`)
- **U-116** — docs(claude): CLAUDE.md re-synced with reality (its own "keep this file honest" rule) ([`579ca7bb0`](https://gitlab.com/e-os/e-os/-/commit/579ca7bb0), `docs(claude): re-sync CLAUDE.md with repo reality (U-116)`)
- **U-118** — supply-chain: every fetched build binary is now SHA256-pinned (audit §4 items 1–3) ([`86d715692`](https://gitlab.com/e-os/e-os/-/commit/86d715692), `build(supply-chain): SHA256-pin every fetched build binary (U-118)`)
- **U-121** — mirror hygiene: the stray ImgBot work is merged, the divergent branch retired ([`799556715`](https://gitlab.com/e-os/e-os/-/commit/799556715), `docs(changelog): record the ImgBot merge + mirror-branch cleanup (U-121)`)
- **U-123** — ops: the `eosbuild` container finally has a written way back ([`922b432b8`](https://gitlab.com/e-os/e-os/-/commit/922b432b8), `ops(ci): document + script the eosbuild container recovery (U-123)`)
- **U-124** — the U-114 outage was never data loss ([`c259335d6`](https://gitlab.com/e-os/e-os/-/commit/c259335d6), `ops(ci): eosbuild caches live in named volumes; recovery script kept them (U-124`)
- **U-127** — the shipping session was never "the COSMIC desktop" ([`2946c6ad1`](https://gitlab.com/e-os/e-os/-/commit/2946c6ad1), `docs: the shipping session is orbital, not "the COSMIC desktop" (U-127, R-D12)`)
- **U-129** — README re-verified against `U-118`…`U-128` and its `SYNC` marker finally moved ([`cbd9ef5ed`](https://gitlab.com/e-os/e-os/-/commit/cbd9ef5ed), `docs(readme): re-verify against U-118..U-128 and move the SYNC marker (U-129)`)
- **U-130** — usbnetd` RX was never broken ([`1597fe0b3`](https://gitlab.com/e-os/e-os/-/commit/1597fe0b3), `docs: usbnetd RX was never broken — recover the lost U-056/U-057 records (U-130,`)
- **U-131** — "history lives in the git log" was not quite true, and it was about to manufacture a false finding ([`bb4855006`](https://gitlab.com/e-os/e-os/-/commit/bb4855006), `docs(changelog): pre-U-071 history lives on an archived branch, not main's log (`)
- **U-136** — the text every user actually reads was still advertising a desktop we don't ship and a handbook that 404s ([`c14b7c174`](https://gitlab.com/e-os/e-os/-/commit/c14b7c174), `docs(image): fix the welcome text users actually read (U-136, R-D12 residue)`)
- **U-138** — CLAUDE.md gains the three sections it was missing: cadence, releases, and the host it actually runs on ([`2a22a8d82`](https://gitlab.com/e-os/e-os/-/commit/2a22a8d82), `docs(claude): add cadence, release discipline and host context (U-138)`)
- **U-139** — branch hygiene: 14 branches down to 5, and the mirror turns out not to replicate deletions ([`160e6995f`](https://gitlab.com/e-os/e-os/-/commit/160e6995f), `chore(repo): prune nine dead branches; record that the mirror ignores deletions `)
- **U-145** — the 13 recipes that actually ship are pinned ([`1c175c2ac`](https://gitlab.com/e-os/e-os/-/commit/1c175c2ac), `build(supply-chain): pin the 13 recipes that actually ship (U-145, R-F11)`)
- **U-147** — correcting my own `R-F16` wording: the defect is narrower than I published ([`5616cf99b`](https://gitlab.com/e-os/e-os/-/commit/5616cf99b), `docs(R-F16): correct my own scope claim -- the defect is initfs-only (U-147)`)
- **U-151** — a dead end on `R-F16`, recorded so the next attempt does not repeat it: `redoxfs` cannot be instrumented through stdout or stderr ([`74240c320`](https://gitlab.com/e-os/e-os/-/commit/74240c320), `docs(R-F16): record a dead end -- redoxfs cannot be instrumented via stdio (U-15`)

### Fixed

- **Generowana strona pokrycia przestała brudzić drzewo przy każdym przebiegu.** Zapisywała hash commita, przez co `verify.sh` nigdy nie zostawiał czystego `git status`, a taka churn uczy ludzi commitować wygenerowany plik bez czytania. Prowieniencję daje `git log docs/reference/coverage.md` i nie trzeba jej dublować w treści.
- **Nowa pułapka hosta P-16 i poprawka skryptu, który przez nią zabił 40-minutową budowę.** Zmierzone, nie wydedukowane: na macOS `mktemp -d` **bez szablonu ignoruje `$TMPDIR`** i pyta system o katalog per-user — `TMPDIR=/Volumes/EOS-Podman/tmp bash -c 'mktemp -d'` → `/var/folders/…`. Przez to `eos-esp-add-cert.sh` kopiował obraz 1400 MiB na **dysk wewnętrzny** niezależnie od tego, co wskazał wywołujący; przy 578 MiB wolnego budowa padła na **ostatnim** kroku, **po** poprawnym złożeniu obu obrazów. Dwie zmiany, bo każda osobno zostawia połowę wady: jawny szablon (`mktemp -d "${TMPDIR:-/tmp}/eos-esp-add-cert.XXXXXX"`) i sprawdzenie miejsca **przed** kopiowaniem, z nazwą urządzenia, punktem montowania oraz „ile trzeba kontra ile jest". **Kontrola przeciwna** na celowo małym wolumenie 20 MiB, ten sam obraz, ten sam `TMPDIR`: wersja **sprzed** poprawki skopiowała mimo wszystko **1401 MiB** na dysk wewnętrzny (5105 → 3703 MiB wolnego) i zakończyła się **zerem**; wersja po — **kod 2** z komunikatem „need 1464 MiB, have 19 MiB". Kod 2, nie 1: bramka nie mogła się wykonać, więc naprawia się przybornik, nie drzewo (`U-177`). `CLAUDE.md` dostaje `P-16` z pomiarem, bo ten sam `mktemp -d` stoi w pięciu innych skryptach.
- **U-075** — vesad: don't panic on a malformed bootloader-env line (`R-F09`) ([`a4165f5f4`](https://gitlab.com/e-os/e-os/-/commit/a4165f5f4), `fix(vesad): bump eos-base 98f22879 (R-F09 env-parse bez paniki) [U-075]`)
- **U-078** — Boot lands directly on the graphical greeter ([`61cb53e41`](https://gitlab.com/e-os/e-os/-/commit/61cb53e41), `fix(boot): pulpit startuje bez Super+F3 — greeter trzyma VT3 (R-F08) [U-078]`)
- **U-080** — Live-ISO text console (VT2) works again ([`7809be3d1`](https://gitlab.com/e-os/e-os/-/commit/7809be3d1), `fix(boot): konsola tekstowa VT2 dziala na live — getty 2 odblokowany (R-601) [U-`)
- **U-085** — Standalone installer writes the right EFI boot file without env `TARGET`; virtio drivers no ([`938ad8389`](https://gitlab.com/e-os/e-os/-/commit/938ad8389), `fix(installer,virtio)+recipes: config-driven installer target; graceful legacy v`)
- **U-104** — netsurf: fix the first-render crash ([`ceb732057`](https://gitlab.com/e-os/e-os/-/commit/ceb732057), `fix(netsurf): drop SDL_RESIZABLE — fix first-render use-after-munmap, browser re`)
- **U-113** — eos-control: render-verify of the Sieć tab surfaced (and fixed) a real on-device gap (`R-902`) ([`194cd8090`](https://gitlab.com/e-os/e-os/-/commit/194cd8090), `fix(control): U-113 — render-verify Sieć tab; fix netcfg-namespace read gap`)
- **U-125** — integrity` gate: stop scanning 15 GB of vendored upstream Rust and calling it "our own ([`b968453b3`](https://gitlab.com/e-os/e-os/-/commit/b968453b3), `fix(ci): scope the integrity gate to tracked sources (U-125)`)
- **U-128** — README roadmap Gantt: one missing brace made its theme directive invalid ([`edc995ece`](https://gitlab.com/e-os/e-os/-/commit/edc995ece), `fix(readme): close the mermaid init directive on the roadmap Gantt (U-128)`)
- **U-148** — the `R-F16` mechanism I published twice was wrong ([`89dcddc76`](https://gitlab.com/e-os/e-os/-/commit/89dcddc76), `fix(docs): correct the R-F16 mechanism, and record R-F17 (U-148)`)
- **U-149** — R-F17` fixed: a kernel return value that is correct by design no longer kills the storage driver ([`14353b3f5`](https://gitlab.com/e-os/e-os/-/commit/14353b3f5), `fix(irq): stop aborting nvmed on a stale INTx ack (U-149, R-F17)`)

### Security

- **U-081** — Security-fix pins land in the image ([`f70e5ac1e`](https://gitlab.com/e-os/e-os/-/commit/f70e5ac1e), `recipes(security): bump base/redoxfs/pkgutils pins — K-01, K-06, UB fix, R-703 (`)
- **U-090** — Guard v2 ([`616454916`](https://gitlab.com/e-os/e-os/-/commit/616454916), `recipes: bump eos-guard to v2 — permission audit + baseline digest (U-090)`)
- **U-095** — E-OS Control ([`1ca28d6ae`](https://gitlab.com/e-os/e-os/-/commit/1ca28d6ae), `feat(apps)+recipes: E-OS Control — unified control center, replaces sysmon+guard`)
- **U-117** — docs(coverage): close the "why is this here?" gaps the audit found in our own tree ([`010fd7057`](https://gitlab.com/e-os/e-os/-/commit/010fd7057), `docs(coverage): document patches/, orbdata pin, src/ vendoring, eos-repo-sign (U`)
- **U-119** — eos-repo-sign: keygen can no longer leak or clobber the repo-signing key (audit §4 item 6) ([`12b61cd64`](https://gitlab.com/e-os/e-os/-/commit/12b61cd64), `fix(eos-repo-sign): keygen writes 0600 secret, refuses to clobber keys (U-119)`)
- **U-120** — unsigned publish is now an explicit opt-in, not a silent default (audit §4 item 4) + gitleaks ([`75734e37e`](https://gitlab.com/e-os/e-os/-/commit/75734e37e), `sec(publish): unsigned publish requires EOS_ALLOW_UNSIGNED=1; gitleaks review (U`)
- **U-122** — docs(hardening): the supply-chain gates get their reference section ([`193c418c3`](https://gitlab.com/e-os/e-os/-/commit/193c418c3), `docs(hardening): reference section for the U-118/U-120 supply-chain gates (U-122`)
- **U-126** — docs honesty pass: the signing chain, the install guide, and a reality-ledger that ([`cd3a2dacb`](https://gitlab.com/e-os/e-os/-/commit/cd3a2dacb), `docs: separate publisher-side signing from the client half that does not exist (`)
- **U-134** — I got the signing chain wrong in `U-126 ([`21b81199b`](https://gitlab.com/e-os/e-os/-/commit/21b81199b), `docs(security): the client verifier exists — correct U-126 and un-invert the tru`)
- **U-135** — the pinned repo key had nowhere to land ([`879e2633b`](https://gitlab.com/e-os/e-os/-/commit/879e2633b), `feat(security): give the pinned repo key somewhere to land (U-135, R-702)`)
- **U-137** — one malformed vendor key could take down every driver binding at boot ([`a9295f974`](https://gitlab.com/e-os/e-os/-/commit/a9295f974), `fix(pcid): a malformed vendor key no longer breaks all driver binding (U-137, R-`)
- **U-140** — the pre-commit secret gate was decorative, `unsafe` had no rule at all, and nothing was signed ([`50ac4a066`](https://gitlab.com/e-os/e-os/-/commit/50ac4a066), `feat(security): close the secret gate, gate unsafe, state the signing gap (U-140`)
- **U-141** — full-ecosystem audit: the security posture document was itself the worst offender ([`1d3c62ea6`](https://gitlab.com/e-os/e-os/-/commit/1d3c62ea6), `docs(security): re-measure the posture table; six audit findings become R-F10..R`)
- **U-142** — docs/plan.md ([`c521590b1`](https://gitlab.com/e-os/e-os/-/commit/c521590b1), `docs(plan): editions, compartmentalisation model, and the order that is the cont`)
- **U-143** — E-OS images no longer ship a package source that undermines their own hardening (`R-701a`) ([`0a4677a6c`](https://gitlab.com/e-os/e-os/-/commit/0a4677a6c), `security(image): stop shipping the upstream package source (U-143, R-701a)`)
- **U-144** — raw IP sockets are no longer handed to every user program ([`c77a0fbde`](https://gitlab.com/e-os/e-os/-/commit/c77a0fbde), `security(net): take raw IP sockets away from user programs (U-144, R-904a)`)
- **U-152** — signing was already live and the docs said otherwise; the tag gap was structural; and a tag would have hung CI ([`374da27de`](https://gitlab.com/e-os/e-os/-/commit/374da27de), `docs(signing): re-measure signing, correct the DCO claim, unhang tag CI (U-152)`)

---

## [0.1.0] - 2026-06-07

Release: [`v0.1.0`](https://github.com/Gh0s777tt/E-OS/releases/tag/v0.1.0) — the only published release object.

**Reconstructed from `git log`, not from this file.** `CHANGELOG.md` did not exist yet at this tag; its own header records that history before `U-071` predates the file. The 18 commits between `eos-base-2026-06-06` and `v0.1.0` are Conventional Commits with unambiguous subjects, so the list below is a faithful reconstruction rather than an invention.

### Added

- feat(eos): E-OS build config + OS-level rebrand + desktop screenshot ([`0d14ebc12`](https://gitlab.com/e-os/e-os/-/commit/0d14ebc12))
- feat(bootloader): red/black E-OS bootloader theme (built from source) ([`897fa66d6`](https://gitlab.com/e-os/e-os/-/commit/897fa66d6))
- feat(userutils): brand console login prompt to "eos login:" (built from source) ([`919a84809`](https://gitlab.com/e-os/e-os/-/commit/919a84809))
- feat(orbdata): E-OS red/black login greeter + desktop wallpaper (built from source) ([`5dc7e14e9`](https://gitlab.com/e-os/e-os/-/commit/5dc7e14e9))
- feat(orbdata): E-OS launcher menu icon (replace Redox R), refresh previews ([`d57a356c1`](https://gitlab.com/e-os/e-os/-/commit/d57a356c1))
- feat(aarch64): E-OS aarch64 desktop config (full branding) ([`82af7a440`](https://gitlab.com/e-os/e-os/-/commit/82af7a440))
- feat(docs): mdBook documentation site + GitHub Pages workflow (R-1004) ([`60d938127`](https://gitlab.com/e-os/e-os/-/commit/60d938127))

### Changed

- build: point patched recipes at E-OS source forks (reproducible main) ([`4a09cf7c3`](https://gitlab.com/e-os/e-os/-/commit/4a09cf7c3))
- docs: aarch64 + reproducibility status (ROADMAP/README) + aarch64 bootloader screenshot ([`5799602bf`](https://gitlab.com/e-os/e-os/-/commit/5799602bf))
- docs(roadmap): refine R-401b - aarch64 boot blocker is upstream redoxfs/relibc, not disk-specific ([`6aca114b8`](https://gitlab.com/e-os/e-os/-/commit/6aca114b8))
- docs(install): document the graphical/TUI installer + image flashing (R-1001) ([`467a78020`](https://gitlab.com/e-os/e-os/-/commit/467a78020))
- docs(lts): lts/0.1 support line + policy (R-1002) ([`0fecbf6c7`](https://gitlab.com/e-os/e-os/-/commit/0fecbf6c7))
- docs(packages): E-OS package repository guide + publish helper (R-1003) ([`b4d2bfab8`](https://gitlab.com/e-os/e-os/-/commit/b4d2bfab8))

### Security

- brand: establish E-OS identity, docs, security & automation ([`cad6d4895`](https://gitlab.com/e-os/e-os/-/commit/cad6d4895))
- feat(fortify): CycloneDX SBOM + checksums + release workflow (R-301/R-302) ([`f17427863`](https://gitlab.com/e-os/e-os/-/commit/f17427863))
- docs(security): threat model + hardening + disk-encryption guides (R-304/R-305) ([`5f61baeef`](https://gitlab.com/e-os/e-os/-/commit/5f61baeef))
- fix(deps): bump time 0.3.44 -> 0.3.47 (CVE-2026-25727 stack-exhaustion DoS) ([`0d90e5c71`](https://gitlab.com/e-os/e-os/-/commit/0d90e5c71))
- feat(release): minisign-sign checksums + E-OS release public key (R-301) ([`630d98e40`](https://gitlab.com/e-os/e-os/-/commit/630d98e40))

---

## Before `v0.1.0`

**Not reconstructable, and deliberately not invented.** Everything before the
`eos-base-2026-06-06` marker is inherited Redox OS history — 9 849 commits that are upstream's
work, not E-OS's. Ten numbered items (`U-001`–`U-006`, `U-008`, `U-012`–`U-014`) referenced by
older documents appear nowhere in `main`'s history: they live on the archived, disjoint branch
`archive/pre-migration-de-phase1` and can be read with
`git show origin/archive/pre-migration-de-phase1:CHANGELOG.md`. That branch is **not an ancestor**
of `main`, so a citation to it resolves only there.
